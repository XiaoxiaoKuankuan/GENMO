# Repository working agreement

- Codex 每次对本仓库完成实质性的代码、配置、数据契约或文档修改后，必须在结束任务前按时间顺序追加更新根目录的 `记录文本.md`。
- 每条记录至少写明修改目标、主要实现、兼容性/功能边界和实际验证结果；存在分支差异时必须注明所属分支。
- 不重写或删除既有历史。纯只读检查、解释或未产生仓库修改的任务不需要新增条目。
- `记录文本.md` 的更新属于任务完成条件，不能仅依赖 Git commit message 或聊天记录替代。

## 代码同步约定

- GitHub 远端分支是代码、配置和文档的统一来源；常规修改必须先在本地工作树完成集成、
  测试、记录、提交和推送，再同步到训练服务器。
- 训练服务器原则上只执行对应分支的 `git pull --ff-only`，不要直接形成长期未提交的代码
  修改；训练日志、checkpoint、数据集和其他运行产物不纳入 Git。
- 确需在服务器紧急修复时，必须单独建分支并立即提交、推送，然后回到本地合并；在该修改
  被远端接收前，不应继续在另一份工作树修改同一代码。
- 同步前必须检查工作树、当前分支和远端差异；发现冲突时保留双方有效历史，完成验证后再
  推送，不使用覆盖式同步代替冲突解决。

## 测试产物清理约定

- 仓库中的 `tests/`、测试脚本和测试夹具属于长期维护的源代码，不得当作临时产物删除；
  本节清理对象仅指运行测试时生成的 smoke、test、preflight、parity、debug、dry-run、
  临时数据集、临时评测目录、缓存和日志。
- 每次运行测试前必须确定全部输出路径。测试产物只能写入系统临时目录或名称明确的独立
  测试目录，禁止写入正式训练的 `version_*`、`checkpoints/`、生产数据集和正式实验目录。
- 每次测试命令结束后，无论成功或失败，都必须在同一任务内自动删除本次产生的临时目录、
  `.pytest_cache`、`.ruff_cache` 和不再需要的 `__pycache__`，并执行一次残留检查；不得把
  smoke/test/preflight 产物长期留在本地或训练服务器。
- 删除前必须解析并复核精确路径，确认其中没有正式训练 checkpoint、训练日志、生产数据
  或用户要求保留的评测结果；禁止用宽泛目录、未解析变量或不受控通配符执行递归删除。
- 只有用户明确要求留存，或失败诊断必须跨任务继续时，才可保留测试产物；此时必须在
  `记录文本.md` 中写明绝对路径、保留原因和后续清理条件。

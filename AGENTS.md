# Repository working agreement

- Codex 每次对本仓库完成实质性的代码、配置、数据契约或文档修改后，必须在结束任务前按时间顺序追加更新根目录的 `记录文本.md`。
- 每条记录至少写明修改目标、主要实现、兼容性/功能边界和实际验证结果；存在分支差异时必须注明所属分支。
- 不重写或删除既有历史。纯只读检查、解释或未产生仓库修改的任务不需要新增条目。
- `记录文本.md` 的更新属于任务完成条件，不能仅依赖 Git commit message 或聊天记录替代。

## Git 分支模型

- `main` 是生产版本分支，必须始终保持可运行、已验证、可交付。禁止直接在 `main` 上进行
  功能开发、算法实验、模块重构或普通参数调整；`main` 只接收验证通过的 `release/*`，
  以及用户明确批准的紧急修复。
- `feature/*` 是功能开发分支，用于新功能、算法研发、模块重构和实验性验证。分支必须从
  最新且工作区状态已核对的 `main` 创建，名称使用小写短横线，例如
  `feature/smpl-music-only-genmo`；禁止误写为 `featur/*`。一个 feature 分支应尽量只包含
  一个明确主题。
- `release/*` 是发布准备分支，建议命名为 `release/vX.Y.Z`。该分支只允许发布前测试、
  Bug 修复、参数调整、兼容性修复和文档补充；禁止继续加入大型新功能或大规模重构。
  发布完成后必须把 release 修复同步回仍在开发的相关 feature 分支。
- `hotfix/*` 用于已发布版本的紧急修复，必须从 `main` 创建。修复验证后合并回 `main`，
  同步到活动的 `release/*` 和受影响的 `feature/*`，并创建补丁版本 Tag。

## Agent 操作与用户修改保护

- 用户已有的所有未提交修改一律视为正确修改和受保护内容。Agent 不得擅自丢弃、覆盖、
  回退、清理、stash、改写或混入无关提交；即使这些修改不属于当前任务，也必须原样保留。
  若当前任务必须修改同一文件，应先说明重叠范围和处理方式，并保留双方有效内容。
- 每次修改前必须检查并报告当前分支、HEAD SHA、`git status`、用户未提交修改，以及本地
  分支与远端的领先/落后关系。若位于 `main`，不得直接修改代码，应先获得用户授权并创建
  或切换到合适的 `feature/*`、`release/*` 或 `hotfix/*`。
- 未经用户明确授权，禁止执行 commit、push、merge、rebase、创建或删除 Tag、删除分支，
  以及其他改变共享仓库历史或远端状态的操作。
- 禁止使用 `git reset --hard`、`git clean -fd`、`git push --force` 等破坏性命令；禁止
  改写已推送提交历史。确需例外时，必须先解析精确目标、说明影响和恢复方案，并取得用户
  的单独明确授权。
- 分支创建、同步、提交等版本管理任务必须和功能实现修改分开执行；用户只要求建立分支
  或同步时，不得顺带修改实现。

## 提交、数据和记录

- 提交必须小而完整，一个提交只包含一个可解释主题。功能、Bug 修复、重构、测试、配置、
  数据契约和文档尽量分开提交；禁止无审查使用 `git add .` 或 `git add -A`，必须逐文件
  暂存并检查 staged diff。
- 提交前必须区分源代码、配置、数据契约、Checkpoint、模型和生成数据。Checkpoint、ONNX、
  CSV、NPZ、PKL、视频、日志、缓存和训练输出不得仅凭扩展名决定是否提交，必须逐文件确认
  用途；大型或生成产物只有在用户明确指定并确定 Git LFS 或外部存储策略后才能进入 Git。
- `记录文本.md` 的每项实质修改记录还应包含所属分支、起始 HEAD、配置/资产/数据/Checkpoint
  来源与关键 SHA256、测试命令及结果、未运行测试及原因、已知风险、输出产物和回滚方法。
- 推荐提交消息使用 `feat`、`fix`、`refactor`、`test`、`docs`、`config`、`data`、
  `chore` 等类型，并在正文说明改动、理由、影响边界、验证结果和未执行项。

## 验证、合并与发布

- 验证结论必须明确区分静态检查、单元测试、集成测试、推理 smoke、训练 smoke、完整训练
  和部署/媒体验收；低级别检查不得描述为高级别验证通过，未运行项必须说明。
- feature 合入 release、release 合入 main 前，必须先核对完整 diff、变更记录、测试证据、
  已知限制和回滚方法，并获得用户明确授权。共享分支不得随意 rebase；合并应保留可审计的
  分支和提交边界。
- 正式版本只允许在 `main` 的已验证提交上创建 annotated Tag，推荐语义版本
  `vMAJOR.MINOR.PATCH`；候选版本使用 `vX.Y.Z-rc.N`。Tag 说明必须包含主要功能、修复、
  关键配置/数据/Checkpoint 来源、验证结果和已知限制；禁止移动、覆盖或复用已发布 Tag。
- GitHub 应为 `main` 配置禁止直接 push、禁止 force push、禁止删除、必须通过状态检查和
  PR 审核等分支保护；`release/*` 建议采用同等保护。Agent 规则是操作约束，不能替代远端
  分支保护。

## 代码同步约定

- GitHub 远端分支是已提交代码、配置和文档的统一来源；用户明确要求提交或同步时，必须先
  在本地工作树完成集成、测试、记录和逐文件审查，再提交、推送并同步到训练服务器。
- 训练服务器原则上只执行对应分支的 `git pull --ff-only`，不要直接形成长期未提交的代码
  修改；训练日志、checkpoint、数据集和其他运行产物不纳入 Git。
- 确需在服务器紧急修复时，必须单独建分支并立即提交、推送，然后回到本地合并；在该修改
  被远端接收前，不应继续在另一份工作树修改同一代码。
- 同步前必须检查工作树、当前分支和远端差异；发现冲突时保留双方有效历史，完成验证后再
  推送，不使用覆盖式同步代替冲突解决。

## 测试产物清理约定

- 仓库中的 `tests/`、测试脚本和测试夹具属于长期维护的源代码，不得当作临时产物删除；
  本节清理对象仅指运行测试时生成的 smoke、test、preflight、parity、debug、dry-run、
  临时数据集、临时评测目录、缓存和日志。
- 每次运行测试前必须确定全部输出路径。测试产物只能写入系统临时目录或名称明确的独立
  测试目录，禁止写入正式训练的 `version_*`、`checkpoints/`、生产数据集和正式实验目录。
- 每次测试命令结束后，无论成功或失败，都必须在同一任务内自动删除本次产生的临时目录、
  `.pytest_cache`、`.ruff_cache` 和不再需要的 `__pycache__`，并执行一次残留检查；不得把
  smoke/test/preflight 产物长期留在本地或训练服务器。
- 删除前必须解析并复核精确路径，确认其中没有正式训练 checkpoint、训练日志、生产数据
  或用户要求保留的评测结果；禁止用宽泛目录、未解析变量或不受控通配符执行递归删除。
- 只有用户明确要求留存，或失败诊断必须跨任务继续时，才可保留测试产物；此时必须在
  `记录文本.md` 中写明绝对路径、保留原因和后续清理条件。

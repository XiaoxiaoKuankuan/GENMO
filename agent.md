# GENMO 测试、渲染与交付产物约定

本文件记录用户于 2026-09-16 明确提出的长期工作规则，适用于本仓库后续代码修改、
模型测试、评测、渲染和网页交付。仓库公共约定同时见 `AGENTS.md`；执行任务前须读取
本文件。本文是操作规则，不代表已经为现有脚本实现了自动清理或网页历史替换功能。

## 临时测试产物必须清理

- 每次测试前确定所有输出路径；临时 smoke、test、preflight、parity、debug、dry-run
  产物写入系统临时目录或明确隔离的临时目录，成功或失败都在同一任务内清理并检查残留。
- `tests/` 中的测试源代码、测试脚本和夹具需要保留，不属于临时产物。
- 用户明确指定以下历史临时目录应清理；后续若再次生成，也必须在任务结束前删除：

```text
outputs/AIOZ-GDANCE-extracted-smoke3-local/
outputs/CoMPAS3D-smoke5-local/
outputs/FineDance-smoke10-local/
outputs/FineDance-smoke10-renders/
outputs/aistpp_official_dryrun/
outputs/beat2_build_dryrun/
outputs/humanml3d_test/
outputs/gem_smpl_music_only/inference_smoke/
outputs/gem_smpl_music_only/eval_smoke/
```

- 删除时使用解析、复核后的精确路径，核实不含正式训练 checkpoint、训练日志、生产
  数据或仍需保留的交付内容；不删除父实验目录，不使用宽泛通配符，不跟随软链接删除外部数据。

## 同一功能复用测试和渲染脚本

- 同一功能的不同模型版本、checkpoint、日期或训练步数，优先调用同一个已有脚本；
  需要改进时修改原脚本，通过参数传入模型、配置、输入清单和输出目录。
- 不为每个 checkpoint 或每次渲染复制一份脚本，不在 `outputs/` 中继续新增长期使用的
  测试、渲染或网页生成实现；可复用入口放在 `scripts/`、`scripts/demo/`、`tools/eval/`
  等源码目录，测试放在 `tests/`，公共逻辑放在 `gem/`。
- 不同功能可以保留独立入口。例如 SMPL 音乐生成、BUMI 音乐生成、SMPL 文本生成
  的表示和条件不同，不因名称相似强行合并。

## 每种功能只保留最新一套有效测试交付

- 同一功能的测试输出文件、生成动作、视频、报告和网页默认只保留最新一套有效结果；
  模型版本、checkpoint 步数和日期不同不构成保留重复旧结果的新功能。
- SMPL 音乐生成、BUMI 音乐生成、SMPL 文本生成等不同功能分别保留模型及其最新结果，
  不得因为某一种功能更新就清掉其他功能的模型或结果。
- 一套结果可以包含完整评测清单中的多首音乐、多条文本、原始/生成对照和不同场景；
  “只保留最新”不是只留下最后一个视频，也不能把未覆盖的场景误判成重复旧结果。
- 新结果先写入隔离的临时位置，完成文件、动作和媒体验收后更新该功能的正式输出或
  最新索引，再清理被替代的旧结果及失效网页引用；失败时保留上一套有效结果。
- 本规则不授权按测试清理逻辑删除训练 checkpoint、训练事件、生产数据或恢复训练所需
  资产；清理同一功能的旧模型权重应另行明确范围。用户另行要求保留的固定对照或正式
  交付按该次明确要求执行，并在 `记录文本.md` 写明路径、原因和后续清理条件。
- 本次登记不对全部历史网页、视频做不加区分的批量删除；后续更新某一功能时按上述
  完整结果范围核验、替换、清理。

## 每次交付说明

- 说明复用或修改了哪个脚本、实际模型/配置、输入清单、输出绝对路径、保留的最新结果，
  以及删除了哪些临时目录和旧结果；实质修改与实际验证追加到 `记录文本.md`。
- `tools/eval/` 用于对真实模型/动作执行评测、导出一致性检查和渲染；`tests/` 用于
  验证这些实现的正确性、数据契约和回归行为。单元测试通过不等于真实模型质量验证通过。

## Git提交说明必须使用详细中文（2026-09-22）

- 此后所有Git提交说明的标题和正文均使用中文；代码标识符、路径和必要专有名词可保留原文。
- 提交标题说明具体目标；正文详细记录修改原因、主要文件与实现、兼容性或功能边界，以及实际验证结果。
- 不用简短英文标题或笼统的“修复问题”替代详细说明，不把未执行的测试、训练、部署或质量验收写成已完成。
- 本规则适用于本仓库各工作树及后续服务器同步提交；仍须按根AGENTS.md追加记录文本.md。

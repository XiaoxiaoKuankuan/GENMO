# checkpoint 保存回调配置。
# 每 1000 个 step 保存一次，适合调试或更频繁保存的实验。
every1000s_top100:
  _target_: gem.callbacks.simple_ckpt_saver.SimpleCkptSaver
  output_dir: ${output_dir}/checkpoints/
  every_n_steps: 1000
  save_top_k: 100

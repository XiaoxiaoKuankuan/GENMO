# 部署包配置；路径相对于部署目录，不必修改终端环境变量。
[runtime]
backend = tensorrt
device = cuda:0
manifest = deployment.json
# 其他电脑改成该电脑已有的本地T5-3B快照，或复制到部署包的models/t5-3b。
t5_model = models/t5-3b
output_root = outputs/bumi_text
num_frames = 120
ddim_steps = 50
seed = 42

[preview]
enabled = true

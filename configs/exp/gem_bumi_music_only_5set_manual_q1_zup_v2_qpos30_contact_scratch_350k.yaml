# @package _global_
# 统一 Z-up 五库 qpos30/contact 的 350k、8 卡、完全从零训练入口。
#
# 该实验仅允许通过 BUMI_MUSIC_QPOS30_STATS_PATH 注入新五库 Z-up v2 统计量；所有旧 checkpoint、
# optimizer 和 global step 入口均置空，避免从错误 Y-up→机器人躺倒数据训练出的权重续训。
# 每卡 batch=192、全局 batch=1536，四个公开库使用重新重定向和重算接触的数据，自建库沿用
# 原生正确 Z-up 数据。配置仍使用 qpos30、两维足接触 head 和既有 FK 物理损失。
defaults:
  - gem_bumi_music_only_5set_manual_q1_v3
  - override /network: diffusion_lg_bumi30_contact
  - override /pipeline: music_only_bumi_qpos30_contact_v2
  - override /endecoder: bumi_qpos30_contact
  - override /scheduler: iters_half_210k_315k
  - override /callbacks:
      - ckpt_saver/every5000s_top100
      - prog_bar/prog_reporter_ed1
      - train_speed_timer/base
      - lr_monitor/pl
  - _self_

exp_name_base: gem_bumi_music_only_5set_manual_q1_zup_v2_qpos30_contact_scratch_b192_s350k
exp_name_var: ""
exp_name: ${exp_name_base}${exp_name_var}
data_name: gem_bumi_music_only_5set_manual_q1_zup_v2_qpos30_contact_scratch_350k
output_dir: outputs/${exp_name}

pretrain_ckpt: null
ckpt_path: null
resume_mode: null

model:
  model_cfg:
    checkpoint_adapter: null

network:
  model_cfg:
    denoiser:
      static_conf_dim: 2

pipeline:
  args:
    ground_semantics: mixed_floor_zero_fk_contact_v2

data:
  stats_path: ${oc.env:BUMI_MUSIC_QPOS30_STATS_PATH}
  loader_opts:
    train:
      batch_size: 192

optimizer:
  lr: 0.0001

pl_trainer:
  max_steps: 350000
  val_check_interval: 5000
  devices: 8
  strategy: ddp
  use_distributed_sampler: false

use_wandb: false

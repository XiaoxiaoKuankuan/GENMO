# step 级学习率调度配置。
# 备用实验配置：在 30000 和 50000 个 optimizer step 将学习率乘以 0.5。
scheduler:
  _target_: torch.optim.lr_scheduler.MultiStepLR
  milestones: [30000, 50000]
  gamma: 0.5
interval: step
frequency: 1

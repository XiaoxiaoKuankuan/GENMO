# HumanML3D 评测数据集配置。
# 用于文本动作生成评测/调试，默认只取随机子集，避免验证耗时过长。
humanml3d_eval:
  _target_: gem.datasets.pure_motion.humanml3d.Humanml3dDataset
  eval_gen_only: true
  cam_augmentation: v11
  use_random_subset: true
  random_subset_size: 2
  random_subset_seed: 7

# 已退役的 BUMI 资产与表示

## 2026-09-23 音乐分支入口清理补充

按用户本轮授权，`feature/bumi-music-only` 进一步删除 3 个 manual_q1 BUMI 与 9 个
SMPL 实验、人体/文本/Webcam/多模态/旧 GMR 专用入口和对应测试。混合测试中的当前
音乐条件、DDIM、GMT 轨迹、数据处理和定时器能力已抽出保留，不再原位保留旧专用测试。
训练及工具的默认实验已改为 UMR70+Mine；下方“历史测试源码原位保留”的说明仅适用于
2026-09-22 那一轮操作，不是本轮状态。完整范围见 [分支维护范围](BUMI_MUSIC_BRANCH_SCOPE.md)。
生产资产与其他分支不变，历史源码可从清理前提交 `270cbb3b54a188f5fb64424e0e282c3b659e3d55`
恢复；不改写 Git 历史。

## 2026-09-22 历史记录

2026-09-22按用户要求跨分支移除旧482138运动学/脚底代理点、旧GMR/SONIC默认质量规则、
93D编解码/网络/pipeline配置及compute_bumi_93d_stats.py。旧数据、模型权重和正式报告
不随此次代码清理删除；需要历史复现时使用对应的历史提交。

删除18个旧资产/规则/93D配置及入口；5个现行qpos30实验展开旧父配置，合成内容逐项保持一致。

本分支保留的筛选CLI必须显式传入 `--config`，构建器若仍存在则必须传入
`--quality-config`，规则必须与输入的资产身份、帧率和既有报告匹配，禁止静默替换成其它
姿态/机器人阈值。历史测试源码原位保留，已退役功能的用例应在其历史代码上运行。

现行文本训练与公共模块整理见 `feature/bumi-text-only` 分支的README；现行机器人音乐
训练使用 `feature/bumi-music-only` 的qpos30实验。其它分支的原有 `exp=mixed` 缺省值
仍需在命令行覆盖为实际存在的实验，这不是本次新增问题。

实际验证：5个qpos30实验Hydra合成前后逐项一致；表示/根高回归22 passed，指定当前fe934资产；显式SMPL实验gem_smpl_music_only_4set_physics_v1配置合成、SMPL数据集包与BUMI运动学包导入通过；保留Python源码AST及已删除模块静态引用检查通过；git diff --check通过。
没有运行正式训练、生产数据重建、GPU推理或动力学验收，没有同步/重启服务器作业。

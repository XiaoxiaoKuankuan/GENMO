# 独立缓存仿真耗时记录与500 Hz配置调整

日期：2026-09-07。对应用户14:58启动、15:01结束的独立缓存模式测试。

## 本次2000 Hz运行结果

用户反馈：这次独立模式的动作效果已经与离线接近。以下记录时间，不将主观观感当成自动跟踪质量验收。

| 范围 | 轨迹时间 | 本次实际执行时间 |
| --- | --- | --- |
| 原《火力全开》音乐/舞蹈 | 标称66.00秒；原离线NPZ首末帧间隔也是66.00秒 | 不用整个仿真进程时长代替 |
| 本次缓存中的纯舞蹈段 | 3299帧，50 Hz，首末帧间隔65.96秒 | 约110.68秒 |
| 从开始消费缓存到切回站姿，含前导、缓入、返回 | 3400个策略步，约68.00秒 | 约114.12秒 |

纯舞蹈段对应约 **0.596倍实时速度、每真实秒29.80次策略更新**。这说明动作按仿真步推进后，时间轴一致了，但本次运行仍没有达到真实50 Hz。

“实际执行时间”用CSV随帧记录的Bridge真实时间戳估算，不是控制器逐帧墙钟打点；存在心跳采样及通信误差，因此按约数使用。它不是GMT模型单次推理耗时。生成、初始化、手动切换模式和结束后的关机时间不计入纯舞蹈耗时。

## 计时依据

- 实际启动参数：`ac_start.launch decimation:=40 gmt_mode:=buffered gmt_redis_key:=gmt_buffered_frame_bumi gmt_redis_ack_key:=gmt_buffered_frame_bumi_ack`。
- ROS会话：`7bf41288-aa89-11f1-b543-30560fa492c7`，启动于`2026-09-07 14:58:09.222 +08:00`。日志确认`GMT BUFFERED: complete clip, one frame per simulation policy step`。
- CSV共4038行；缓存开始于数据行索引920，下一站姿流开始于索引3976。索引从0起，不含表头。
- 缓存中的`gmt_source_sequence`实际是消费帧号，而不是网络包计数；整段满足`gmt_policy_sequence - 帧号 = 291`。
- 纯舞蹈为缓存帧50～3348。对应真实时间戳：`1788764355.1276898`到`1788764465.8077047`，即`14:59:15.127690`到`15:01:05.807705`，差`110.6800148487091秒`。
- 整段缓存起点到下一站姿流：`1788764353.3476763`到`1788764467.4677613`，即`14:59:13.347676`到`15:01:07.467761`，差`114.12008500099182秒`。
- 已用CSV中的21关节参考核对分段：帧10～49符合40帧缓入，帧3349～3398符合50帧返回；与源码smoothstep计算的最大误差分别为`2.44e-8`和`3.21e-8`。缓存还有用于未来参考窗口的尾垫，不能把全部3500帧都当成实际舞蹈时长。
- 原音乐是`3168000 / 48000 = 66秒`；原离线NPZ是3301帧、50 Hz，首末帧间隔`(3301-1)/50 = 66秒`。本次纯舞蹈3299帧与原离线文件的尾端口径略有差异，不能称为逐帧相同。

原始CSV SHA256：

```text
820e31c53b6b734d2872c8c400e5ea3050044886463a602990779fa372aefe12
```

原始CSV、完整ROS日志及修改前四份配置已归档，供下次对照，不作为临时测试产物清理；仅在用户确认不再需要或要求删除后清理：

[2000 Hz基线目录](/home/weili/GENMO/outputs/gmt_simulation_baselines/20260907_145809_buffered_2000hz)

## 已完成的配置调整

仅修改实际部署目录`/home/weili/docker_projects/bumi_GMT_deployment_listao/bumi_GMT_deployment_obs`。

| 项目 | 修改前 | 修改后 |
| --- | --- | --- |
| Gazebo物理步长 | 0.0005秒 | 0.002秒 |
| Gazebo目标更新频率 | 2000 Hz | 500 Hz |
| 仿真控制基础频率 | 2000 Hz | 500 Hz |
| 策略分频 | 40 | 10 |
| 策略频率（按仿真时间） | 50 Hz | 50 Hz，保持不变 |
| GMT绘图及CSV共用节点 | 默认启动 | 默认不启动 |

- `simulation.sh`：显式传入500 Hz、分频10、关闭绘图/CSV；独立入口`simulation_buffered.sh`调用它，因此一起生效。
- `ac_start.launch`：同样更新默认值，直接运行roslaunch也保持一致。
- `load_ac_controller.launch`：基础频率默认值改为500，避免独立加载仍回到2000。
- `empty_world.world`：同步修改物理步长与目标频率，两者乘积仍为1。

机器人资产、轨迹、策略模型、关节顺序、PD增益、保护逻辑和`gazebo.delay=0.009`均未修改。现有`iters=2000`不是Hz，本次保留；未借此次调频修改求解器配置。物理步长变化仍可能影响接触、速度差分及延迟量化，需在后续仿真中检查动作稳定性。

取消的是绘图/CSV节点；控制器内部诊断发布仍存在，只是随基础频率降低。不删除历史CSV，不取消必要日志和保护，不修改实机入口的绘图默认设置。

## 验证与重新启动

- shell语法、XML和本次增量的空白检查通过。
- 在`noetic`容器解析实际ROS配置，buffered/online/offline三种模式均为500/10，均不包含绘图、CSV或录包节点；实机入口仍为原有500/10。
- Gazebo自身的SDF解析确认物理步长0.002、更新频率500。实际BUMI3控制插件未设置独立`controlPeriod`，沿用物理步进；原P3D状态发布已为500 Hz，无需改资产。
- 本次没有启动新一轮Gazebo、控制器或实机，没有写生产Redis；因此500 Hz配置下的真实耗时和跟踪效果仍待运行验证。
- 配置验证使用的容器临时目录`/tmp/gmt_500hz_config_check.ecXqO8`已检查为空并删除；用户本次ROS日志和归档数据保留不动。

本次没有修改C++，**不需要重新编译**。在原容器重新启动独立模式：

```bash
cd /host/Documents/bumi_GMT_deployment_obs
bash ./simulation_buffered.sh
```

Bridge和Console继续使用原来的独立模式启动指令。需要临时恢复绘图和CSV时，可显式传入`plot_gmt_actions:=true`，但性能对照应保持两轮采集条件一致。

## 15:26的500 Hz实测尝试：未完成整段舞蹈

按用户要求实际启动了隔离仿真，保留Gazebo界面，关闭绘图/CSV；使用ROS/Gazebo端口11341/11342、
临时Redis端口16479和独立Bridge端口17023。测试加载当前启动配置、机器人和策略，
仅跳过不参与控制的URDF导出节点，避免覆盖用户的`/tmp/legged_control/bumi.urdf`。

- Gazebo运行时服务确认：物理步长`0.002秒`、最大更新率`500`；控制器日志确认基础频率`500`、分频`10`。
- “启动控制→切换行走”的初始化区间：仿真推进`7.064秒`，真实时间`7.240114秒`，实时率约`0.976`。
  这段没有进入GMT舞蹈，不能据此估算整首耗时或认定GMT实际达到50 Hz。
- 自动顺序为控制→趴姿等待3仿真秒→站姿等待4仿真秒→行走。切入行走后立刻打印“摔倒保护”，
  控制模式回退，尚未进入GMT，也未发送音乐生成请求。因此**本次没有得到500 Hz下的完整舞蹈执行耗时**。
- 代码核对发现额外的频率耦合：站立过渡使用`standDuration_=2*500`个基础循环，并按每个循环递增。
  2000 Hz时过渡约0.5仿真秒，500 Hz时约2仿真秒；这是当前代码的启动行为变化，尚不能断言就是倒地原因。
- 没有修改保护、增益、站立时间或机器人状态来绕过失败。已询问用户正常初始化时是否需要手动扶正/摆放，
  待确认实际步骤后再按一致条件测量。
- 所有测试进程已停止，私有端口已释放；生产Redis、旧CSV、原ROS latest和共享URDF未动。
  为继续定位此次前置失败，暂保留主机`/tmp/gmt_500hz_runtime.Q9YbD2`与容器
  `/tmp/gmt_500hz_runtime.hO0XgA`中的临时探针及日志；在故障定位完成、成功测完或用户决定不再继续时删除。

# 修改实现记录

本文档用于持续记录 Codex 对本仓库所做的代码修改。后续每次实质修改必须在任务结束前按时间顺序追加一条记录，包含修改目标、涉及范围、实现方式、兼容性/功能边界和实际验证结果。不能只用 Git commit message 或聊天记录代替本文档；不同分支的修改必须显式标注分支。

## 2026-07-14：GEM-SMPL 实时输出接入 SONIC ZMQ Protocol v3（初版）

> 历史说明：本节保留初版实现记录。其中 `supermotion_smpl24` 关节生成方式已在下方“SONIC FK joint 转换”记录中替换，不再是当前实现。

### 修改目标

在不改变 GEM 模型、ONNX 加载、denoiser、SMPL 网络和原始 webcam 推理逻辑的前提下，增加以下实时数据链路：

```text
Camera / Video
  -> GENMO WebcamGEMSMPLDemo
  -> GEM-SMPL realtime SMPL
  -> ZMQ Protocol v3
  -> SONIC
  -> MuJoCo G1
```

### 涉及文件

- `scripts/demo/demo_webcam_sonic.py`
  - 新增 SONIC webcam demo。
  - 继承并复用 `scripts/demo/demo_webcam.py` 中的 `WebcamGEMSMPLDemo`。
- `gem/utils/sonic/zmq_publisher.py`
  - 新增异步 `SonicPublisher` 和 Protocol v3 打包兼容层。
- `gem/utils/sonic/__init__.py`
  - 导出 `SonicPublisher`。
- `setup.cfg`
  - 增加 `pyzmq` 运行依赖。

原文件 `scripts/demo/demo_webcam.py` 未修改。

### Webcam streaming adapter

`WebcamGEMSMPLSonicDemo` 继承 `WebcamGEMSMPLDemo`，保留以下原有参数和功能：

- `--camera_id`
- `--video`
- `--no_imgfeat`
- `--render`
- `--context_frames`
- `--yolo_period`
- `--vitpose_period`
- `--render_mode`
- `--render_port`
- 原异步 pipeline 开关

新增参数：

- `--sonic_zmq`：启用 SONIC 发布。
- `--sonic_host`：PUB socket 绑定地址，默认 `localhost`。
- `--sonic_port`：PUB socket 绑定端口，默认 `5556`。
- `--sonic_topic`：消息 topic，默认 `pose`。

Adapter 覆写 `process_frame()`，先调用原实现，然后只处理 `ready=True` 的推理结果。原异步 pipeline 会在新 backend 结果完成前多次返回同一个 pending result，因此 adapter 使用内部 body-params 对象身份去重，避免相同预测被重复发布。

Publisher 在 demo 初始化时启动；初始化失败或主循环退出时都会关闭 publisher。未启用 `--sonic_zmq` 时不会导入或启动 ZMQ publisher。

### 异步 publisher

`SonicPublisher.publish_smpl()` 是 producer 端接口，只执行以下轻量操作：

1. 检查输入 frame 数量。
2. 在 producer 侧预留连续的 `frame_index`。
3. 将已经 detach 到 CPU 的 body params 非阻塞地放入有界队列。

SMPL forward、SciPy quaternion 转换、协议打包和 ZMQ send 全部在独立 consumer 线程执行，不进入 GEM GPU inference 路径。

队列默认长度为 2。队列满时丢弃最旧的未发送姿态并保留新姿态，以保证实时性。`frame_index` 在入队前生成，因此发生丢帧时，SONIC 可以从索引缺口识别时间跳跃。

Publisher 使用显式生命周期状态和线程锁管理：

- `disconnected`
- `starting`
- `connected`
- `closing`

已处理启动/关闭竞争、关闭超时、worker 异常退出和关闭后重连等情况。ZMQ 使用 `NOBLOCK` 和有限 `SNDHWM`，避免 subscriber 速度影响 inference producer。

### SMPL 数据转换

输入来自 webcam ready result：

- `body_params_incam`
  - `body_pose`
  - `global_orient`
  - `transl`
  - `betas`
- `body_params_global`
  - `global_orient`
  - `transl`

转换时优先使用 global 的 root orientation 和 translation，并从 incam 参数补充 `body_pose` 与 `betas`。

关节计算复用 GENMO 已有工具：

```python
make_smplx("supermotion_smpl24")
```

该模型接受 GEM 的 21-joint、63D body pose，通过项目已有 SMPL-X LBS 和 SMPL-neutral joint regressor 输出 `[N, 24, 3]`，没有重新实现 SMPL。

四元数转换使用：

```python
Rotation.from_rotvec(global_orient).as_quat()
```

SciPy 输出顺序为 `[x, y, z, w]`，发送前转换为 SONIC 要求的 `[w, x, y, z]`。

### Protocol v3 输出字段

每条 pose payload 包含：

| 字段 | Shape | Dtype | 来源 |
|---|---:|---|---|
| `smpl_pose` | `[N, 21, 3]` | `float32` | GEM `body_pose` |
| `smpl_joints` | `[N, 24, 3]` | `float32` | GENMO SMPL24 body model |
| `body_quat` | `[N, 4]` | `float32` | global orientation，`wxyz` |
| `joint_pos` | `[N, 29]` | `float32` | Protocol v3 零占位 |
| `joint_vel` | `[N, 29]` | `float32` | Protocol v3 零占位 |
| `frame_index` | `[N]` | `int64` | producer 递增索引 |

GENMO 当前没有直接输出经过 G1 retargeting 的 29-DoF qpos/qvel，因此 `joint_pos` 和 `joint_vel` 使用合法的零占位；主要人体动作由 SMPL 字段承载。本次修改没有额外引入 G1 retargeter，也没有改变 GEM 推理结果。

### ZMQ wire format

优先导入 GR00T-WholeBodyControl 的官方实现：

```python
from gear_sonic.utils.teleop.zmq.zmq_planner_sender import pack_pose_message
```

如果 GR00T Python package 不在 `PYTHONPATH`，使用本地兼容实现。实际 wire format 为一个 single-part ZMQ message：

```text
[topic bytes]
+ [1280-byte NUL-padded JSON header]
+ [按 header fields 顺序拼接的连续 NumPy binary payload]
```

JSON header 包含：

- `v: 3`
- `endian: "le"`
- `count: 1`
- `fields: [{name, dtype, shape}, ...]`

SONIC 使用 SUB socket 并主动连接 publisher，所以 `SonicPublisher.connect()` 内部创建 PUB socket 并执行 `bind()`。默认 endpoint 为：

```text
tcp://localhost:5556
```

跨机器使用时，GENMO 侧可指定 `--sonic_host 0.0.0.0`，SONIC 侧连接 GENMO 主机 IP。

### 日志

Publisher 成功启动时输出：

```text
[SONIC] ZMQ publisher started
```

每发送 100 帧输出：

```text
[SONIC]
fps=...
frames=...
smpl_pose_shape=...
smpl_joint_shape=...
```

### 运行方法

```bash
CUDA_VISIBLE_DEVICES=0 python scripts/demo/demo_webcam_sonic.py \
  --camera_id 2 \
  --no_imgfeat \
  --sonic_zmq
```

原 webcam demo 保持原样：

```bash
python scripts/demo/demo_webcam.py
```

### 验证结果

已完成以下验证：

- Ruff lint 通过。
- Ruff format check 通过。
- Python `py_compile` 通过。
- 新旧 demo CLI 默认参数兼容检查通过。
- 使用真实 `supermotion_smpl24` 模型验证输出 shape、dtype 和 quaternion 顺序。
- 本地 fallback packer 与 GR00T 官方 `pack_pose_message` 逐字节一致。
- 本地真实 ZMQ PUB/SUB Protocol v3 收发通过。
- queue 满载时 producer 保持非阻塞，丢帧索引缺口正确保留。
- connect/close 启动竞争、关闭超时、worker fatal error 和重连测试通过。
- `git diff --check` 通过。

## 2026-07-14：切换为 SONIC FK joint 转换

> 历史说明：本节记录的 raw-root local 转换已被下方“严格 SONIC 坐标、50 Hz 与 5 帧窗口”替换；当前实现已完整加入 Y-up -> Z-up 和 `remove_smpl_base_rot`。

### 修改目标

`smpl_joints` 不再来自 GENMO `supermotion_smpl24` 的 posed-vertex joint regression，而是严格使用 GR00T-WholeBodyControl 的 `compute_human_joints()` 生成 SONIC 定义的 24 个 FK joints，然后转为 root-local 坐标发送。

### 涉及文件

- `gem/utils/sonic/smpl_converter.py`
  - 新增 `SonicSMPLConverter`。
  - 将 GR00T-WholeBodyControl 仓库根目录加入 `sys.path`。
  - 导入 SONIC 原生 `compute_human_joints` 和 quaternion 工具。
  - 使用绝对路径加载 SONIC `human_joints_info.pkl`，避免受 GENMO 启动目录影响。
- `gem/utils/sonic/zmq_publisher.py`
  - 删除 SONIC adapter 内的 `make_smplx("supermotion_smpl24")` 和 `model_output.joints[:, :24]` 路径。
  - consumer 线程中初始化 converter，并将 `smpl_joints_local` 作为 Protocol v3 `smpl_joints`。
  - 保留初版 `smpl_pose_shape`/`smpl_joint_shape` 日志 key，同时增加分段的 pose/joints shape debug。
  - 每 100 帧增加 pelvis、left wrist 和 right wrist 的 local position 日志。
- `scripts/demo/demo_webcam_sonic.py`
  - 新增 `--sonic_repo_path`，默认指向 GENMO 同级的 `GR00T-WholeBodyControl` 仓库。
- `gem/utils/sonic/__init__.py`
  - 导出 `SonicSMPLConverter`。
- `记录文本.md`
  - 保留初版历史并追加本次实现说明。

### 原来的 joint 生成方式

初版 publisher 在 consumer 线程创建：

```python
make_smplx("supermotion_smpl24")
```

然后从 body model 输出取 `model_output.joints` 并截取前 24 点。该路径与 SONIC 的纯 FK joint 定义形状一致，但数值语义不一致。

### 修改后的 joint 生成方式

converter 优先使用 `body_params_global` 的 root orientation，并从 `body_params_incam` 获取 GEM 的 63D `body_pose`：

```python
joints = compute_human_joints(
    body_pose=body_pose[..., :63],
    global_orient=global_orient,
    human_joints_info_path=absolute_joint_info_path,
)
```

SONIC 输出顺序是 SMPLH 前 22 个 body joints，再追加左右 thumb joints，shape 为 `[B, 24, 3]`。其中调试索引为：

- pelvis: `0`
- left wrist: `20`
- right wrist: `21`

然后按本次要求的明确流程转换：

```text
global_orient axis-angle
  -> SONIC quaternion [w,x,y,z]
  -> inverse root quaternion
  -> rotate SONIC FK joints
  -> smpl_joints_local [B,24,3]
```

本 adapter 不向 joints 加入 `transl`，不发送 world joints，也不使用 `betas` 重算骨架。按用户给出的伪代码与箭头流程，这里使用原始 `global_orient` 求逆，没有额外混入 Pico 路径中可选的 y-up to z-up/base-rotation 处理。

`smpl_pose` 仍是 GEM `body_pose` 直接 reshape 为 `[B, 21, 3]`，不做重排序。`body_quat` 仍由 SciPy axis-angle 转换并按 `[w,x,y,z]` 发送。

### 数据流

```text
Camera / Video
  -> 原 WebcamGEMSMPLDemo（不变）
  -> ready body_params_global + body_params_incam
  -> producer 非阻塞入队
  -> consumer 线程
  -> SONIC compute_human_joints FK
  -> inverse-root rotation -> smpl_joints_local
  -> Protocol v3 pack_pose_message
  -> ZMQ PUB -> SONIC -> MuJoCo G1
```

### 运行方法

```bash
CUDA_VISIBLE_DEVICES=0 python scripts/demo/demo_webcam_sonic.py \
  --camera_id 2 \
  --no_imgfeat \
  --sonic_zmq
```

如果 GR00T 仓库不在 GENMO 同级目录：

```bash
--sonic_repo_path /path/to/GR00T-WholeBodyControl
```

### 兼容性

- `scripts/demo/demo_webcam.py` 未修改。
- GEM inference、ONNX loading、denoiser 和 render 未修改。
- FK、资源加载、quaternion 转换、打包和 ZMQ send 仍全部在 consumer 线程执行。
- 没有创建新的 SMPL/SMPL-X layer。

### 验证结果

- converter 的 `smpl_joints_local` 与 GR00T 原生 `compute_human_joints + quat_inv + quat_apply` 参考路径逐值一致。
- `transl` 和 `betas` 改变不会影响 local joints。
- Protocol v3 六个字段的 shape、dtype 和 contiguous layout 检查通过。
- fallback packer 与 GR00T 官方 `pack_pose_message` 逐字节一致。
- 本地真实 ZMQ PUB/SUB 收发通过。
- publisher connect/close/reconnect 回归通过。
- SONIC demo CLI 与 `--sonic_repo_path` 默认值检查通过。
- Ruff lint/format、Python `py_compile` 和 `git diff --check` 通过。

## 2026-07-14：严格 SONIC 坐标、50 Hz 与 5 帧窗口

### 修改目标

将 GEM-SMPL bridge 的坐标、root quaternion、时间采样、滑动窗口和 G1 wrist qpos 对齐 GR00T-WholeBodyControl 的 SONIC/Pico streaming 语义，同时保持 GEM inference 线程非阻塞。

### 涉及文件

- `gem/utils/sonic/smpl_converter.py`
  - 按 `process_smpl_joints()` 的完整顺序执行 Y-up -> Z-up、FK、base rotation removal 和 local-joint 转换。
  - 返回 `smpl_pose`、`smpl_joints_local`、`body_quat_w`。
  - 支持首帧 yaw-only heading 校准。
- `gem/utils/sonic/resampler.py`
  - 新增 `SMPLRealtimeResampler`。
  - 使用单调纳秒时间轴输出 50 Hz 采样。
  - joints 线性插值，pose/body quaternion 使用 shortest-path SLERP。
  - 对时间倒退或超过 0.5 s 的断流重置，不批量补发陈旧帧。
- `gem/utils/sonic/zmq_publisher.py`
  - consumer 内集成 resampler 和 `deque(maxlen=5)`。
  - 窗口填满后以 20 ms 节拍发送固定 5 帧 Protocol v3 payload。
  - `body_quat` 直接使用 converter 的 SONIC `[w,x,y,z]` root quaternion，删除原始 GEM root 的 SciPy 转换。
  - 复制 Pico 的 elbow-swing/wrist Euler 映射生成 6 个 G1 wrist DoF，为零 elbow rotation 增加 identity 保护。
  - 新增 `check_health()`；worker 异常打印 `[SONIC ERROR]` 并在主线程下一轮立即上抛。
- `scripts/demo/demo_webcam_sonic.py`
  - producer 在首次观察到每个唯一 GEM ready result 时记录 `time.monotonic_ns()`。
  - 新增 `--enable_yaw_calibration`。
  - 每个 camera loop 调用 `check_health()`。
- `gem/utils/sonic/__init__.py`
  - 导出 `SMPLRealtimeResampler`。
- `记录文本.md`
  - 追加本次修改记录，并将上一版 raw-root 记录标记为历史实现。

### 坐标转换

```text
GEM global_orient axis-angle
  -> angle_axis_to_quaternion()              [w,x,y,z]
  -> smpl_root_ytoz_up()
  -> quaternion_to_angle_axis()
  -> compute_human_joints(body_pose[:63], converted root)
  -> remove_smpl_base_rot(..., w_last=False) -> body_quat_w
  -> quat_inv(body_quat_w)
  -> quat_apply(..., FK joints)
  -> smpl_joints_local
```

`body_quat_w` 在协议中使用用户指定的字段名 `body_quat`。不再使用原始 GEM `global_orient` 直接生成该字段。

### Yaw 校准

启用 `--enable_yaw_calibration` 后，首帧有效 `body_quat_w` 提取 Z 轴 yaw-only heading：

```text
initial_heading = get_heading_q(first_body_quat_w)
body_quat = quat_mul(quat_inv(initial_heading), body_quat_w)
```

只消除初始 yaw，保留 pitch/roll。重连时 converter 重建，校准状态同步清空。

### 50 Hz 重采样和窗口

- 目标周期：`20,000,000 ns`。
- `smpl_joints_local`：线性插值。
- `smpl_pose`：21 个 axis-angle 分别转 quaternion，shortest-path SLERP 后转回 axis-angle。
- `body_quat`：`[w,x,y,z]` shortest-path SLERP 并归一化。
- `frame_index`：由 consumer 按重采样帧递增，不再使用 GEM 源帧数。
- 首次窗口必须等待 5 个新采样帧，不使用首帧 padding。

adapter 不修改 `demo_webcam.py`，因此无法获得 camera exposure timestamp；当前 GEM timestamp 是 SONIC adapter 首次看到唯一 ready result 时的单调时间。

50 Hz 不仅是数据时间网格，ZMQ consumer 也使用墙钟 deadline 每 20 ms 最多发送一条滑窗消息：

- 5 帧 payload 窗口与待发 packet queue 彼此独立。payload 仍为 `deque(maxlen=5)`，待发 packet 最多保留最新 2 条。
- 启动使用有限 2-packet lookahead；只有一条时最多等待 40 ms，因此视频末尾不会永久滞留。
- deadline 基于上一个绝对时间点累加，不将 pack/send 开销逐帧累积成频率漂移；同时限制两次 send slot 启动至少间隔 20 ms，防止 worker 迟到后在 1 ms 内 catch-up burst。
- consumer 在等待下一个 send slot 时仍持续消费 raw queue；若错过 slot 或产生大批补点，丢弃旧 packet 并保留最新两条，不回放历史 backlog。
- raw queue 丢帧只清理待发旧 packet，保留 resampler endpoint 和已积累的 5 帧时序窗口，避免轻度过载时反复重置导致输出饥饿。真正的时间倒退或超过 0.5 s 断流由 resampler 清窗并重新锚定。

### G1 wrist 映射

SMPL pose 索引：

- left elbow: `17`
- right elbow: `18`
- left wrist: `19`
- right wrist: `20`

G1 `joint_pos` 映射：

- `23`: left wrist roll
- `25`: left wrist pitch
- `27`: left wrist yaw
- `24`: right wrist roll
- `26`: right wrist pitch
- `28`: right wrist yaw

其余 23 个 DoF 保持 0，`joint_vel` 按 Pico streamer 保持 0。

### Protocol v3 输出

```text
smpl_pose    [5,21,3] float32
smpl_joints  [5,24,3] float32
body_quat    [5,4]    float32, wxyz
joint_pos    [5,29]   float32
joint_vel    [5,29]   float32
frame_index  [5]      int64
```

### 启动与运行

```bash
CUDA_VISIBLE_DEVICES=0 python scripts/demo/demo_webcam_sonic.py \
  --camera_id 2 \
  --no_imgfeat \
  --sonic_zmq \
  --sonic_repo_path /home/weili/GR00T-WholeBodyControl
```

可选启用 yaw 校准：

```bash
--enable_yaw_calibration
```

### 验证结果

- converter 与 GR00T 完整 `process_smpl_joints` 参考路径的 joints/body quaternion 最大误差均为 `0.0`。
- axis-angle `179° -> -179°` 的中点为 `180°`，确认走 shortest path。
- Pico wrist 映射随机姿态最大误差约 `2.2e-8`；零姿态输出有限且为 0。
- 5 帧窗口 shape、dtype、contiguous layout 和滑动 `frame_index` 通过。
- 真实 ZMQ PUB/SUB Protocol v3 六字段收发通过。
- 以 30 Hz 模拟 GEM 输入进行 120 条真实 ZMQ 收发：平均间隔 `19.998 ms`，中位数 `19.981 ms`，95% 分位数 `20.882 ms`。
- 490 ms 源时间跨度不再回放约 25 条历史消息；实测只保留最新 2 条，发送跨度 `19.842 ms`。
- 确定性调度边界测试通过：单 packet 40 ms 后可发送、迟到 19 ms 后不会在下一毫秒连发、超时 slot 只保留最新 packet。
- publisher `connect -> close -> reconnect` 通过；worker 故障会打印 `[SONIC ERROR]`，`check_health()` 在主线程以 chained exception 上抛原始异常。
- Ruff lint/format、Python `py_compile` 和 `git diff --check` 通过。

## 2026-07-14：SONIC 非有限姿态容错与坏帧诊断

### 修改目标

修复偶发 `smpl_joints_local contains non-finite values` 导致 publisher worker 退出、进而终止整个 webcam demo 的问题。单个数值异常帧现在可恢复；只有连续 30 个异常源帧才升级为致命错误。

### 涉及文件

- `gem/utils/sonic/smpl_converter.py`
  - 新增 `InvalidSMPLFrameError`、有限值诊断和安全 quaternion 归一化。
  - 在输入、坐标转换、FK、base rotation、quaternion inverse、local joints 和 yaw 校准后逐阶段检查。
  - `body_pose` 改为 in-camera 优先，`global_orient` 仍为 global 优先；首选候选非有限时只回退到有限备选。
- `gem/utils/sonic/zmq_publisher.py`
  - 单个 `InvalidSMPLFrameError` 只记录、限频告警并丢帧，不设置 worker fatal error。
  - 保留上一有效 resampler endpoint、5 帧窗口和递增 `frame_index`，下一有效帧可继续插值。
  - scalar timestamp 延后到 consumer 选定 finite fallback pose 后再按实际批大小展开，避免主/备 pose 批大小不同时误杀 worker。
  - 显式 timestamp 数组可匹配任一结构有效的 pose 候选批大小，consumer 再对最终 finite fallback pose 做严格长度校验。
  - 每 100 个已发窗口增加 invalid/consecutive/queue-drop 计数。
  - 新增可选坏帧快照，每个 publisher 实例最多尝试保存 3 个 CPU 副本，保存失败不会退出 worker。
- `scripts/demo/demo_webcam_sonic.py`
  - publish 前轻量检查 raw `body_pose/global_orient`，只打印限频诊断，不做 FK、不抛异常。
  - 新增 `--sonic_bad_frame_dir`。
- `tests/test_sonic_invalid_frames.py`
  - 新增 converter fallback、阶段定位、worker 恢复、30 帧升级和 3 个快照上限的回归测试。

### 非有限值定位

converter 现在会在最早出现异常的阶段抛出带 shape 和 `nan/posinf/neginf` 计数的错误，而不是让坏值一直传到 resampler。检查顺序：

```text
body_pose / global_orient
  -> angle_axis_to_quaternion
  -> smpl_root_ytoz_up
  -> quaternion_to_angle_axis
  -> compute_human_joints
  -> remove_smpl_base_rot
  -> quat_inv
  -> quat_apply / smpl_joints_local
  -> yaw-calibrated body_quat_w
```

所有 quaternion 在归一化前后都检查 finite，norm 非有限或小于 `1e-8` 的帧会被丢弃。没有使用 `nan_to_num`，也不会用零姿态代替坏姿态。

### fallback 和恢复策略

```text
body_pose:     incam finite -> global finite -> InvalidSMPLFrameError
global_orient: global finite -> incam finite  -> InvalidSMPLFrameError
```

- 单个异常批次：`invalid_source_frames += N`，`consecutive_invalid_frames += N`，不停 worker。
- 下一批转换成功：`consecutive_invalid_frames = 0`，流式输出继续。
- 连续累计到 30 帧：升级为 `Too many consecutive invalid GEM-SMPL frames`，交给原有 `check_health()` 传播。
- 其他编程、协议、shape 或 ZMQ 异常仍然是致命错误，不会被误判为人体坏帧。

### 坏帧快照

```bash
--sonic_bad_frame_dir /path/to/bad_frames
```

快照名为 `bad_smpl_frame_000.pt` 到 `bad_smpl_frame_002.pt`，包含 timestamp、incam/global CPU body-parameter 副本和详细错误信息。默认 `None`，不写文件。

### 兼容性

- 50 Hz resampler、5 帧窗口、G1 wrist mapping 和 Protocol v3 payload 未修改。
- `scripts/demo/demo_webcam.py`、GEM/ONNX/denoiser 和 GR00T-WholeBodyControl 未修改。
- 有效帧与原 GR00T 参考路径的最大数值差异低于 `3e-7`。

### 验证结果

- `python -m unittest` 的 15 个定向回归测试全部通过。
- 覆盖 zero pose、incam/global fallback、两个 root 都非有限、缺少 pose、FK 输出阶段定位、单帧恢复、第 30 帧升级、快照上限/保存失败和统计日志。
- 新增主 pose 非有限 `[1,63]` -> 备用 pose 有效 `[6,63]` 的批大小切换回归；scalar timestamp 按转换后的 6 帧展开，worker 保持健康。
- 同一 fallback 路径的 6 元素显式 timestamp 数组也可正常入队、转换和发送。
- 新增 `valid -> invalid -> valid` 回归：坏帧前后 resampler 历史、窗口和 `frame_index` 连续，后续有效帧将 consecutive 计数清零。
- 真实 ZMQ SUB 验证一个坏帧后 worker 仍健康，后续正常 5 帧窗口可继续接收。
- 正常 30 Hz 源输入回归收到 69 条 Protocol v3 消息，中位间隔 `20.18 ms`，六字段 shape 保持不变。
- Ruff、Python `py_compile`、CLI 参数和 raw finite precheck 通过。

### 运行方法

```bash
CUDA_VISIBLE_DEVICES=0 python scripts/demo/demo_webcam_sonic.py \
  --camera_id 2 \
  --no_imgfeat \
  --sonic_zmq \
  --sonic_repo_path /home/weili/GR00T-WholeBodyControl \
  --sonic_bad_frame_dir ./sonic_bad_frames
```

## 2026-07-14：视频文件按源 FPS 限速播放

### 修改目标

修复 `scripts/demo/demo_webcam.py --video ...` 会按解码/推理速度尽快读取、导致视频播放过快的问题。

### 涉及文件

- `scripts/demo/demo_webcam.py`
  - 显式区分 `--video` 文件源与 `--camera_id` 实时摄像头源。
  - 读取 OpenCV `CAP_PROP_FPS`，当元数为 NaN、无穷或小于等于 0 时限频告警并回退到 30 FPS。
  - 视频文件的每帧处理开始时间至少间隔 `1 / source_fps`；摄像头路径不增加 sleep。
  - 当单帧推理已慢于源帧率时从当前时间重新锚定，不连续读取多帧追赶旧 deadline。
- `记录文本.md`
  - 追加本次实现说明。

### 兼容性

- 不改变 GEM 推理、ONNX、denoiser、render、GMR UDP 和 SONIC adapter。
- 摄像头仍按设备实时到帧速度读取，只有 `--video` 启用播放节拍。
- 若推理比视频帧周期更慢，整体播放会低于源 FPS，但不会再高于源 FPS。

### 验证结果

- 使用无模型伪视频源模拟 50 FPS，5 帧实测间隔为 `20.2/20.2/20.1/20.1 ms`。
- 伪摄像头源验证未调用 `sleep`，摄像头实时路径不受影响。
- Python `py_compile`、Ruff 针对性检查和 `git diff --check` 通过。

### 运行方法

```bash
python scripts/demo/demo_webcam.py --video inputs/demo/walk.mp4
```

## 后续记录模板

后续修改按以下格式追加：

```markdown
## YYYY-MM-DD：修改标题

### 修改目标

### 涉及文件

### 实现说明

### 兼容性

### 验证结果
```

## 2026-07-20：新增纯文本 GEM-SMPL 动作生成

### 修改目标

新增不依赖任何视频输入的文本生成入口，完整执行 `T5-3B -> GEM diffusion -> DDIM/CFG -> EnDecoder -> SMPL`，不运行 YOLO、ByteTrack、ViTPose、HMR2 或实时 ONNX denoiser。

### 涉及文件

- `scripts/demo/demo_smpl_text.py`
  - 新增纯文本 CLI、T5 预编码及显存释放、合成静态相机数据、checkpoint 能力检查、DDIM/CFG 配置、输出有限值/shape 校验、四类结果保存和全局动作渲染。
  - 非文本 condition mask 全部关闭，非零虚拟 bbox 只供统一 SMPL 解码接口使用。
  - `--dry_run` 不加载 T5、GEM、checkpoint 或 CUDA，只检查输入 tensor 合约。
- `gem/gem.py`
  - 修复 `GEM.predict()` 未将 `data["text_embed"]` 放入 batch 的问题。
  - 新增预计算文本特征 helper，兼容 `[50,1024]` 和 `[1,50,1024]`，严格检查单 batch、文本长度及 denoiser 的 `encoded_text_dim`。
- `tests/test_demo_smpl_text.py`
  - 新增 CPU-only 轻量测试，覆盖合成相机、bbox、condition mask、零视频条件、单帧角速度、slug、非法参数和预计算文本特征接口。
- `README.md`
  - Demo 部分新增 “Text-only motion generation”，说明 checkpoint、T5 和 ONNX 限制及运行方法。
- `记录文本.md`
  - 追加本次实现说明和验证结果。

### 实现说明

```text
raw prompt
  -> T5-3B [50,1024]（先编码并释放）
  -> 纯文本 data（静态 K/R_w2c，所有非文本 mask=False）
  -> 完整 gem_smpl checkpoint
  -> DDIM 50 steps + CFG 2.5
  -> 151-D motion
  -> EnDecoder
  -> body_pose/global_orient/transl/betas
  -> smpl_params.pt + motion.npz + prompt.txt + metadata.json
  -> 可选 global.mp4
```

渲染仍复用 `normalize_global_verts()`、`render_global_frames()`、`make_smplx("supermotion")` 和 `save_video()`；仅在新脚本中把浮点 FPS 转成 PyAV 接受的有理数，没有修改现有渲染/视频模块。渲染异常只告警，已经保存的参数不会丢失。

### 兼容性

- 未修改 `demo_smpl.py`、`demo_webcam.py`、视频检测/回归路径、训练流程或 ONNX 模型。
- 完整 `gem_smpl` 继续支持现有视频和混合文本推理。
- `gem_smpl_regression` 会在推理前被明确拒绝，不会以随机文本层或全零 embedding 继续运行。

### 验证结果

- `python -m py_compile scripts/demo/demo_smpl_text.py gem/gem.py tests/test_demo_smpl_text.py`：通过。
- `pytest -q tests/test_demo_smpl_text.py`：23 passed。
- Ruff check/format 和 `git diff --check`：通过。
- 60 帧 `--dry_run`：所有要求 shape 正确，五类非文本 mask 均为 `0 / 60`。
- 完整 checkpoint 文本扩散权重检查：通过。
- RTX 4090 实际生成 120 帧：`body_pose [120,63]`、`global_orient [120,3]`、`transl [120,3]`、`betas [120,10]`，全部有限，并生成四个参数/元数据文件。
- 实际生成并渲染 300 帧：四组 SMPL 参数全部有限；`global.mp4` 为 1280×720、30 FPS、300 帧、10 秒。

### 运行方法

```bash
python scripts/demo/demo_smpl_text.py \
  --prompt "a person walks forward, turns left, and waves with the right hand" \
  --ckpt_path inputs/pretrained/gem_smpl.ckpt \
  --num_frames 300 \
  --fps 30 \
  --seed 42
```

## 2026-07-20：统一实时渲染与 GMR FK 的 SMPL-X 体型

### 修改目标

为 `demo_webcam.py` 增加统一 `--shape_mode`，确保模型预测的 raw betas 只经过一次 shape policy，随后由实时渲染、incam/global body params 和 GMR SMPL-X FK 共同使用。

### 涉及文件

- `gem/smplx_gmr_reference.py`
  - `BetaStabilizer` 新增 `zero` 模式，从第一帧起返回同 shape/device/dtype 的全零 betas，并始终报告 `frozen=True`。
  - zero 模式仍先执行 NaN/Inf 检查，不进入 warmup，也不累计预测体型。
- `scripts/demo/demo_webcam.py`
  - 新增正式参数 `--shape_mode`、`--shape_warmup`，保留旧 GMR 参数名作为 CLI 别名，默认体型改为 `zero`。
  - stabilizer 改为无条件创建，不再依赖是否启用 GMR。
  - 新增 `resolve_effective_betas()`，统一检查/生成 effective betas。
  - backend 在渲染和 GMR 分支之前覆盖 incam/global betas；GMR FK 删除第二次 stabilizer 调用，直接使用同一 effective tensor。
  - 第一帧只记录一次 raw/effective betas norm。
- `tests/test_smplx_gmr_reference.py`
  - 增加 zero 模式 NumPy、Torch CPU/CUDA、dtype/device/shape、连续输入、frozen 和非有限输入测试。
- `tests/test_demo_webcam_shape.py`
  - 增加 helper、缺失/非法 betas、非有限结果、CLI 新旧名称和 mock backend 端到端共享体型测试。
- `README.md`
  - 实时 Webcam 参数表和 GMR 示例增加 `--shape_mode zero`。

### 数据流

```text
decode_dict["betas"]
  -> resolve_effective_betas()
  -> effective_betas
       -> body_params_incam（OpenCV 渲染）
       -> body_params_global（Viser 渲染）
       -> EnDecoder.fk_v2（GMR 的 14 个 SMP1 目标）
```

### 兼容性

- `--gmr_shape_mode` 和 `--gmr_shape_warmup` 仍可作为旧命令别名使用，但内部只保存为 `args.shape_mode` / `args.shape_warmup`。
- 未修改 GEM ONNX、151 维 EnDecoder 格式、SMP1 UDP 包、14 目标顺序、坐标转换、检测/姿态/HMR2/rollout 或文本生成脚本。

### 验证结果

- Python `py_compile`：通过。
- `tests/test_smplx_gmr_reference.py` 和 `tests/test_demo_webcam_shape.py`：24 passed，3 subtests passed。
- 完整 `tests/` 回归：62 passed，3 subtests passed（仅有 3 条既有弃用告警）。
- CUDA zero betas 保持 CUDA device 和原 dtype。
- mock backend 断言 incam/global 输出和 `fk_v2()` 输入均为同 shape 的全零 betas，GMR 包结构未改。
- `/dev/video2` 真实相机链路完成模型加载、warmup、实时推理和 SMP1 发送；启动打印 raw norm `2.186237`、effective norm `0.000000`，GMR 稳定发送约 24 Hz。

### 运行方法

```bash
python scripts/demo/demo_webcam.py \
  --camera_id 2 \
  --no_imgfeat \
  --display \
  --gmr_host 127.0.0.1 \
  --gmr_port 7006 \
  --gmr_protocol smplx1 \
  --shape_mode zero
```

## 2026-07-20：任意音乐文件生成 GEM-SMPL 人体动作

### 修改目标

新增不依赖视频、检测器、HMR2 或 T5 的 music-only 扩散推理链路：

```text
WAV / MP3 / FLAC
  -> EDGE baseline35（30 FPS）
  -> 完整 PyTorch gem_smpl.ckpt
  -> DDIM + classifier-free guidance
  -> 151-D GEM motion
  -> EnDecoder
  -> global/incam SMPL 参数
  -> 可选全局渲染与原音乐合成
```

### 涉及文件

- `gem/utils/music_features.py`
  - 实现严格 EDGE 通道顺序的 baseline35 特征、AIST BPM 先验解析和显式时长对齐策略。
- `tools/data/aistpp/extract_musicfeat_v2.py`
  - 支持单音频提取和同 stem、per-sequence WAV 的 AIST++ 批量提取；不猜音乐偏移，不修改 annotations。
- `tools/data/aistpp/audit_aistpp.py`
  - 输出 AIST++ 目录、motion 帧长、60 FPS 下采样兼容性和 aligned WAV 覆盖率 JSON 报告。
- `gem/datasets/aistpp/aistplusplus.py`
  - v2 loader 同时支持 NumPy/Tensor `.pt`，验证 `[L,35]`、有限值和二值峰值；保留 legacy 第 53 维 beat，缺失时使用 v2 第 34 维。
- `gem/gem.py`
  - 修复 `predict()` 对 `data["has_text"]` 的处理；music-only 的 `has_text=False` 保持为 False，并按模型配置构造全零文本特征，不调用 T5。
  - meta 缺失时使用安全默认值，原预计算文本 embedding 路径保持兼容。
- `scripts/demo/demo_music.py`
  - 新增完整 checkpoint 能力/维数检查、music-only 静态相机输入、DDIM/CFG 推理、多 sample 保存、有限值检查、全局渲染和 ffmpeg mux。
  - 保存 pipeline 实际 `model_output["pred_x"]` 为 `motion_151d.pt`，不猜输出 key。
- `tests/test_music_features.py`
  - 覆盖合成音频、35 通道顺序、BPM、对齐策略、NumPy/Tensor loader 和 beat fallback。
- `tests/test_music_only_inference.py`
  - 覆盖 music-only mask、无文本条件和带完整 checkpoint 的 CUDA smoke test。
- `docs/MUSIC_DEMO.md`、`README.md`
  - 记录运行方法、EDGE 定义、ONNX 限制、AIST++ 对齐要求、输出和机器人边界。
- `setup.cfg`
  - 增加必要的 `librosa>=0.10,<0.11` 运行依赖。
- `.gitignore`
  - 仅解除 `tools/data/aistpp/*.py` 的忽略，使新增审计/提取工具能够纳入代码变更；普通 `data/` 仍保持忽略。

### 模型与数据审计

- 仓库：`/home/weili/GENMO`，remote 为 `XiaoxiaoKuankuan/GENMO`，`main` 与当时的 `origin/main` commit 一致。
- `gem_smpl.ckpt` 和 `SMPLX_NEUTRAL.npz` 均存在。
- checkpoint 含 4 个 `music_embedder` 参数；模型 `in_attr` 含 `encoded_music`，且 `regression_only=False`。
- checkpoint 和模型第一个 music `nn.Linear` 的实际 `in_features` 都是 **35**，因此启用 `edge_baseline35`；未实现维数不匹配的 Jukebox 替代或任何补列/压缩。
- AIST++ annotations 有 411 个有效 motion，帧长 426–2878；cameras 15、keypoints2d 1510、splits 7，annotations 内无音频。原始 music-ID WAV 位于 annotations 外，不能替代同名对齐的 per-sequence WAV。

### 验证结果

- `py_compile`：所有新增/修改 Python 文件通过。
- Ruff lint/format：通过。
- `tests/test_music_features.py tests/test_music_only_inference.py`：15 passed，其中包含 RTX 4090 + 完整 checkpoint 的 CUDA DDIM smoke test。
- 现有纯文本 demo 回归测试：23 passed。
- 完整 `tests/` 回归：77 passed，3 subtests passed；没有 webcam、SONIC/GMR 或文本 demo 回归失败。
- 5 秒命令行端到端：生成 151 帧；`music_features [151,35]`、`motion_151d [151,151]`，global/incam 四类 SMPL 参数全部有限。
- 10 秒带渲染和音频合成：生成 301 帧；成功输出 `motion_global.mp4` 和含 H.264 视频、AAC 音频的 `motion_with_audio.mp4`，时长约 10.034 秒。

### 已验证输出

```text
outputs/music_demo/genmo_music_acceptance/
  music_features.pt
  music_features_meta.json
  run_config.json
  sample_000/smpl_params.pt
  sample_000/motion_151d.pt

outputs/music_demo/genmo_music_render/sample_000/
  smpl_params.pt
  motion_151d.pt
  motion_global.mp4
  motion_with_audio.mp4
```

### 运行方法

```bash
python scripts/demo/demo_music.py \
  --audio /path/to/song.wav \
  --ckpt_path inputs/pretrained/gem_smpl.ckpt \
  --duration_sec 10 \
  --output_root outputs/music_demo \
  --save_features \
  --mux_audio
```

输出是 SMPL 人体动作。机器人实机仍需要 retarget、控制策略、安全约束和硬件验证；当前版本也不会自动分段或拼接超过 `--max_frames` 的长音频。

## 2026-07-20：文本动作常驻 GMR 播放器与安全状态机

### 修改目标

将纯文本生成和实时机器人参考发送解耦：GEM 可以离线生成整段 SMPL-X
动作，常驻 streamer 始终以固定频率向 GMR-CPP 发送缓存动作、平滑过渡或
安全站立目标。

```text
文本 -> demo_smpl_text.py -> 原子发布动作目录 + READY
                              ↓
stream_smpl_params_to_gmr.py -> 时间重采样 -> SMPL-X FK -> SMP1 UDP
                              ↓
GMR-CPP / BUMI3 -> Redis/GMT -> tracking policy
```

### 涉及文件

- `gem/runtime/motion_streamer.py`
  - 新增严格动作加载/有限值和 shape 校验、`SMPLMotion`/`SMPLFrame` 数据类。
  - 新增 STARTING、IDLE、LOADING、BLENDING、PLAYING、RETURNING、HOLDING、ERROR、ESTOP 状态机。
  - 新增 axis-angle→quaternion shortest-path SLERP、smoothstep 根平移、30/50 Hz 单调时间重采样和落后 deadline 跳过。
  - 新增 Y-up 根 yaw、根平移连续化、动作结束原地返回 idle、软件 ESTOP 锁存。
  - 新增 READY 目录轮询、完成时间排序、queue/latest/interrupt 和 `.watch_state.json` 去重。
- `gem/runtime/__init__.py`
  - 导出播放器的公共类型和辅助函数。
- `scripts/demo/stream_smpl_params_to_gmr.py`
  - 新增常驻 CLI；复用 Webcam 相同配置的 `EnDecoder.fk_v2()`、`SMPLXGMRReference` 和 `GMRUDPBridge`。
  - 每次 FK 都重新构造 `torch.zeros(1, 1, 10)`，不信任动作文件中的 betas。
  - 无动作、加载中、回站和 ESTOP 时仍按 publish FPS 发送；连续发送错误进入安全返回而不立即退出。
  - dry-run 不创建 socket，可选用 `--dry_run_fk` 执行真实 SMPL-X FK。
- `scripts/tools/extract_smpl_idle_pose.py`
  - 从已验证动作原子提取单帧、全零 betas 的 robot idle 文件；默认拒绝覆盖已有文件。
- `scripts/demo/demo_smpl_text.py`
  - 新增默认且唯一的 `--shape_mode zero`，同时覆盖 global/incam betas 并断言全零。
  - 输出改为同文件系统 `.tmp_<uuid>` 完整写入、唯一目录原子重命名、最后写 `READY`；metadata 记录 UTC 完成时间。
- `tests/test_motion_streamer.py`
  - 覆盖输入校验、READY 去重、三种队列策略、持续 idle、RETURNING、ESTOP、SLERP、smoothstep、root 对齐、FPS 重采样和 deadline 跳过。
- `tests/test_stream_smpl_params_to_gmr.py`
  - 覆盖 CLI 安全默认值、robot idle 限制、dry-run 无 socket、mock SMP1 边界、全零 FK betas、idle 提取和真实 CPU SMPL-X FK。
- `tests/test_demo_smpl_text.py`
  - 增加文本输出双分支 zero betas 和原子 READY 发布测试。
- `README.md`
  - 增加三终端 Text-to-motion robot streaming 运行说明、动作目录协议及实物安全限制。

### 动作连续性与安全行为

- 新动作先将第一帧根位置对齐当前 root，并围绕 GENMO Y-up 重力轴对齐 yaw；动作内部相对路径和转向保留。
- body pose 与 global orient 使用四元数最短路径 SLERP；根平移使用 cubic smoothstep。
- 动作结束后不保持最后一帧，按 `return_seconds` 回到与当前脚下位置对齐的 idle，然后持续发送。
- robot 模式没有经过验证的 `--idle_motion` 时拒绝启动；sim 模式的合成 idle 会打印不可用于实机的警告。
- ESTOP 文件出现后用有限的 `estop_blend_seconds` 回 idle，并保持锁存；单纯删除文件不会恢复旧动作，必须等待新的有效动作。
- streamer 不修改 412-byte SMP1 格式、14 个目标顺序、GMR-CPP、BUMI3 或 GMT。

### 验证结果

- `py_compile`：四个要求文件全部通过。
- Ruff：新增/修改 Python 文件通过。
- 本次新增播放器定向测试：24 passed；连同纯文本输出协议回归测试共 49 passed。
- `pytest -q tests`：103 passed、3 subtests passed。
- 实际 300 帧动作 dry-run：30 FPS 源动作重采样到 50 Hz，首/中/末帧真实 SMPL-X FK 全部有限，betas norm 为 0。
- 本机 SMP1 UDP 完整播放：实际发送约 29.6–30.0 Hz，状态完整经过 BLENDING→PLAYING→RETURNING→HOLDING，并由 `--once` 在安全回站后退出。

### 推荐运行

```bash
python scripts/demo/stream_smpl_params_to_gmr.py \
  --watch_dir outputs/text_motion \
  --gmr_host 127.0.0.1 \
  --gmr_port 7006 \
  --publish_fps 30 \
  --shape_mode zero \
  --mode sim \
  --new_motion_policy queue
```

实物模式必须额外提供已验证的 `--idle_motion`，并且软件 ESTOP 不能替代机器人硬件急停。实物测试前必须先完成 MuJoCo、低速和吊装保护验证。

## 2026-07-20：T5-3B 本地缓存优先与代理兼容修复

### 问题

`demo_smpl_text.py` 在本机已有完整 T5-3B 缓存时，Transformers 仍可能先请求
Hugging Face Hub 元数据。当 shell 中的代理为
`socks://127.0.0.1:7897` 时，当前 HTTPX 会在读取缓存前报
`Unknown scheme for proxy URL`。

### 修改

- `scripts/demo/demo_smpl_text.py`
  - 新增 `_load_t5_components_cached_first()`。
  - 默认先用 `local_files_only=True` 严格加载完整本地缓存；只有缓存不完整时才访问 Hub。
  - 本地 tokenizer 成功但 encoder 权重不完整时，不会误认为可用缓存。
  - 本地和在线加载都失败时，同时报告两次失败原因。
  - 检测到 `socks://` 时，错误信息明确提示使用 HTTP 代理，或安装 SOCKS 支持后改用 `socks5://`。
- `tests/test_demo_smpl_text.py`
  - 新增完整缓存不访问网络、缓存不完整才回退 Hub、SOCKS 错误可操作提示三项测试。
- `README.md`
  - 增加 cache-first 行为和 HTTP/SOCKS 代理配置说明。

### 验证

- 本机 `~/.cache/huggingface/hub/models--t5-3b` 约 11GB，包含完整 config、SentencePiece 和 `model.safetensors`。
- `tests/test_demo_smpl_text.py`：28 passed。
- 用用户原始的 300 帧命令完整复跑成功，日志确认 `T5 loaded from local cache/path`。
- GEM DDIM 生成 300 帧，global/incam betas 均为全零。
- 原子发布目录：

```text
outputs/text_motion/a_person_walks_forward_turns_left_raises_both_ar_seed42_20260720T123808Z_dc8e766c/
  READY
  smpl_params.pt
  motion.npz
  metadata.json
  prompt.txt
```

## 2026-07-20：无动作时的合成 idle 改为双臂下垂

### 问题

streamer 原来的 sim 合成 idle 使用全零 `body_pose`，对 SMPL-X 来说这是双臂水平展开的 T-pose。因此没有新文本动作、动作播放完或 ESTOP 回到 idle 时，双手会水平举起。

### 修改

- `gem/runtime/motion_streamer.py`
  - sim 合成 idle 中，左肩在 SMPL-X body-pose joint 15 绕 Z 轴旋转 `-pi/2`。
  - 右肩在 body-pose joint 16 绕 Z 轴旋转 `+pi/2`。
  - 这会将休息骨架中沿 `+/-X` 展开的手臂旋转到 GENMO Y-up 坐标中的 `-Y` 方向。
  - idle metadata 记录 `source=synthetic_idle_arms_down` 和 `arms=down`。
- `scripts/demo/stream_smpl_params_to_gmr.py`
  - 启动时明确打印 `[Idle] Synthetic simulation pose: standing with both arms down`。
- `tests/test_motion_streamer.py`
  - 检查 HOLDING 持续产生同一下垂姿态，且只有左右肩的两个 axis-angle 分量非零。
- `tests/test_stream_smpl_params_to_gmr.py`
  - 使用真实 `EnDecoder.fk_v2()` 验证左右 wrist 的 Z-up 高度均低于对应 shoulder。
- `README.md`
  - 说明 sim 默认为双臂下垂，robot 模式仍以经验证的 `--idle_motion` 为准。

### 验证结果

- Ruff 和 `py_compile` 通过。
- `tests/test_motion_streamer.py tests/test_stream_smpl_params_to_gmr.py`：24 passed。
- 完整 `pytest -q tests`：106 passed、3 subtests passed。
- 真实 SMPL-X FK 中，左右腕都低于左右肩，不再是水平 T-pose。

robot 模式不会自动改写用户已验证的 idle 文件。用于实物时，应当提供本身就是双臂下垂、已经 MuJoCo 和实机低速验证的 `--idle_motion`。

## 2026-07-20：音乐动作 READY 发布与 GMR 常驻播放链路

### 实现内容

- `gem/runtime/artifact_publish.py`
  - 提取文本/音乐共用的同文件系统临时目录、文件与目录 fsync、原子重命名和最后创建 READY 的逻辑。
  - 提供 global/incam 同时强制零 betas 的共享 shape policy。
- `scripts/demo/demo_smpl_text.py`
  - 改为复用共享原子发布和 zero-shape 函数；原有目录命名、文件内容和 READY 行为保持兼容。
- `scripts/demo/demo_music.py`
  - 默认输出改为 `outputs/music_motion`，每个 sample 是 MotionWatcher 可直接发现的唯一子目录。
  - 增加 `--dry_run` 和 `--shape_mode zero`；dry-run 只读取真实音乐、提取 EDGE baseline35 并打印条件 mask，不加载 GEM/checkpoint/CUDA/SMPL-X/渲染，也不创建 READY。
  - 正式推理审计 checkpoint/model/features 三方维度均为 35，并拒绝 regression-only 模型。
  - 配置 DDIM steps 和 CFG，每个 sample 用 `seed + sample_index` 重新设置随机种子。
  - global/incam betas 在渲染和保存前同时归零；`motion.npz` 也保存零 betas，原始 151 维扩散输出单独保存为 `raw_motion_151d.pt`。
  - 输出完整 `smpl_params.pt`、`motion.npz`、`raw_motion_151d.pt`、`music_features.pt`、`metadata.json`、`source_audio.txt`；render/mux 是可恢复的 best-effort 步骤。
- `gem/runtime/motion_streamer.py`
  - `load_smpl_motion()` 保留音频路径、范围、BPM、特征、sample、CFG/DDIM 等可选审计字段。
  - `MotionWatcher` 增加 `any/text_only/music_only` source filter；不匹配、缺失或损坏 metadata 的目录不会加入队列，也不会被标记 consumed。
- `scripts/demo/stream_smpl_params_to_gmr.py`
  - 增加 `--source_filter`、`--audio_playback off|ffplay` 和 `--audio_offset_sec`。
  - ffplay 只在 BLENDING 进入 PLAYING 时异步启动；RETURNING、ERROR、ESTOP、中断切换和程序退出都会终止子进程。
  - ffplay 缺失或启动失败只打印 warning，不影响 30/50 Hz GMR 定频发送。
  - 继续复用 `EnDecoder.fk_v2()`、`SMPLXGMRReference`、`GMRUDPBridge` 和原 412-byte SMP1；FK betas 仍每帧显式构造为全零。
- 测试与文档
  - 新增 `tests/test_demo_music.py`、`tests/test_music_motion_publish.py`，扩展音乐 smoke、source filter、音乐 metadata、ffplay 状态转换和 zero-shape 测试。
  - 更新 `README.md` 与 `docs/MUSIC_DEMO.md`，加入三终端音乐动作链路、READY 协议、长音乐限制、音频同步和实机安全说明。

### 当前数据流

```text
WAV/MP3/FLAC
  -> EDGE baseline35 [L,35] @30Hz
  -> full gem_smpl.ckpt DDIM/CFG
  -> EnDecoder -> global/incam SMPL-X（betas 全零）
  -> .tmp_<uuid> 完整写入 -> 原子 rename -> READY 最后创建
  -> MotionWatcher(source_filter=music_only)
  -> MotionPlayer blend/play/return/idle
  -> EnDecoder.fk_v2(zero betas)
  -> SMPLXGMRReference -> 原 SMP1 UDP -> GMR/BUMI3/GMT
```

### 验证结果

- 用户指定的五个音乐/streamer 测试文件：53 passed。
- 完整 `pytest -q tests`：124 passed、3 subtests passed。
- Ruff、`py_compile` 和 `git diff --check` 通过。
- CPU dry-run 使用真实 30.013 秒 GTZAN rock 音频的前 2 秒：提取 `(61,35)` 有限特征，onset peaks=15、beat peaks=5、BPM=112.5；仅 music mask 为 True。
- 本机完整 checkpoint 和 CUDA 端到端生成前 5 秒：模型实际 music input dimension=35，DDIM=50、CFG=2.5，生成并发布 151 帧 READY 动作。
- 集成输出目录：

```text
outputs/music_motion/rock_00008_start0p000_seed42_20260720T131106Z_f747416f/
```

- 该目录包含全部七个必需文件，global/incam 参数 shape 正确，两组 betas 非零计数均为 0，音乐特征为 `(151,35)`。
- `MotionWatcher(source_filter=music_only, replay_existing=True)` 成功发现该直接子目录。
- streamer `--dry_run --dry_run_fk` 对 151 帧动作完成 30 Hz 重采样检查，首/中/末帧真实 SMPL-X FK 全部有限，betas norm=0，未创建 UDP socket。
- 未在本次自动验证中启动外部 GMR-CPP、MuJoCo、GMT 或实物机器人；该部分需要用户按文档三终端命令在外部进程中验证。

## 2026-07-22：主 README 与音乐演示文档中文化

### 修改内容

- `README.md`
  - 将项目介绍、最新动态、快速开始、预训练模型、全部 Demo、参数表、输出说明、文本/音乐到机器人链路、实时 Webcam、训练、安全提示、相关项目和许可证说明完整改写为中文。
  - 保留模型名、CLI 参数、路径、状态机名称、协议名称、命令和 BibTeX 原文，确保所有命令仍可直接复制运行。
  - 修正 BONES-SEED 链接中原有的 `ttps://` 拼写错误。
- `docs/MUSIC_DEMO.md`
  - 将 EDGE baseline35 契约、CPU dry-run、完整生成命令、READY 原子发布、文件结构、三终端播放、ffplay 音频同步、状态机、实物安全和 AIST++ 准备说明完整改写为中文。
  - 继续保留 `music_only`、`READY`、`BLENDING`、`PLAYING`、`RETURNING`、`SMP1` 等实际代码/协议字段，避免中文化破坏接口含义。

本次只修改文档文字，不修改任何 Python 代码、模型、推理流程、输出格式或协议。

## 2026-07-22：HumanML3D 到 GENMO-SMPL 训练格式构建工具

### 实现内容

- 新增 `tools/data/humanml3d/build_humanml3d_smpl.py`
  - 将 HumanML3D `train.txt`、官方文本、精确映射 CSV 与已有 `smplxpose_v2.pth` 组合成 `Humanml3dDataset` 直接读取的训练 PTH。
  - 只处理 `exact_family_path`，明确跳过 HumanAct12、unmatched 和 ambiguous，不进行模糊匹配。
  - 按官方 notebook 规则处理五类动作前缀，并把 index.csv 的 20 FPS 裁剪区间换算到 AMASS 30 FPS 张量。
  - 保留完整动作描述；将不同时间区间的描述分组为确定性 `__seg_<start_ms>_<end_ms>` 子片段，避免混入同一 record。
  - 使用旋转矩阵反射和官方 22 关节左右交换实现镜像；镜像文本只读取官方 `M<id>.txt`。
  - 官方数据当前仅有 `texts.zip`，因此增加只读 ZIP 回退；优先级仍是已解压的 `HumanML3D/texts/`，不会修改官方目录。
  - 输出张量全部为 CPU float32、连续内存，并在切片后执行 clone，避免保存原始大 AMASS storage。
  - 保存时先写同名 `.tmp`，重新加载做全结构/有限值验证，再通过 `os.replace` 原子发布。
  - 独立输出构建摘要、已构建/跳过记录、非法文本、丢弃/过短子片段、时长不一致和负 end frame 报告。
- 新增 `tests/test_build_humanml3d_smpl.py`
  - 覆盖 20→30 FPS 时间换算、五类前缀、文本解析和分组、确定性子片段 key、关节镜像、双重镜像、平移镜像、storage 脱离、record 契约和 dry-run。
- 更新 `README.md`
  - 增加中文的 HumanML3D SMPL-X 构建说明、dry-run/正式命令、输出与 T5 embedding 边界。

### 实际结果

- 映射审计：10600 条 exact 基础动作；跳过 959 条 HumanAct12 和 133 条 unmatched；无重复 ID、无缺失 AMASS key、无精确匹配负 end frame。
- 前 20 条小规模构建：50 条记录、9164 帧，输出 2,622,274 字节。
- 全量 dry-run 与正式构建：
  - 全文原动作 10587 条；
  - 全文镜像动作 10587 条；
  - 子片段 2068 条（原/镜像各 1034）；
  - 合计 23242 条、5,044,068 帧、46.7043 小时；
  - 正式 PTH 为 1,435,215,526 字节。
- 数据审计异常：107 个 duration mismatch、140 行官方文本含非有限时间标签、80 个越界子片段、14 个过短子片段、9 个过短基础动作和 2 个空裁剪；均在非 strict 模式中记录，未静默修补。
- 14 个 CPU 单元测试通过；正式文件重新加载后，每条记录的字段、shape、dtype、连续内存和有限值均通过验证。

本脚本没有生成 T5 embedding，也没有修改训练配置。下一阶段才会根据最终 PTH 的 motion key 和 `text_data` 生成 `all_text_embed.pth`。

## 2026-07-22：本地 411 条 AIST++ 的 partial 音乐训练数据集

### 实现内容

- 新增 `tools/data/aistpp/build_annot_aist_30fps.py`
  - 只读取本地 AIST++ motion、keypoints2d、camera、split 和已有 EDGE baseline35 音乐特征，不下载或重新提取数据。
  - 支持 `[N,24,3]` 与 `[N,72]` SMPL pose；通过确定性的 `[::2]` 将官方 60 FPS 动作同步转换为 30 FPS。
  - 根据 `--view` 精确匹配相机 JSON 中的具名视角，用 Rodrigues rvec 构造满足 `Xc=R@Xw+t` 的 `T_w2c`。
  - 使用当前 `make_smplx("supermotion")`、零 betas 骨架 offset 和 `get_c_rootparam()` 构造相机坐标 root，并在保存前重新计算验证 `1e-4` 一致性。
  - 从所选视角 COCO17 关键点生成未扩大的紧 bbox；中间空洞线性插值，首尾空洞使用最近有效 bbox 填充。
  - 严格验证每条音乐特征为有限 float32 `[L,35]`，且第 33/34 通道近似二值、长度与动作完全相等；缺失特征直接跳过，不伪造或复用。
  - 构建自定义 partial train/val/test：保留当前可用的官方 crossmodal val/test，其余成功序列进入 train；minitrain 从足够长的排序后 train 中确定性选择。
  - annot 和四个 split 先写 `.tmp`，重新加载验证结构、长度、集合互斥和引用完整性，再用 `os.replace` 原子发布。
  - 输出 build summary、逐序列 manifest、跳过原因、bbox 填充、相机、scaling 和无效记录报告；annot 顶层没有任何 metadata 特殊 key。
- 修改 `gem/datasets/aistpp/aistplusplus.py`
  - `AISTPlusPlusSmplDataset` 新增可选 `annot_file` 和 `split_file`，允许保持 `split="train"` 时读取 partial 文件。
  - 默认 None 时仍读取 `annot_aist_30fps.pt` 和 `<split>.pt`，旧行为不变。
  - 增加兼容 PyTorch 2.6 的可信本地 NumPy/Tensor artifact 加载，显式使用 `weights_only=False`。
- 新增 `configs/train_datasets/aistpp_partial_train.yaml`
  - 指向 partial annot、partial train split 和 v2 音乐特征，不修改原 `aistpp_train.yaml` 或 `gem_smpl.yaml`。
- 新增 `tests/test_build_annot_aist_30fps.py`
  - 23 个 CPU 测试覆盖 pose、下采样、关键点 clamp、九视角、相机、bbox、音乐、split、Dataset 新旧路径、dry-run 与原子保存。
- 更新 `README.md`
  - 增加中文 partial AIST++ 构建方法和非官方 benchmark 声明。

### 实际构建结果

- 输入：411 条 motion、1510 条 keypoints2d、14 个 camera JSON、408 条音乐特征；ignore 与本地 motion 交集为 0，官方 crossmodal_train 与本地 motion 交集为 0。
- 成功构建 408 条；仅跳过 3 条缺音乐特征序列：
  - `gKR_sBM_cAll_d28_mKR2_ch04`
  - `gKR_sBM_cAll_d28_mKR2_ch07`
  - `gLO_sBM_cAll_d13_mLO2_ch01`
- 其他跳过统计均为 0：keypoint、camera、音乐/动作长度、bbox、motion 结构、音乐结构均无额外异常。
- partial split：train=369、val=19、test=20、minitrain=16，集合互不重叠。
- 总计 136,803 帧，即 30 FPS 下 1.266694 小时。
- c01 视角下 408 条全部为 1920×1080；14 个 environment 均成功解析。
- 4 条序列存在无效 bbox，共插值/填充 40 帧；关键点尾帧 clamp 数为 0。
- `smpl_scaling` 仅审计、不参与 betas 或 translation：范围 78.5032～99.1295，均值 88.3785，中位数 88.2686。
- 正式 annot 文件大小 125,949,538 字节；Dataset 集成测试通过 120 帧随机训练裁剪、`[120,35]` 音乐条件和相机 root 一致性断言。

该数据集只用于工程验证 GENMO 的音乐条件训练链路，不是官方 980 条 AIST++ crossmodal_train，也不能用于声明官方 benchmark 或论文指标复现。

## 2026-07-22：HumanML3D T5-3B token embedding 提取

### 实现内容

- 新增 `tools/data/humanml3d/extract_t5_embeddings.py`
  - 读取 `humanml3d_smplhpose_train.pth` 中每条 `text_data` 的原始 caption，严格保留 motion ID 排序、caption 顺序和重复文本。
  - 复用 `gem.network.utils.encode_text_batch()` 的 50-token T5 编码语义，不使用 processed tokens，不做 mean pooling、归一化、PCA 或维度裁剪。
  - 正常流程先复制轻量 caption 元数据，再释放 1.4 GB 动作字典并加载 T5-3B，避免两份大对象同时常驻。
  - 输出按 motion 恢复为 `[num_captions,50,1024]`，保存为 CPU float16 连续 Tensor，并检查有限值和 padding 精确置零。
  - 增加确定性 SHA256 fingerprint、磁盘空间预估、256-motion 分片、临时文件回读验证、原子发布、manifest 兼容校验和可验证断点续跑。
  - `--estimate-only` 不加载 T5；`--finalize-only` 只验证/合并已有分片；`--cleanup-shards` 只在最终文件回读验证成功后删除分片 PTH。
  - 最终 PTH 顶层只包含 `motion_id -> embedding Tensor`，不添加 `__meta__`、manifest 或其他特殊 key。
- 修改 `gem/network/utils.py`
  - 保留原 `batch_encode_plus` 语义，并兼容新版 Transformers 已移除该公开别名的情况；回退到 tokenizer `__call__` 时使用完全相同的 padding、max length 和 truncation 参数。
- 新增 `tests/test_extract_t5_embeddings.py`
  - 使用 fake tokenizer/encoder 执行 CPU 测试，不下载或加载真实 T5-3B。
  - 覆盖输入解析、排序/重复 caption、owner 映射、padding 置零、FP16/CPU/连续内存、非有限值拒绝、分片损坏、fingerprint 冲突、精确 key 合并、原子输出、estimate-only、finalize-only 和确定性 limit。
- 更新 `README.md`
  - 增加中文的 HumanML3D T5-3B 空间预估、全量提取、断点续跑、输出契约和报告说明。

### 真实提取与验证结果

- 输入动作记录：23,242 条；caption：63,044 条；镜像记录：11,621 条；子片段记录：2,068 条。
- 理论 FP16 payload：6,455,705,600 字节（6.012 GiB）；按 2.2 倍估算的峰值磁盘需求：14,202,552,320 字节（13.227 GiB）。
- 小规模真实 T5-3B：32 个 motion、87 条 caption，在 CUDA/float32、batch size 8 下通过，输出 8,918,722 字节。
- 全量真实 T5-3B：CUDA `cuda:0`、模型 dtype float32、输出 dtype float16、batch size 16；生成 91 个已验证分片，本次无断点恢复（resumed=0）。
- 最终文件 `inputs/HumanML3D_SMPL/t5_embeddings_v1_half/all_text_embed.pth` 为 6,462,946,360 字节；清理参数执行后分片 PTH 为 0，manifest 和报告保留。
- 最终回读验证：23,242 个 key 与动作文件完全一致，63,044 条 caption 数量一致，全部 Tensor 均为 CPU float16、连续且无 NaN/Inf；padding 位置最大误差为 0。
- `Humanml3dDataset` 真实集成验证通过：随机子集 4 个样本均读取到 `[50,1024]` float32 文本条件（Loader 会将磁盘 FP16 转为 float32）。
- 18 个针对性 CPU pytest 用例通过；`py_compile`、Ruff 和后续全量回读检查均通过。另外使用本地 tokenizer 对全部 63,044 条最终 embedding 执行 padding 位置独立审计，最大绝对值为 0。

本次输出只是 T5-3B token embeddings，没有修改 SMPL 动作、原始 caption、processed tokens 或 HumanML3D 训练划分。

## 2026-07-22：BEAT2 四语言 `all_splits.pth` 索引构建

### 实现内容

- 新增 `tools/data/beat2/build_all_splits.py`
  - 自动发现排序后的 `beat_*_v2.0.0` 子集，解析 `train_test_split.csv` 的 `id/type`，将 `validation` 规范化为 `val`。
  - 以 `(subset, video_id)` 为唯一键，拒绝重复行和跨 split 冲突；不将 CSV 之外的 NPZ/WAV 自动加入索引。
  - 对每个正式记录检查 NPZ/WAV 存在、文件大小和 Git LFS pointer，并严格验证 `poses/betas/trans/gender`、有限值、SMPL-X 帧数、30 FPS 以及当前 GENMO Loader 对一维 betas/gender 的真实兼容性。
  - 使用 `soundfile.info` 读取 WAV 元数据，不加载整段波形，不重采样、裁剪或重写原音频。
  - 默认 `additional` 只保留在独立 split；`--include-additional-as-train` 才会同时合入 train。`minitrain` 从排序后 train 确定性选取。
  - 默认拒绝正式 train/val/test 的缺失配对；`--allow-missing-pairs` 只是显式跳过并报告，不会伪造动作或音频。
  - 输出先写 `all_splits.pth.tmp`，回读验证顶层契约、item 字段、排序、split 互斥和所有磁盘引用，然后原子替换正式文件。
  - 独立保存 build/subset summary 及 valid、skipped、missing、orphan、invalid NPZ、short audio、too short、unknown split 和 duplicate 报告，不在 PTH 中添加 metadata key。
- 新增 `tests/test_build_beat2_all_splits.py`
  - 29 个 CPU 测试用临时 CSV/NPZ/WAV 覆盖 split 规范化、additional 策略、联合唯一键、冲突、NPZ 契约、gender、缺失配对、LFS pointer、音频时长、orphan、minitrain、split 互斥、原子保存与 dry-run。
- 更新 `README.md`
  - 增加中文 BEAT2 四语言索引构建、缺失配对安全策略、additional 语义、输出契约和软链接说明。

### 真实数据结果

- 四语言源文件：
  - Chinese：CSV 316、NPZ 310、WAV 317；
  - English：CSV 1620、NPZ 1620、WAV 1628；
  - Japanese：CSV 66、NPZ 66、WAV 66；
  - Spanish：CSV 52、NPZ 52、WAV 52。
- NPZ/WAV 总数差 15 条的来源：7 条 Chinese `22_luqi_2_104_104` 至 `22_luqi_2_110_110` 在 CSV/WAV 中存在但缺 NPZ；8 条 English `22_luqi_0_9_9` 至 `22_luqi_0_16_16` 只有 WAV，不在 CSV/NPZ 中。Chinese 另有 `23_hailing_3_1_1` NPZ+WAV 配对，但不在 CSV 中，因此只报告为 orphan，不加入索引。
- 默认 dry-run 完整扫描后因 7 条正式 train 缺 NPZ 而按设计失败；增加 `--allow-missing-pairs` 后 dry-run 成功，精确验证 2047 条。
- 有效 split：train 1376、val 118、test 355、additional 198、minitrain 16；additional 未并入 train。
- 累计 7,137,223 帧，30 FPS 下 66.0854 小时；无缺 WAV、无非法 NPZ、无 LFS pointer、无短音频、无过短动作、无未知 split 或重复 ID。
- 正式输出 `/home/weili/datasets/BEAT2_official/all_splits.pth` 为 97,596 字节；`inputs/BEAT2` 已是指向该根目录的正确软链接。
- `BEAT2SmplDataset` 真实 smoke test 通过：1,376 条 train，前 3 条均输出 120 帧 `[120,63]` body pose 和 72,000 个 18 kHz 音频采样。
- 28 个针对性 pytest 通过，`py_compile`、Ruff 与正式 PTH 独立回读验证通过。

本工具只构建索引，没有复制或修改原始 NPZ/WAV/CSV，没有重处理动作，也没有预提取音频特征。

## 2026-07-22：AIST++ 官方 crossmodal 30 FPS 构建器

### 实现内容

- 新增 `tools/data/aistpp/build_annot_aist_official_30fps.py`
  - 严格读取并保留 `crossmodal_train.txt`、`crossmodal_val.txt`、`crossmodal_test.txt` 的原始顺序，默认要求 980/20/20、并集 1020 且完全互斥。
  - 复用现有 partial builder 的 motion、60→30 FPS、keypoints2d、tight bbox、camera、`get_c_rootparam()`、SMPL-X skeleton offset、音乐特征和 annot record 校验函数，没有复制另一套几何/FK 实现。
  - 只构建官方 split 并集；额外 motion、keypoints2d 和 musicfeat 仅写报告，不进入 train。
  - 发布前统一预检所有官方 ID 的 motion、keypoints2d、EDGE baseline35 `[L,35]`、camera mapping/environment JSON 和 ignore 交集；任何缺失或 ignore 冲突均不允许跳过后发布。
  - annot 插入顺序固定为 train→val→test；minitrain 按官方 train 原始顺序选择前 16 条足够长的序列。
  - 五个 PTH 先写 `.tmp`、重新加载完整验证，再通过带备份回滚的 `os.replace` 发布；失败时删除临时/半发布文件并恢复已有正式文件。
  - 输出 build/split/sequence/missing/extra/invalid/bbox/camera/scaling/validation 共 10 份独立报告，annot 顶层不加入 metadata。
  - `--limit` 仅允许 dry-run 或独立测试输出目录，不能把调试子集写到标准正式文件名。
- 新增 `tests/test_build_annot_aist_official_30fps.py`
  - 覆盖有序 split、重复/重叠、严格官方并集、extra 忽略、四类必需数据缺失、ignore 冲突、minitrain、标准文件名、partial 文件不变、dry-run、原子失败清理和正式回读验证。
- 更新 `README.md`
  - 增加中文的官方 crossmodal 构建命令、标准输出、严格预检、原子发布和 partial 数据集区别说明。

### 测试和真实数据审计

- `py_compile` 通过；新正式 builder 与原 partial builder 合计 43 个 CPU pytest 用例全部通过；Ruff 通过。
- 真实源统计：motion 1408、keypoints2d 1510、musicfeat 1389；官方 train/val/test 为 980/20/20，并集 1020且互不重叠。
- 官方 1020 条对 motion、keypoints2d、musicfeat、camera mapping/environment 的缺失数均为 0。
- 正确忽略官方 split 外的 extra：motion 388、keypoints2d 490、musicfeat 369。
- 真实默认 dry-run 按安全契约失败：官方 split 与 `ignore_list.txt` 有 28 条交集。冲突 ID 已完整写入 `outputs/aistpp_official_dryrun/missing_required_data.json`；没有生成任何标准正式 PTH。
- 为验证 ignore 冲突之外的完整 record 链路，额外执行了不发布文件的隔离诊断 dry-run：1020/1020 条均通过 motion、相机、bbox、音乐长度和 record 校验；总计 430,287 帧，即 30 FPS 下 3.9841 小时；bbox 填充 448 帧；train/val/test/minitrain 为 980/20/20/16。诊断报告位于 `outputs/aistpp_official_diagnostic_ignore_bypass/`，明确标记 `diagnostic_only_ignore_list_bypassed=true`，不能当作正式安全预检结果。
- partial 五个 PTH 及 `musicfeat_v2_partial_408/` 的 SHA256 汇总在前后检查中完全一致。

### 交集序列确认后的正式发布

- 用户随后明确确认 28 条 official/ignore 交集序列可以使用。
- builder 新增显式 `--allow-ignored-official`：默认行为仍是发现交集立即失败；只有人工审计后主动提供该参数才保留这些官方 ID。参数不会修改原始 `ignore_list.txt`，授权状态和完整交集列表会写入报告。
- 新增相应回归测试，验证默认拒绝、显式授权继续构建；新旧 AIST++ builder 合计 44 个 CPU 测试通过。
- 带显式授权的真实完整 dry-run 成功：1020/1020 条全部通过，train/val/test/minitrain 为 980/20/20/16，无缺 motion、keypoints2d、musicfeat 或 camera，无无效 record。
- 正式原子发布成功：
  - `annot_aist_30fps.pt`：395,886,946 字节；
  - `train.pt`：35,364 字节；
  - `val.pt`：1,500 字节；
  - `test.pt`：1,504 字节；
  - `minitrain.pt`：1,396 字节。
- 独立逐条回读验证通过：annot 插入顺序严格为官方 train→val→test，split 完全互斥且并集等于 annot；全部 430,287 帧的 record shape、float32、连续内存、有限值及对应 `[L,35]` 音乐长度均正确。
- `AISTPlusPlusSmplDataset` 使用默认标准文件名成功加载 train/minitrain/val/test，长度分别为 980/16/20/20；train 和 minitrain 实际取样均得到 `[120,35]` 音乐特征和 `[120,63]` body pose。
- partial 五个 PTH 以及 `musicfeat_v2_partial_408/` 的 SHA256 汇总仍与修改前完全一致。

## 2026-07-23：完整 GEM-SMPL 服务器训练就绪检查

### 实现内容

- 新增 `configs/exp/gem_smpl_server.yaml`
  - 保留 `amass_v11`、`bedlam_v2`、`h36m_v1`、`3dpw_v1`、`aistpp_train`、`beat2_static_train`、`humanml3d_static_train` 七个训练集。
  - 保留 EMDB1、EMDB2、3DPW 和 RICH 四个验证集，以及 regression + DDIM diffusion、文本/音乐/语音/图像条件、151 维 EnDecoder、AdamW、16-mixed、500K steps 和 0.5 梯度裁剪。
  - 仅移除当前不存在 `inputs/3DPW/hmr4d_support/imgfeats/3dpw_occ_train` 的 `3dpw_occ_v1` 及 `metric_3dpw_occ`；默认单卡 batch size 4、worker 4。
  - 原始 `configs/exp/gem_smpl.yaml` 未修改，SHA256 仍为 `5131c329caaf4c26ac5cd84da9a2e6cdbff7f096fe2c97ce9bfb000dc553f1dd`。
- 新增 `tools/train/preflight_gem_smpl.py`
  - 使用 Hydra compose 读取真实 `train`/`exp` 配置，不复制一套隐藏的数据集列表作为运行来源。
  - 全量验证 HumanML3D 23,242 个 motion/T5 key、63,044 条 caption 的 `[N,50,1024]` FP16 finite embedding。
  - 验证 AIST++ 1020/980/20/20/16 和所有官方 `[L,35]` 特征，同时允许并报告额外 369 个音乐特征。
  - 验证 BEAT2 train/val/test/minitrain 以及每个索引的 NPZ/WAV 配对；允许当前 train=1376，并设置 `non_exact_beat2_train_count=true`。
  - 检查全部回归、验证和身体模型产物；3DPW-OCC 仅作为 optional，缺失不会令 server 配置失败。
  - 逐个实例化七个训练 Dataset 和四个验证 Dataset，确定性读取样本，递归检查 Tensor/NumPy finite、SMPL shape、mask 与各条件分支。
  - 按 `scripts/train.py` 的真实 `_recursive_=False` 方式实例化 DataModule 和 GEM，检查一个混合 batch 与完整模型结构；可加载完整 checkpoint 并核验文本/音乐权重。
  - 输出原子 JSON 报告到 `outputs/preflight_gem_smpl/report.json`，strict 失败返回非零。
- 新增 `tests/test_preflight_gem_smpl.py`
  - 13 个 CPU 测试覆盖 AIST 正式 split/extra 特征、HumanML3D key、BEAT2 配对、server 配置、3DPW-OCC optional、混合 batch、finite、JSON、strict、原配置哈希及 RICH PyTorch 2.6 加载。
- 修复 `gem/datasets/rich/rich_motion_test.py` 和 `gem/datasets/rich/rich_utils.py`
  - 可信本地 RICH PTH 显式使用 `weights_only=False` 并兼容旧 PyTorch，解决 PyTorch 2.6 默认策略造成的反序列化失败。
  - 补入 GVHMR 官方 26 KB `gem/datasets/rich/resource/cam2params.pt`，SHA256 为 `e260ae6f8f65e8e0a56af2a1130181ec663f3a382147984272111aec74f7c260`；没有伪造或改写 RICH 标注。
  - `.gitignore` 仅为这一份官方 `cam2params.pt` 增加精确例外，其他 `.pt`/`.pth` 数据和模型文件仍保持忽略，避免服务器部署时再次缺少 RICH 必需资源。
- 更新 `README.md`
  - 增加中文服务器配置、完整 preflight、正式训练命令和 3DPW-OCC/RICH 说明。

### 真实 preflight 结果

- 状态：`PREFLIGHT PASSED`，完整运行耗时 77.95 秒；报告位于 `outputs/preflight_gem_smpl/report.json`。
- 训练 Dataset 长度：AMASS 52,788；BEDLAM 37,537；H36M 6,196；3DPW 88；AIST++ 980；BEAT2 1,376；HumanML3D 34,534。
- 验证 Dataset 长度：EMDB1 17；EMDB2 25；3DPW 37；RICH 191。
- 条件样本检查全部 finite：
  - AMASS 无图像/音乐/音频条件；
  - BEDLAM/H36M/3DPW 图像特征均为 `[120,1024]`，有效 mask 与各自样本长度一致；
  - AIST++ 为 `[120,35]` 音乐条件，仅启用 music mask；
  - BEAT2 为 18 kHz、72,000 samples 音频条件，仅启用 audio mask；
  - HumanML3D 为 `[50,1024]` 文本条件，caption 非空且 `has_text=true`。
- 混合 batch：`B=2`、时间维 120、text `[2,50,1024]`、music `[2,120,35]`、image `[2,120,1024]`、audio `[2,72000]`，来源为 AIST++ 与 AMASS，全部 finite。
- 完整 `gem.gem.GEM` 成功实例化：`regression_only=false`、`encode_text=true`、text dim 1024、EnDecoder dim 151。
- 5,518,923,941 字节官方 `gem_smpl.ckpt` 成功加载：709 个 state_dict key，文本与 music_embedder 权重均存在。
- 13 个目标 CPU 单元测试、py_compile 和 Ruff 全部通过；没有运行渲染，也没有修改任何已生成训练数据文件。

## 2026-07-23：移除 MetricRICH 未使用的 SMPL faces 依赖

- 审计 `gem/callbacks/metric/metric_rich.py`、`gem/utils/smplx_utils.py`、回调配置及全仓 `faces_smpl` 引用，确认 `MetricRICH.faces_smpl` 只有初始化赋值，没有任何后续读取；其他 metric 中的同名字段属于独立实现。
- 删除 `MetricRICH.__init__()` 中未使用的 `make_smplx("smpl").faces`，不再为 RICH 回调加载 `inputs/checkpoints/body_models/smpl/SMPL_NEUTRAL.pkl`。
- 保留 male/female/neutral 三个 `make_smplx("supermotion")` 模型，以及 `smpl_neutral_J_regressor.pt` 和 `smplx2smpl_sparse.pt` 两个必要回归矩阵。
- 没有修改相机坐标指标、全局指标、数据集过滤、metric key、聚合、日志名称或回调接口。
- 新增 `tests/test_metric_rich_init.py`：任何 `make_smplx("smpl")` 调用都会立即令测试失败，同时验证三个 SMPL-X 实例和两个回归矩阵仍被创建/加载。
- 验收结果：目标 pytest `1 passed`，`py_compile`、Ruff 和 `git diff --check` 全部通过。

## 2026-07-23：T5-3B + GEM 同 GPU 常驻文本动作服务

### 实现

- 新增 `gem/runtime/resident_text_motion.py`
  - `ResidentTextMotionEngine` 在启动阶段一次性加载 T5 tokenizer、FP16 T5-3B 和完整 GEM-SMPL，并把两个模型长期保留在同一 CUDA device。
  - 常驻服务使用延迟 diffusion 初始化：固定 DDIM steps/CFG 后只显式调用一次 `init_diffusion()`；`GEMDiffusion` 默认行为没有改变，单次 demo 和其他模型调用仍会在构造时正常初始化。
  - 新增与 GEM 编码一致的 `encode_prompt_with_loaded_t5()`，只把小型 CPU float32 contiguous `[50,1024]` embedding 移出 GPU，T5 本体不删除。
  - 实现线程安全 CPU LRU embedding 缓存、单并发 generation lock、请求/成功/失败统计、GPU allocator/物理显存快照、预热、状态和优雅关闭。
  - 正常请求不执行 `gc.collect()`、不卸载模型、不调用 `torch.cuda.empty_cache()`；只有 OOM 恢复和最终关闭允许清理。
  - 每次请求复用现有 `build_text_only_data()`、`validate_smpl_prediction()`、`save_results()`、`enforce_zero_shape()` 和 READY 原子发布，不复制 GEM 推理或 GMR 逻辑。
- 新增 `scripts/demo/demo_smpl_text_server.py`
  - stdin 模式支持纯文本、JSON 行、`/status`、`/help`、`/clear-cache`、`/quit`。
  - ZMQ 模式使用单线程 REP JSON 协议；单请求错误返回结构化结果并继续服务，SIGINT/SIGTERM 会安全关闭。
- 新增 `scripts/demo/text_motion_client.py`
  - 支持 prompt/prompt file、帧数、FPS、seed、request ID 和超时，输出格式化 JSON。
- 扩展 `gem/runtime/artifact_publish.py`
  - 新增带 fsync 和 `os.replace` 的原子 JSON 更新，用于 `latest_ready.json`；READY 目录协议保持不变。
- 小幅修改 `scripts/demo/demo_smpl_text.py`
  - 单次模式复用新的 loaded-T5 编码函数，但仍保持原 CLI、先释放 T5 再加载 GEM、输出和 dry-run 行为。
- 小幅修改 `scripts/demo/demo_utils.py` 和 `gem/network/gem_diffusion.py`
  - 增加默认关闭的 `defer_diffusion_init`，只供常驻服务保证 DDIM 初始化一次；模型结构、checkpoint key 和其他调用默认行为不变。
- GMR streamer 无需修改：现有 `MotionWatcher` 已监视 `outputs/text_motion` 中所有带 READY 的直接子目录；生成服务和 GMR/SMP1 仍是独立进程。

### 测试与真实 RTX 4090 验收

- 新增 `tests/test_resident_text_motion.py` 和 `tests/test_text_motion_server_protocol.py`，覆盖模型只加载一次、DDIM 单次初始化、缓存、zero betas、READY/latest、失败恢复、OOM、正常路径不清 cache、stdin/ZMQ、管理命令和优雅关闭。
- 新服务、协议和原单次文本 demo 合计 62 个 CPU 测试通过；`py_compile`、Ruff、`git diff --check` 和原单次 `--dry_run` 均通过。
- 真实启动显存：
  - CUDA 初始化 free 约 22.119 GiB；
  - T5 加载后 allocated 3.061 GiB；
  - T5+GEM 加载后 allocated 5.041 GiB；
  - 30 帧预热后 allocated 5.048 GiB、free 16.969 GiB。
- 三个真实请求：
  - 120 帧 squat：总计 0.549 秒，生成 0.516 秒；
  - 120 帧 wave：总计 0.507 秒，生成 0.485 秒；
  - 300 帧 walk：总计 0.786 秒，生成 0.758 秒。
- 同一服务继续执行 50 次请求（40×120 帧、10×300 帧）：
  - 总耗时中位数 0.496 秒、最大 0.790 秒；
  - 120 帧生成中位数 0.474 秒；
  - 300 帧生成中位数 0.757 秒；
  - 46 次 embedding cache hit；
  - 前后 allocated 都为 5.0484657 GiB，增长 0 MiB。
- 共检查 53 个真实输出目录：全部包含 READY，SMPL shape 正确，global/incam betas 全零；`latest_ready.json` 指向 sequence 53。
- `/quit` 后服务打印 `SERVICE STOPPED`，独立进程退出，PyTorch allocated 回到 0 GiB，GPU free 恢复约 22.123 GiB。

## 2026-07-23：HumanML3D 训练文本中英对照重新排版

- 更新 `HumanML3D训练集文本_中英对照_200条.txt`。
- 删除每条文本前的 HumanML3D motion key，仅保留连续编号。
- 将 200 条动作分为行走跑步、跳跃平衡、下蹲跪地、手势礼仪、搬运劳动、格斗踢腿、模仿舞动 7 类。
- 每条改为中文在第一行、英文在第二行；中文压缩为简短动作含义。
- 已重新对照实际训练文件 `humanml3d_smplhpose_train.pth`：200 条英文训练原文全部匹配，未加入训练集之外的英文文本。

## 2026-07-23：新增常驻音乐到 SMPL-X 动作生成服务

- 新增 `gem/runtime/resident_music_motion.py`
  - 启动时只加载一次完整 GEM-SMPL，只初始化一次固定 DDIM/CFG，不加载 T5。
  - 使用串行推理锁和线程安全 CPU EDGE baseline35 LRU 缓存；缓存 key 包含音频真实路径、文件 stat、选择范围和特征版本。
  - 每次请求复用现有 `demo_music.py` 的输入构造、输出校验、zero betas、151 维诊断、制品写入和 READY 原子发布逻辑。
  - 支持音频路径白名单、symlink 越界防护、结构化错误、OOM 恢复、状态查询、缓存清理和优雅关闭。
- 新增 `scripts/demo/demo_music_server.py`
  - 支持 stdin 和单线程 ZMQ REP；stdin 可直接输入包含空格的 WAV/MP3/FLAC 服务端路径，也支持 JSON。
  - 管理命令为 `/status`、`/help`、`/clear-cache`、`/quit`，ZMQ 默认只监听 `127.0.0.1:7011`。
- 新增 `scripts/demo/music_motion_client.py`
  - 通过 ZMQ 发送服务端音频路径、范围、seed、request ID 和可选 metadata，不上传音频字节。
- 更新 `gem/utils/music_features.py`
  - 优先使用 `offset`/`duration` 只解码选中范围；后端不支持时回退完整解码切片，并在 metadata 记录 `range` 或 `full_fallback`。
  - EDGE 的 15360 Hz、512 hop、30 FPS 和 35 通道顺序保持不变。
- 新增 `tests/test_resident_music_motion.py`、`tests/test_music_motion_server_protocol.py`，并扩展 `tests/test_music_features.py`。
  - 覆盖单次模型/DDIM 初始化、缓存命中与失效、LRU、zero betas、READY/latest、异常/OOM 恢复、白名单、stdin/ZMQ/客户端和范围解码。
- 更新中文 `README.md` 和 `docs/MUSIC_DEMO.md`，补充启动命令、服务端路径语义、缓存、长音乐限制、READY 消费和 v1 不渲染/不 mux 的说明。
- 现有单次音乐 Demo、常驻文本服务、MotionWatcher、GMR streamer、SMP1、GMR-CPP 和 BUMI3 协议未修改。

### 测试与 RTX 4090 真实验收

- 指定的音乐、文本服务和 streamer 回归测试共 `125 passed`；`py_compile`、Ruff、Ruff format check 和 `git diff --check` 通过。
- GTZAN 真实 WAV 的 2 秒 CPU dry-run 得到 `[61,35]` 有限 EDGE 特征，只有 music condition mask 开启。
- 常驻服务真实启动只出现一次 GEM checkpoint 加载和一次 DDIM 初始化；30 帧 warmup 成功且未发布目录。
- 连续执行 20 个真实 61 帧请求：
  - 全部成功，全部包含 READY 和 7 个必需文件；
  - global/incam SMPL-X shape 正确且 betas 全零；
  - 首次 cache miss：特征提取约 1.517 秒，总计约 2.007 秒；
  - 第二次 cache hit：特征查找约 0.04 毫秒，总计约 0.474 秒；
  - 20 次总耗时中位数约 0.465 秒，生成中位数约 0.446 秒；
  - cache hits/misses 为 19/1；
  - PyTorch allocated 首尾均约 1.9876 GiB，增长 0 MiB，峰值约 1.9997 GiB。
- 验收输出位于 `outputs/music_motion_resident_validation/`，`latest_ready.json` 指向 sequence 20。
- `/quit` 后打印 `[ResidentMusic] SERVICE STOPPED`，服务进程正常退出。

## 2026-07-24：统一多模态常驻动作控制服务

### 实现内容

- 新增 `gem/runtime/resident_multimodal_motion.py`
  - 一个进程只创建一个 T5 tokenizer、一个 T5-3B、一个完整 GEM-SMPL 和一个 DDIM。
  - 文本、音乐和文本+音乐三种生成共用同一个串行 `generation_lock`。
  - 文本 embedding 与 EDGE baseline35 音乐特征使用 CPU float32 LRU 缓存，不缓存 GPU 中间张量。
  - `text_music` 在同一个 GEM data/batch 中同时设置 `[50,1024]` 文本特征和 `[L,35]` 音乐特征，只调用一次 `GEM.predict()`；不是分别生成动作后混合。
  - 固定服务级 `clip_frames` 和 30 FPS；音乐边界差异仅在 ±2 帧内使用现有 `trim_or_pad_last` 对齐。
  - 三种模式统一向 `outputs/multimodal_motion` 发布直接 READY 子目录，global/incam betas 均强制为零。
- 新增 `gem/runtime/resident_video_session.py`
  - 将 YOLOX、ViTPose、可选 HMR2、ONNX denoiser 和 EnDecoder 组成只加载一次的 `ResidentVideoModelStack`。
  - 切换摄像头、视频文件或 RTSP 时只重新打开源和创建 tracker，不卸载模型。
  - 生成期间通过 pause/ack Event 确认视频 GPU 推理暂停；生成完成后清空 tracker、滑窗、rollout state 和 frame index 再恢复。
- 修改 `scripts/demo/demo_webcam.py`
  - 增加可选 `frame_sink`、外部常驻模型栈和关闭内部 GMR bridge 的注入接口。
  - 统一服务中 Webcam 只提交最新的零体型 `SMPLFrame`；原 CLI 未传这些参数时仍按原方式加载模型和创建 GMR。
- 新增 `gem/runtime/motion_source_mux.py`
  - `MotionSourceMux` 是唯一持有 `GMRUDPBridge`、`SMPLXGMRReference`、EnDecoder FK 和 30 Hz 发送线程的组件。
  - 实时视频只保留最新帧；过期自动回到 idle。
  - READY clip 复用现有 `load_smpl_motion`、`MotionPlayer`、旋转插值、根节点对齐、blend 和 return。
  - 生成期间视频输出不中断；clip 播放完成后返回视频或安全 idle；ESTOP 优先级最高。
  - 所有发送帧和每次 FK 的 betas 都是全零，SMP1 magic、版本、412 字节包和 14 个目标没有修改。
- 新增 `scripts/demo/demo_multimodal_server.py`
  - 提供默认 `tcp://127.0.0.1:7020` 的 ZMQ REP 服务和可选 stdin JSON 行模式。
  - 支持 `text`、`music`、`text_music`、`video_start`、`video_stop`、`idle`、`estop`、`clear_estop`、`status`、`clear_cache` 和 `shutdown`。
  - `video_text`、`video_music`、`video_text_music` 明确返回 `UnsupportedModeError`，不会静默退化。
  - 音频/视频均按服务端路径处理，并支持重复路径白名单、真实路径解析、扩展名和普通文件检查。
- 新增 `scripts/demo/multimodal_motion_client.py`
  - 严格互斥地构造上述 ZMQ 请求，支持文本、音乐、联合生成和所有控制操作。
- 修改 `gem/runtime/__init__.py`，导出统一引擎、视频会话和 Mux。
- 新增四组 CPU 测试：
  - `tests/test_resident_multimodal_motion.py`
  - `tests/test_multimodal_server_protocol.py`
  - `tests/test_motion_source_mux.py`
  - `tests/test_resident_video_session.py`
- 更新中文 `README.md`，加入统一服务架构、模式约束、三种生成请求、视频控制、stdin/ZMQ、路径安全、单一发送端和 sim/robot 安全说明。

### CPU 测试

- 统一服务、新旧常驻引擎、Webcam shape 和 GMR streamer 回归：`96 passed`。
- 原文本 Demo、音乐 Demo、READY 发布以及原文本/音乐服务协议回归：`70 passed`。
- 合计执行 166 个测试，全部通过；仅保留已有 PyTorch deprecated 和短测试音频的 librosa warning。
- `py_compile`、Ruff check、Ruff format check 和 `git diff --check` 全部通过。

### RTX 4090 真实服务验收

- 使用 `inputs/pretrained/gem_smpl.ckpt`、本地 T5-3B、20 步 DDIM、120 帧、30 FPS、`data/dance3.mp4` 启动 eager 服务。
- 实际实例计数：
  - GEM：1；
  - T5：1；
  - DDIM 初始化：1；
  - 视频模型栈加载：1；
  - GMR UDP 发送端：1。
- 启动显存：
  - CUDA 初始化：allocated 0 GiB，free 21.994 GiB；
  - T5 后：allocated 3.061 GiB；
  - GEM 后：allocated 5.041 GiB；
  - warmup 后：allocated 5.048 GiB；
  - 视频模型栈后：PyTorch allocated 5.055 GiB，free 11.254 GiB。ONNX Runtime 占用不计入 PyTorch allocated。
- 首轮请求：
  - 文本：总响应 0.575 秒；
  - 音乐首次特征提取：2.026 秒，其中 librosa 特征约 1.465 秒；
  - 音乐缓存命中：0.561 秒；
  - 文本+音乐双缓存命中：0.613 秒。
- 连续 50 次缓存命中请求：
  - 文本平均 0.573 秒，最大 0.667 秒；
  - 音乐平均 0.574 秒，最大 0.639 秒；
  - 文本+音乐平均 0.593 秒，最大 0.617 秒；
  - PyTorch allocated 增长 0 MiB，reserved 增长 0 MiB，低于 256 MiB 验收上限；
  - 54 次累计真实生成全部成功，没有 CUDA OOM。
- 视频模型实际处理 3067 个有效帧，模型栈始终只加载一次。4 次 clip 恢复都触发 tracker/滑窗/rollout/frame index reset，随后重新进入 `VIDEO_LIVE`；日志中没有视频推理错误。此项验证的是服务状态和数据连续化，实机视觉效果仍需在 MuJoCo/GMR 可视化中人工检查。
- Mux 实际持续约 30.0 Hz 发送，单次生成、特征提取、文件保存和 50 次连续请求均未阻塞发送线程；SMP1 包仍为 412 字节。
- 54 个 READY 目录全部是 output root 的直接子目录，来源统计为 18 个 `text_only`、19 个 `music_only`、17 个 `text_music`。全部为 `[120,63]` body pose、有限值，并且 global/incam `[120,10]` betas 全零。
- 实测 `video_stop → idle → estop → clear_estop → shutdown` 均返回成功；ESTOP 清除后回到 HOLDING，关闭后单一 Mux 发送线程和服务正常退出。
- 由于本轮没有启动并人工观察 GMR-CPP/MuJoCo 界面，机器人重定向后的视觉连续性和实物安全效果不能仅由以上自动测试替代，仍必须先完成 MuJoCo、低速、吊装保护和物理急停验收。

## 2026-07-24：修复生成动作结束和下一动作开始时的根朝向瞬跳

- 修复 `gem/runtime/motion_streamer.py`：
  - 原实现返回 idle 时同时将 idle 的 yaw 对齐到动作最后一帧，因此会保留动作最终朝向。
  - Mux 随后又发送固定 idle 朝向，导致动作结束后的下一帧可能瞬间转向；播放器内部仍保留旧 yaw 时，下一条动作开始还可能反向瞬跳。
  - 新实现只保留当前水平根位置，使用 idle 自身的 `global_orient` 作为返回目标，通过现有 quaternion SLERP 在 `return_seconds` 内平滑回到 idle 朝向。
  - idle 的高度和姿态来自安全 idle 文件，不把根位置瞬移回世界原点。
- 修复 `gem/runtime/motion_source_mux.py`：
  - `MotionPlayer` 完成平滑返回后继续发送其最终帧，不再在下一发送周期用原始 idle 帧覆盖，从而消除一帧 yaw/translation 跳变。
  - 视频源真正过期时仍优先切换到安全 idle。
- 扩展 `tests/test_motion_source_mux.py`：
  - 构造最后一帧 yaw 为 180° 的动作；
  - 验证其在 `return_seconds` 内回到正面；
  - 验证 RETURNING → HOLDING → IDLE 之间旋转矩阵连续；
  - 验证下一动作启动首帧不会恢复旧的180°朝向。
- 真实 `rock.00013` READY 动作离线检查：
  - 进入动作前 yaw 为 0°；
  - blend 完成时仍为 0°；
  - 返回过程从动作最终约 -4.4°平滑收敛到 0°；
  - HOLDING 后下一 idle 帧继续为 0°，不存在单帧跳变。
- `tests/test_motion_source_mux.py`、`tests/test_motion_streamer.py` 和 `tests/test_stream_smpl_params_to_gmr.py` 共 `38 passed`；Ruff、`py_compile` 和 `git diff --check` 通过。
- 视频文件的120帧参数仍只表示 ONNX 滑动上下文长度，不限制视频播放长度；`video_start --video_path` 会按原视频 FPS 处理到文件结束或收到 `video_stop`。

## 2026-07-24：修复生成动作后启动视频回到原点

- 问题原因：
  - 文本或音乐动作播放后，人物可能已经移动到新的世界位置；
  - 每次新视频 session 都会清空 rollout，视频第一帧的 `transl` 从视频自己的局部原点重新开始；
  - 原 Mux 直接使用该视频根节点，因此会把人物瞬间拉回原点，并可能同时产生根朝向和身体姿态扭曲。
- 修改 `gem/runtime/motion_source_mux.py`：
  - 每次 `start_video_mode()` 都令视频根标定失效，等待新 session 的第一帧重新标定；
  - 使用现有 `align_motion_root_yaw()` 将视频第一帧 yaw 对齐当前发送帧；
  - 记录一次性的 yaw 旋转、视频源起点和当前世界目标起点；
  - 后续视频帧统一应用该刚体变换，保留视频内部的相对位移和转向；
  - 在 `blend_seconds` 内从当前动作/idle 姿态平滑进入第一批视频帧；
  - 视频过期回安全 idle 时保留当前水平根位置，不再回到 idle 文件的世界原点；
  - 状态增加 `video_root_aligned` 和 `video_alignment_count` 诊断字段。
- 增加回归测试：
  - 当前人物位于 `[5,0,3]` 且朝向90°、新视频从原点且朝向180°开始；
  - 验证视频首帧位置和朝向与当前输出完全连续；
  - 验证后续视频一米相对位移在对齐后的世界坐标中仍为一米；
  - 验证视频过期回 idle 不改变当前水平位置。
- 使用真实文本 READY 动作复现：
  - 视频启动前位置为 `[-0.0304, 0, 1.6528]`；
  - 对齐后视频首帧仍为同一位置，位置跳变量为 `0.0`；
  - 模拟视频过期回 idle 后仍为同一位置，位置跳变量为 `0.0`。
- Mux、MotionPlayer、旧 streamer、统一服务协议和视频 session 共 `57 passed`；Ruff、`py_compile` 和 `git diff --check` 通过。

## 2026-07-24：Motion-X++ 三维动作与语义文本训练支持

- 新增 `tools/data/motionxpp/common.py`：
  - 用统一 `AssetRef` 同时读取原始 ZIP 和已解压目录；
  - ZIP 内部任意长前缀不会影响 basename stem 精确配对；
  - 解析官方 `[F,322]`、NPZ 和分字段 dict；
  - 实现固定坐标旋转、轴角 quaternion SLERP、平移线性重采样、内容 hash、
    原 sequence group split、原子 JSON/PTH 写入和结构验证。
- 新增 `inspect_motionxpp.py`：
  - 本地真实数据发现 7 个 subset；
  - SMPL-X 与 semantic text 均为 25,861 个，精确配对 25,861 个；
  - keypoints 为 25,862 个，`kungfu` 多 1 个；
  - 真实 keypoint JSON 有 COCO-WholeBody 像素坐标与置信度，但没有图像宽高和
    calibrated K，因此第一版不启用关键点条件；
  - 官方资料确认 30 FPS；官方预处理和真实根平移统计共同确认发布数据是 Y-up。
- 新增 `build_motionxpp_genmo.py`：
  - 输出 train/val/test JSONL manifest 和每 512 条一个的 motion shard；
  - 确定性 98/1/1 group hash split，防止同一 sequence 的 clip 跨 split；
  - 每个分片都带 fingerprint 和 meta，支持严格 resume；
  - 记录拒绝、重复、坐标审计、异常速度/位移/角度统计，不静默删除；
  - 不写入未校准的 2D 条件，也不修改任何原始 ZIP。
- 新增 `extract_t5_embeddings.py`：
  - 按 motion manifest 逐个小分片读取 caption；
  - 生成 `[caption数,50,1024]` CPU FP16 T5-3B 分片；
  - 提供 fingerprint、原子发布、重载 finite/shape 验证和 resume；
  - 不生成需要整体加载的 `all_text_embed.pth`。
- 新增 `MotionXppDataset`：
  - motion/embedding 两个独立 worker 本地 LRU，默认各缓存 2 个 shard；
  - 随机 caption、随机 120 帧裁剪和短动作 valid padding；
  - 返回与 `Humanml3dDataset`、现有 collate 和 dual-mode pipeline 兼容的字段；
  - 3D 样本固定 `2d_only=false`，未校准 kp 固定 `has_2d_mask=false`；
  - 不手工伪造 audio/music 数据，由现有 collate 默认补齐。
- 新增 `preflight_motionxpp.py`：
  - 抽查 motion/embedding key、shape、finite 和 caption 数；
  - 实例化 Dataset 并用现有 `collate_fn` 组成 batch_size=2。
- 新增 Hydra 配置：
  - `motionxpp_static_train.yaml`；
  - `gem_smpl_motionxpp.yaml`，保留完整 GEM 网络，从官方 `gem_smpl.ckpt`
    权重微调，学习率 `2e-5`，20,000 步，不加入 BEAT2/3DPW-OCC。
- 本地真实数据 smoke：
  - `animation.zip` 转换 8 条，0 拒绝、0 重复，668 帧、2 个 motion shard；
  - 使用本地缓存的真实 T5-3B 在 RTX 4090 上提取 8 条 caption，得到 2 个 FP16
    embedding shard；
  - 使用真实 SMPL-X body model 读取 8 条 Dataset 样本并组成 batch，preflight 通过。
- 新增 4 组 CPU 测试，共 `16 passed`：
  - ZIP/解压目录、直接 322 维/分字段格式、30/60 FPS、NaN、重复 hash；
  - group split、shard resume、随机裁剪、短动作 padding、LRU pickle；
  - dummy T5 caption 映射、embedding 分片和 resume。
- 完整服务器命令和当前功能边界写入 `docs/motionxpp.md`。本地未启动全量转换、
  全量 T5 或正式训练。

## 2026-07-28：整理完整中文训练复现手册

- 新增 `docs/TRAINING_REPRODUCTION_CN.md`，将此前分散在 README、数据文档、构建报告
  和阶段记录中的操作整理为一条从空机器开始的完整复现路线。
- 手册包含：
  - 项目和 GEM-SMPL 框架的快速理解；
  - Ubuntu、uv、Python 3.10、PyTorch CUDA 环境安装和验证；
  - 完整 GEM checkpoint、T5-3B、SMPL-X 与 GVHMR 预处理数据的获取方法；
  - HumanML3D、AIST++、BEAT2 和 Motion-X++ 的下载、目录组织、构建、特征提取与
    真实产物统计；
  - `gem_smpl_server` 完整训练、`gem_smpl_regression` 回归训练和
    `gem_smpl_motionxpp` 微调的适用边界；
  - 统一 preflight、20 步 smoke、单卡、多卡、W&B、checkpoint 权重加载和真正断点
    恢复命令；
  - 新服务器迁移检查表、常见错误和需要额外备份的本地资产。
- 明确记录两个无法由当前仓库脚本独立重建的关键资产：
  - `outputs/humanml3d_amass_exact_coverage.csv` 是构建器的输入，不是构建器输出；
  - AIST++ baseline35 必须来自每个 sequence 的同名对齐 WAV，仓库不会猜测音乐偏移。
- README 的训练章节增加中文完整复现指南入口；原有简明训练命令保持不变。

## 2026-07-29：补充新机器快速 Demo 验证路线

- 在 `docs/TRAINING_REPRODUCTION_CN.md` 中增加独立的“拿到仓库后的快速 Demo 验证”
  章节。
- 新章节从仓库克隆和虚拟环境开始，依次给出：
  - 完整 GEM checkpoint、SMPL-X 和本地 T5-3B 的最少准备；
  - 不加载大模型的文本、音乐 dry-run；
  - 真实纯文本 DDIM 生成和 `smpl_params.pt` shape/finite/zero-beta 检查；
  - 真实音乐生成、EDGE baseline35 制品和可选音频合成；
  - HMR2/ViTPose 离线视频 Demo；
  - ONNX Runtime 摄像头和视频实时 Demo；
  - 新机器最短四步验证顺序。
- 明确区分完整 PyTorch 文本/音乐扩散路径与实时 ONNX regression 路径，避免只验证
  webcam 后误认为完整生成模型也已经配置成功。

## 2026-07-29：Motion-X++ 改为八卡从头训练

- 修改 `configs/exp/gem_smpl_motionxpp.yaml`，不再以官方 `gem_smpl.ckpt` 作为默认
  微调起点，显式设置 `ckpt_path: null`，从随机初始化开始训练。
- 正式训练参数改为：
  - AdamW 学习率 `2e-4`；
  - `max_steps=500000`；
  - 8 张 GPU；
  - 每张 GPU batch size 128；
  - 无梯度累积时全局 batch size 1024；
  - 每 3000 个优化步骤验证一次；
  - 保持 `16-mixed` 和梯度裁剪 0.5。
- README、Motion-X++ 专项文档和完整训练复现指南中的旧微调命令已同步改成从头训练，
  首次启动命令不再传入 `ckpt_path`。
- `resume_mode=last` 仍用于恢复本实验自己的 optimizer、scheduler 和 global step，
  与首次随机初始化训练明确区分。
## 2026-07-31：gem_smpl_server 适配单机 8×6000D

- 将 `configs/exp/gem_smpl_server.yaml` 的默认训练规模调整为单机 8 卡：
  `devices=8`、`num_nodes=1`、`strategy=auto`。
- 每个 GPU 的 `batch_size` 调整为 128，8 卡全局 batch 为 1024；仍使用
  `16-mixed`、梯度裁剪 0.5、500,000 steps 和每进程 4 个 DataLoader worker。
- 单卡无需修改配置文件，只需覆盖 `pl_trainer.devices=1`，此时单卡 batch 仍为 128，
  Lightning 会从多卡 DDP 自动退化为单 GPU。
- 增加配置回归测试，并同步更新 README 与完整训练复现指南。

## 2026-08-04：评估阶段 NaN/Inf 防护、异常留档与安全跳过

- `gem/utils/eval_utils.py` 新增统一的 `InvalidMetricInputError`、逐 Tensor 有限值诊断、
  逐帧点云退化检查、最短有效帧检查和精简原子 dump；诊断包含 shape、NaN、正负
  Inf、非有限总数、前 20 个异常帧及有限值范围，且不使用 `nan_to_num`。
- 相机与全局指标在 mask 前后约定的位置检查输入、对齐结果和最终指标；异常序列整体
  拒绝，不会删除某个异常帧后继续计算加速度、轨迹或 jitter。
- Procrustes 在原 device 上局部改用 float64 和
  `torch.linalg.svd(..., full_matrices=False)`，正确处理 `Vh`，检查输入、方差、协方差
  `K`、SVD 输出、旋转、尺度、平移和最终点云，并恢复原 dtype/device。
- EMDB、3DPW、RICH 回调增加 `invalid_policy={skip,raise}` 与 `dump_invalid`；预测参数在
  body model 前检查，GT 参数/坐标变换、body model 顶点、转换后顶点和关节进入指标前
  再检查。RICH 的相机和全局指标采用事务式提交，任一部分失败均不会留下部分指标。
- 默认异常文件写到运行目录的 `eval_nonfinite/`，只保存预测 SMPL 参数、诊断和少量
  batch 元数据，不保存完整顶点；文件名含 step、epoch、rank、dataset、sequence 和
  唯一后缀，使用临时文件加 `os.replace` 原子发布。
- epoch 末使用现有 `all_gather` 一次性汇总指标与异常序列，仅 global rank 0 输出摘要；
  全部序列异常时安全跳过聚合并清空状态，不再执行 `np.concatenate([])`。
- 四份 metric YAML 显式启用 `invalid_policy: skip` 和 `dump_invalid: true`；严格模式的
  实际 Hydra 路径为 `callbacks.metric.<callback_name>.invalid_policy=raise`。
- 新增 CPU 测试覆盖正常指标、NaN、正负 Inf、退化点云、零方差、已知 Procrustes、
  旧算法数值回归、标量/空 Tensor、原子精简 dump 和 skip/raise 策略。

## 2026-08-19：`main` 分支 8 月 4 日后记录补录说明

- 本文档此前停在 8 月 4 日。本次以当前 `main` 的可达 Git 历史为准，补录
  2026-08-12 至 2026-08-19 的 21 个提交，不使用其他分支的提交凑数。
- 上面的 NaN/Inf 防护条目对应提交 `fb52927`，Git commit date 为 2026-08-12；已有
  内容完整，所以本次只补充提交关联，不重复复制功能说明。
- `main` 在 `06060b4` 后继续开发 SMPL 151D 物理损失、GENMO→GMR→GMT 和 TensorRT
  实物部署；BUMI-native 93D 训练线从该提交处分叉，属于
  `feature/bumi-music-only`，不计入本分支实现记录。

## 2026-08-12：新增独立 GEM-SMPL Music-only 专家模型

提交：`e62da9a`。

- 新增独立 `gem_smpl_music_only` 实验，只接受逐帧对齐的 EDGE baseline35
  `music_embed[B,T,35]`；图像、相机、文本和语音不再作为网络条件。
- 保留 SMPL 151D 表示、扩散 Transformer、EnDecoder 和原几何监督。Music-only 指
  “唯一条件是音乐”，不是删除原动作/几何损失。
- 训练使用 diffusion-only，以 10% sample-level 概率丢弃整段音乐训练 unconditional
  branch，并关闭旧逐帧随机 null，建立唯一的 music CFG 语义。
- 扩充 AIST++ reader 和严格 preflight，检查 official 980/20/20 split、30 FPS、
  motion/music 帧对齐、`[T,35]` shape、finite 和 split 不重叠。
- 新增 checkpoint demo、评估工具、训练文档和专项测试；旧 generalist、回归路径和
  checkpoint 加载语义保持兼容。

## 2026-08-13：修复 Music-only 接触监督、AIST++ 平移尺度和步级调度

提交：`bc479cb`、`9606969`。

- 修复 Music-only AIST++ 样本没有正确启用足部静止/接触监督的问题，训练和评估配置
  显式声明接触目标。
- scheduler 改为按 optimizer step 更新，避免数据量或 epoch 长度变化导致学习率衰减
  偏离计划。
- AIST++ 构建增加 root translation 单位审计和米制转换，防止错误量纲让脚速度阈值
  失效并污染 contact 标签。
- Dataset 与 builder 均检查尺度 metadata；测试覆盖合法米制、可转换尺度和异常尺度
  拒绝路径。

## 2026-08-13：三套新数据转换与四数据集 Music-only 统一训练

提交：`b92e2b6`、`8dda341`、`2e4fffa`、`ca362dd`、`f5333dd`、`3b67738`。

- 为 AIOZ-GDANCE、FineDance、CoMPAS3D 分别建立 inspect、convert、validate 工具，
  正式产物统一使用 manifest、canonical SMPL motion、EDGE35 和音频引用。
- AIOZ 明确 group/dancer 与共享音乐；FineDance 支持数字 `song_name` 并隔离音乐缓存；
  CoMPAS3D 明确 pair/take/leader/follower，两种角色共享音乐但动作分别保留。
- 转换时检查坐标轴、米制、30 FPS、帧数、shape、finite、重复 ID、路径边界和原子发布，
  原始数据不被覆盖。
- 新增统一四集 SMPL reader，返回同步 120 帧 motion/music 窗口；三套转换数据已经是
  GENMO Y-up 米制，reader 不重复变换。synthetic camera 只维持辅助几何监督自洽，
  唯一有效模型条件仍为音乐。
- 新增 `gem_smpl_music_only_4set`，不覆盖单 AIST++ 基线。当时采用 duration-aware
  `ConcatDataset + shuffle`，后续 physics_v1 已改为去重分层采样。
- 完成真实四集 mixed batch、完整模型 forward/backward 和 batch 128 的 100-step AMP
  smoke，按 `max_steps=100` 正常停止。

## 2026-08-14：四数据集动作筛选、引用清理和 AIST++ 坐标修复

提交：`1dd46de`、`06060b4`。

- 新增 review package 导出、结果验证、应用和正式数据复检；按 sample ID 精确回写，
  不覆盖原始动作。
- 删除动作时同步统计 EDGE35 引用；仍被其他舞者/角色引用的音乐保留，零引用特征进入
  隔离区，避免误删共享音乐。
- 新增 `gem_smpl_music_only_4set_curated`，让筛选数据与原四集基线并存。
- 修复 AIST++ review 导出重复坐标变换；新增安全增量刷新，只重建受影响 AIST++ 项，
  并验证其他文件 hash 不变。
- review package、筛选结果和正式数据分别检查 schema、shape、坐标、finite、帧数和
  音乐引用闭包。

## 2026-08-14：Music-only checkpoint、ONNX 与带音乐视频验证

提交：`0a09731`、`9133a29`、`4b48780`。

- 新增 checkpoint 验证，从 WAV 提取真实 EDGE35，执行固定 seed 的 DDIM/CFG，检查
  151D、SMPL FK、root 轨迹、有限值和基本运动合理性。
- 新增固定 120 帧 ONNX 导出，把 EDGE35 embedding、conditional/unconditional CFG
  和一次 denoiser `x0` prediction 纳入图；支持 PyTorch/ONNX Runtime 单步及完整 DDIM
  数值对照。
- 验证产物可渲染 SMPL 视频并用 ffmpeg 保留原音乐，报告保存 checkpoint、seed、CFG、
  DDIM、帧数和文件路径。
- 修复 AIST++ translation 量纲和 gravity axis，统一 Y-up 生成、root rollout 与渲染；
  增加 motion sanity，异常朝向、跳变和非有限结果不再静默通过。
- 完善 `demo_music_only.py` 的裁剪、完整音乐时长、产物目录和视频路径；PyTorch 离线
  demo 与 ONNX 验证互不替换。

## 2026-08-17：[feature/bumi-music-only] 建立 BUMI-native Music-only 骨架

提交：`561143a`。

- 新增独立 `motion_backend=bumi`，使 Music-only GENMO 直接生成机器人状态，不在在线
  推理中依赖 SMPL→GMR；原 SMPL 151D 路径和实验保持不变。
- 定义 MuJoCo-native `qpos28`：root XYZ 3D、`wxyz` root quaternion 4D 和 21 个真实
  关节角；定义 93D 学习表示：root local position 3D、root rot6d 6D、关节角 21D、
  21 个 feature body 位置 63D。
- 实现以每个 crop 第一帧 XY/yaw 为 anchor 的 canonicalization，保留 root 高度、roll、
  pitch 和相对轨迹；提供 canonical/world qpos 组合。
- 新增由版本化机器人资产驱动的可微 Torch FK、93D codec、统计量归一化、机器人损失、
  运动学指标和 MuJoCo parity 工具；最终几何始终从预测 qpos 重新 FK，raw 63D 不做 IK。
- 新增 BUMI dataset/DataModule、Music-only GEM/Pipeline、SMPL music checkpoint 显式迁移
  adapter、Demo、评估、渲染和初始 Hydra 配置。
- 初始实现提供完整数据契约和测试骨架，但此时正式 legacy pickle→训练数据转换器、
  生产资产绑定和正式随机初始化实验仍在后续提交完成。

## 2026-08-17：SMPL 151D 时序损失、地面 sidecar 和去重采样

提交：`8615423`、`de5ee49`。

- 新增 `gem_smpl_music_only_4set_physics_v1` 派生实验，保留原四集配置作为基线，并可从
  已有 checkpoint 重置 optimizer 后微调。
- 增加 Root 位移、关节 SO(3)、FK body 的速度/加速度/jerk GT 匹配，以及足底代理点
  穿地项；所有导数按 30 FPS、FP32 和连续有效帧 mask 计算，不把动作压向静止。
- 新增 ground sidecar：从完整 GT 序列的低速足底候选估计地面，记录 source SHA、FPS、
  帧数和有效性；估计不可信时关闭该样本监督，不伪造 `Y=0`。
- 修复短序列 padding mask，一至三阶差分分别要求连续 2/3/4 帧真实有效。
- 训练采样改为按去重音乐时长开方并投影到每集 5%–50%，实现确定性 DDP 分片、
  `set_epoch` 和恢复复现。
- 修复 AIST++ ground sidecar builder 的 canonical pose shape 分支，使现有 annotation
  能正确构建和验证。

## 2026-08-18：[feature/bumi-music-only] BUMI legacy 动作自动质量筛选

提交：`a388d34`。

- 新增对 NumPy 2 legacy pickle 兼容的只读解析器，严格检查 root quaternion、21 关节
  顺序、帧数、FPS、finite 和源资产指纹。
- 自动统计 joint/root 的速度、加速度、jerk、root 高度/倾斜、持续低姿态和碎片化贴地
  风格，按版本化阈值输出 PASS/REJECT/REVIEW 及逐条拒绝原因。
- 全量 7,286 条结果为 PASS 6,610、REJECT 287、REVIEW 389；正式训练设置
  `include_review=false`，只接收 PASS，不需要人工二次决定。
- PASS 数据通过硬链接安全物化，原始动作不改写；每条记录保存 source SHA、质量配置
  SHA、MJCF SHA、指标和判定，支持恢复、审计及重复运行。
- 新增 legacy BUMI 离线渲染工具、质量报告文档和合成回归测试；筛选只判断数据质量，
  不把 GMT 动力学可跟踪性视为已经验证。

## 2026-08-18：physics_v2 损失配置

提交：`ee88ad7`。

- 新增 `gem_smpl_music_only_4set_physics_v2`，在不覆盖 physics_v1 的前提下调整新增时序
  项权重和训练长度。
- 增加 100k/150k step 衰减 scheduler，并修复 DataModule/训练入口对更大 batch 和
  `samples_per_epoch` 的参数传递。
- 增加配置和 loss 权重回归测试；physics_v1 与原四集实验仍可独立复现。

## 2026-08-18：GENMO→GMR→GMT `trajectory_v1` 完整链路

提交：`5fda7b0`。

- 建立 SMPL 完整动作→GMR→BUMI `qpos[28]`→50 Hz `trajectory_v1`，不再把单帧复制成
  伪历史或伪未来。
- `trajectory_v1` 每次严格发送过去 10、当前 1、未来 99 共 110 帧；GMT 继续读取真实
  过去 10、当前和未来 10，不修改其既有 observation 合约。
- root translation/joints 线性插值，quaternion 保持符号连续并使用 SLERP；root 线速度
  转 body frame，角速度用 SO(3)，关节速度用中心差分。
- 增加 GMR capture、离线 SMPL-X→BUMI 导出、Redis 序列推进/ACK 检查、trajectory
  单元测试和完整运行文档。

## 2026-08-18：TensorRT 滑窗与常驻机器人部署

提交：`ae14468`。

- 新增固定 120 帧 TensorRT denoiser：默认 FP16、20-step DDIM、CFG 2.5，engine 缓存
  绑定 ONNX/checkpoint/TensorRT/CUDA/GPU/shape；实物模式不静默回退 PyTorch。
- 后续窗口使用 30 帧硬 inpainting：每个 DDIM timestep 都写回对应
  `q_sample(known_x0)`，最终精确覆盖 overlap，只提交新增 90 帧，不在线性空间混合旋转。
- 新增独立安全桥和常驻交互终端。安全桥拥有 GMR、GMT、音乐、合成站姿、急停与
  50 Hz 时钟；终端常驻 engine，支持音乐路径、`stand`、`status`、`quit`、`shutdown`。
- 定义带 request/revision/chunk/绝对帧号/CRC 的 `robot_stream_v1`；迟到旧 revision、
  乱序、CRC 错误或 heartbeat 超时会取消当前动作并返回合成站姿。
- 增加同步 BUMI qpos MuJoCo viewer，用于和 GMT/Gazebo 实际跟踪结果实时比较；关闭
  viewer 不终止安全桥。
- 增加 ONNX→TensorRT 构建、PyTorch/ONNX/TRT parity、滑窗、inpainting、流式 root、
  协议和安全状态测试；同步 GMR 一输入一输出仍需 GMR-CPP 配套服务。

## 2026-08-18：[feature/bumi-music-only] 正式 BUMI 数据、资产、采样和随机初始化训练配置

提交：`92b2bb2`、`fc01e4a`、`9e449b4`、`12266c6`、`3bcc030`。

- 绑定生产 BUMI3 MJCF `482138…`，提交由其导出的 Torch kinematics 和真实 ankle-roll
  mesh sole proxy 版本配置；训练不会猜测关节名或混用其他 BUMI 资产。
- 完成正式转换器：只读取质量报告中的 PASS，重新核对 source/quality/MJCF SHA，把
  legacy root `xyzw` 转为连续 `wxyz`，按完整关节名映射到 MuJoCo qpos 顺序，并保持
  原始 root XYZ 和原人体 sample basename。
- 转换器继承原 train/val/test split，逐条验证 qpos、EDGE35 和 manifest 等长，输出
  motion、music、audio、manifest、dataset_info 和报告；staging 全部通过后才原子发布。
- 正式结果严格为 6,610 条，split `5537/547/526`，引用 2,548 个唯一 EDGE35；train-only
  93D stats 与四套 dataset fingerprint 绑定，数据或统计量变化会在建 loader 前失败。
- 实现四层去重分层采样：数据集→音乐组→舞者/编舞/take/role→120 帧窗口；概率约为
  AIST++ 11.3993%、AIOZ 50%、FineDance 33.6007%、CoMPAS3D 5%。
- 新增 `physical_v1` BUMI 损失：四组 normalized representation、root/qpos/FK、
  63D/FK 一致性、关节速度/加速度、软关节限位和 root 高度；辅助项前 10k step warmup。
  因数据地面是 `legacy_body_origin_min_zero`，contact、sliding、penetration 和 contact
  head 明确关闭。
- 新增 `gem_bumi_music_only_4set_random_v1`：随机初始化、AdamW `2e-4`、8 GPU、每卡
  batch 128、500k step、300k/450k 衰减、每 10k step 保存，不加载 SMPL checkpoint。
- 修复 DDP 初始化前 rank/world-size 被错误固定为单进程的问题；sampler 在迭代时动态
  解析分布式上下文并按全局 draw index 跨 rank 无重复分片。同步修复进度条 epoch 总步数
  和正式 Demo 的生成接口。

## 2026-08-19：30→50 Hz 与 GMT frame 真正增量构建

提交：`dbb30f1`。

- 定位长音乐中途断开原因：每新增 90 帧，旧实现会在 50 Hz 发布锁内重采样整段 qpos
  并重建全部 trajectory；序列越长持锁越久，最终 sequence 停止推进并触发 GMT timeout。
- 30→50 Hz 改为固定全局时间网格上的增量插值，只保留上一 30 Hz 边界帧，对新 suffix
  计算线性插值和 quaternion SLERP，已经发布的历史不再重算。
- GMT frame 仅重算旧尾帧和新 suffix 所需的最多两个 qpos halo，保证中心差分速度与
  离线整段算法一致。
- 每次 append 生成只读数组快照，发布线程读取不可变引用；全量重采样、速度重建和大块
  分配不再进入发布锁。
- 增加增量/离线等价、跨 chunk quaternion 符号、边界速度、快照不可变和长序列回归
  测试。

## 2026-08-19：[feature/bumi-music-only] 服务器 2 正式训练验收与运行记录

提交：`c061caa`（训练代码沿用此前提交，本提交主要补充验收与运行文档）。

- 核对本地和服务器 2 的分支/commit、正式 BUMI 数据、EDGE35、MJCF、kinematics、
  train-only stats fingerprint、CUDA/PyTorch 和 8×RTX 6000D 环境。
- 正式数据门槛全部通过：动作 6,610、split `5537/547/526`、2,548 个唯一 EDGE35、
  动作与音乐逐条等长、MJCF 与数据 metadata 同源。
- BUMI 专项测试为 `37 passed`；当时全仓结果为 `499 passed, 1 skipped, 3 subtests
  passed`。
- 服务器 2 已启动 8 卡随机初始化 `version_0` 正式训练，目标 500k step；记录快照时约
  96.7k step，训练 loss 从 Epoch 0 的 3.02 降到约 0.24–0.27，无 NaN/Inf，并正常保存
  `s010000` 至 `s090000` checkpoint。
- 文档明确区分“数值稳定”和“模型收敛”：是否收敛仍需四集 validation、固定音乐生成、
  运动学指标和 GMT/实物验证，不能只凭训练 loss 判断。

## 2026-08-19：[feature/bumi-music-only] BUMI checkpoint Demo、ONNX 与完整 DDIM parity

提交：`ef47bb5`。

- 完善 BUMI checkpoint Demo，支持 WAV 或 EDGE35、固定 seed/CFG/DDIM、canonical/world
  root、可选配对 GT 指标、MJCF 视频渲染和保留原音乐。
- Demo 严格拒绝旧 SMPL 151D checkpoint、非 35D music input、camera/static head 或
  错误机器人资产；输出保存 normalized 93D、canonical/world qpos28、音乐特征、
  checkpoint/kinematics/stats SHA 和运动学报告。
- 新增 BUMI ONNX 固定边界：输入 noisy motion `[1,120,93]`、timestep、EDGE35、length、
  guidance scale；图内把 conditional/unconditional 合成 batch 2，一次 Transformer
  forward 完成 CFG，只输出 `pred_motion[1,120,93]`。
- 导出 metadata 绑定 ONNX、checkpoint、统计量和运动学资产 SHA；验证工具先做相同输入
  的单步 PyTorch/ONNX Runtime parity，可选使用同一初始噪声和 DDIM scheduler 比较完整
  normalized 93D、qpos28 和 Torch FK。
- 实际核对服务器 `s090000.ckpt`：global step 90,000、输出 93D、音乐输入 35D、无
  camera/static head；本地旧 `s050000.ckpt` 为 SMPL 151D，不能用于 BUMI Demo。
- Ruff、`py_compile`、CLI help、ONNX Runtime CPU 实导出/推理均通过；BUMI suite
  `42 passed`，全仓 `504 passed, 1 skipped, 3 subtests passed`。

## 2026-08-19：[feature/bumi-music-only] 新增方法与框架文档并恢复持续记录机制

- 新增 `docs/BUMI_GENMO_METHOD_CN.md`，以方法而非源码目录为主线，解释任务定义、
  数据形成、qpos28、93D 冗余表示、first-frame canonicalization、EDGE35、`x0`
  diffusion、RoPE Transformer、music CFG、DDIM、损失、去重采样、权威 FK 和方法边界。
- 明确 `BumiEndecoder` 是确定性几何/归一化桥梁而非可学习 VAE，`BumiMusicPipeline`
  是训练与推理编排层而非第二个生成网络；93D 后 63 维只作几何辅助监督，最终机器人
  状态始终由前 30D 组合成 qpos28 后重新 FK。
- 在 `docs/bumi_native_music_genmo.md` 顶部增加方法文档入口，区分方法说明与工程、
  数据契约、命令和训练状态说明。
- 补录本文档从 8 月 4 日后遗漏的两条分支历史；新增根目录 `AGENTS.md`，把每次实质
  修改必须追加 `记录文本.md` 设为仓库级完成条件，避免以后再次只更新代码或 commit
  message 而漏写持续记录。
- 本轮只修改文档和仓库协作约定；`git diff --check`、文档相对链接和基本结构检查通过。

## 2026-08-19：补齐当前 `main` 分支持续记录

- 逐项审计当前 `main@dbb30f1` 从 `fb52927` 到 `dbb30f1` 的 21 个可达提交，并把此前
  遗漏的 Music-only、四数据集、物理损失、GMR/GMT、TensorRT 和增量发布实现补入本文。
- 明确未把 `feature/bumi-music-only` 的 93D、BUMI FK、正式 BUMI 数据和训练实现写成
  `main` 已有能力；这些内容保留在对应 feature 分支记录中。
- 新增根目录 `AGENTS.md`，把“每次实质修改必须同步追加 `记录文本.md`”设为仓库级
  完成条件，后续不能只更新代码、聊天记录或 commit message。
- 本次仅修改持续记录和协作约定；`git diff --check`、可达提交覆盖和分支归属检查通过。

## 2026-08-20：[feature/bumi-music-only] smooth_q1 实际 50 Hz 轨迹物理预检查

- 修改目标：不再用旧 30 Hz legacy pickle 的结果代替部署轨迹，直接对
  `data/motions_npz_bumi3_smooth_q1` 中 3,163 条七字段 50 Hz NPZ 重新执行可审计的
  运动学/动力学预检查，并保留逐条筛选结果。
- 新增 `configs/bumi/quality_filter_sonic_npz_50hz_v1.yaml`，显式绑定当前 GMR BUMI3
  XML、Isaac-Lab publish-order preset 和 OMG kinematics SHA；动力学阈值沿用 legacy
  v1 的物理单位，连续超限、贴地持续、halo 和安全段帧数按等时长从 30 Hz 换算到
  50 Hz。22-body 契约缺少 virtual torso/hand，躯干改用左右 shoulder body 高度均值，
  手部只诊断规则移除；GMR 0.05 m clearance 下不再错误套用 legacy body-origin=0 门禁。
- 新增 `tools/data/bumi/filter_sonic_npz_motions.py`：严格检查键、float32、50 Hz、形状、
  finite、wxyz 单位四元数、publish-order 关节限位、根高度/倾角、贴地风格和真实
  50 Hz 速度/加速度/jerk；源 NPZ 只读，报告原子写入独立输出目录。报告同时保存
  严格状态、逐条原因和“忽略关节限位”的纯诊断反事实状态，后者不参与正式筛选。
- 新增 `tests/bumi/test_sonic_npz_quality_filter.py`，覆盖严格 NPZ 契约、publish-order
  限位、50 Hz 0.5 秒持续门槛、肩部躯干代理、严重速度峰值以及速度字段诊断不改变
  状态；目标测试为 `6 passed`，Ruff 和 `py_compile` 均通过。
- 全量实际结果保存在 `outputs/bumi_quality_smooth_q1_50hz_v1`：5,978,881 帧、
  33.2160 小时，3,163/3,163 契约通过；严格 PASS 36、REJECT 3,127、REVIEW 0。
  3,127 条均触发生产关节限位，主要集中在四个手臂 roll/elbow 关节；忽略限位的诊断
  视图为 PASS 2,986、REVIEW 59、REJECT 118。输出包含完整 JSONL/CSV、配置快照、
  四份状态路径清单、容差敏感性、逐关节统计及中文 `筛选结果说明.md`，源轨迹未修改。
- 功能边界：该结论是确定性的离线数据预筛选，不声称已经通过 Isaac-Lab 物理
  rollout 或 SONIC 策略跟踪。`body_lin_vel_w` 的 root/body[0] 与中心差分一致，但
  非根 body 存在较大差异；v1 将其作为诊断而非任意设阈值拒绝。结合 GMR 求解器限位
  代码与越界只集中于手臂，平滑 overshoot 是合理推断，但因缺少同版平滑前 50 Hz
  中间轨迹，没有把该推断写成筛选事实。
- 实际验证：全量脚本完成 3,163/3,163，`quality_report.jsonl` 3,163 行、CSV 3,164 行
  （含表头）；四份列表分别为严格 PASS 36、严格 REJECT 3,127、忽略限位 REVIEW 59、
  忽略限位 REJECT 118，计数与汇总完全一致；`git diff --check` 通过。

## 2026-08-20：[feature/bumi-music-only] smooth_q1 auto025 正式训练数据版本与服务器2发布

- 修改目标：把手工 q1 的真实 50 Hz SONIC 轨迹按 0.25 rad 关节越界策略重新筛选，
  只把自动 PASS 作为下一次训练数据；在服务器2重建 30 Hz BUMI qpos28/在线 93D、
  音频与 EDGE35 配对、manifest、train-only 统计量和显式四库训练采样权重，不覆盖旧
  `bumi_music_genmo_v1`，也不自动停止当前作业或启动下一次正式训练。
- 新增独立质量配置 `quality_filter_sonic_npz_50hz_auto025_v1.yaml`，保留 strict v1 的
  其余规则并把 `joint_limit_violation_max` 固定为 0.25 rad。全量结果为 PASS 2,986、
  REVIEW 59、REJECT 118；按库 PASS 为 AIST++ 854、AIOZ 1,947、FineDance 118、
  CoMPAS3D 67，源文件未修改，配置 SHA 为 `1ff1773e…`。
- 新增 `build_bumi_music_dataset_from_sonic_npz.py`：复核 PASS report/源 NPZ/资产 SHA，
  按实际右端点不包含时间网格把 50 Hz 位置/关节线性重采样、连续 wxyz 四元数 SLERP
  到权威 30 Hz 长度，按完整名称从 Isaac-Lab publish order 重排到 GENMO MuJoCo
  order；再用 `482138…` kinematics FK 施加单条常量 root-Z 地面规范化。所有输出先写
  staging，失败不留半成品；AIST 同一基础版/`_armfix` qpos 完全相同，显式保留后者并
  在报告记录替代，而不是重复训练。
- 扩展 BUMI v1 reader：保持旧 `root_z_adjusted=false` 兼容，同时允许 dataset info 与
  motion 一致声明 true；数据版本可绑定 `reader_joint_limit_tolerance_rad`，训练参数不
  一致会在读数据前失败。stats 和 validator CLI 新增相同容差参数；0.25 只用于新版本，
  旧配置默认 0.001 不变。
- 服务器2新增源版本 `bumi_motions_smooth_q1_50hz_auto025_v1`（2,986 NPZ，6.8 GiB）、
  音频版本 `bumi_music_audio_smooth_q1_auto025_v1`（旧文件硬链接并补齐 38 个 WAV）和
  正式版本 `bumi_music_genmo_smooth_q1_auto025_v1`（8.4 GiB 目录视图）。正式唯一动作
  2,985，split `2623/171/191`，总 3,303,925 帧/30.5919 小时，1,669 个唯一 EDGE35；
  AIST/AIOZ/Fine/CoMPAS 分别为 853/1,947/118/67。Fine val 筛选后为 0，保持原 split，
  新实验用其 test 评估。
- 重算 train-only `bumi_93d_stats_train_auto025_v1.json`：24,274 个窗口、2,912,765 个
  统计帧、93D mean/std 全 finite、最小 std 0.03028，SHA 为 `3c9aa73b…`，并绑定四库
  dataset info/train manifest fingerprint。四库 payload、qpos/EDGE35/manifest 对齐及
  motion/EDGE35/WAV SHA 严格扫描通过；真实 DataModule fingerprint 验证通过，首个
  batch qpos `[128,120,28]`、music `[128,120,35]` 全 finite。
- 为 sampler 增加可选 `dataset_sampling_weights`，旧版仍按去重音乐时长平方根工作；
  新入口 `gem_bumi_music_only_4set_smooth_q1_auto025_v1` 使用显式 AIST/AIOZ/Fine/
  CoMPAS `30%/50%/17%/3%`。依据实际 train 去重音乐组 `50/555/98/2` 将 CoMPAS 从
  初稿 8% 下调到 3%，避免两个音乐组被过度重复；训练序列硬校验更新为 2,623，四库
  reader 容差均固定 0.25，Fine 评估切到 test。
- 兼容性/边界：正式 motion `.pt` 的权威状态仍是 30 Hz qpos28，GENMO 按固定
  kinematics/codec 在线确定性编码为 93D，不新增可能与 qpos 漂移的第二份轨迹缓存；
  source GMR XML `041c81…` 与 feature kinematics 来源 MJCF `482138…` 分开记录。0.25
  rad PASS 仍只是离线预筛选，不声明 SONIC/GMT 动力学可跟踪或实物安全。
- 实际验证：本地 Ruff 检查通过，改动文件格式检查通过；BUMI 全套为 `53 passed,
  1 skipped`。服务器2正式转换报告 SHA `f7c63170…`、stats SHA `3c9aa73b…`，四份严格
  validation 报告均保存；最终 sampler 真实实例化报告 explicit weights 精确为
  `0.30/0.50/0.17/0.03`。数据根另存实际训练 YAML 和 `data_version_release.json`，后者
  SHA 为 `283e2715…` 并汇总 conversion/stats/validation/config 指纹；没有启动新训练
  进程。

## 2026-08-20：按 BUMI 人工评分备份高质量人体动作

分支：`main@3f01222` 工作树修改，未提交。

- 修改目标：读取 `data/motions_npz_bumi3_smooth_q1/rate` 四张评分表，在服务器1从完整
  `music_only_4set_v1` 人体审阅包筛出所有 `score=1` 的原始人体 SMPL-X 动作，建立独立
  备用版本供后续微调数据构建，不修改全量源包和现有训练数据。
- 主要实现：新增 `select_human_motions_from_ratings.py`。工具严格校验 CSV 列、评分值、
  重复名称、四表对源 master 的完整覆盖，以及每条 NPZ 的 30 FPS、`pose[T,66]`、
  `transl[T,3]`、`betas[T,10]`、finite、内部 dataset/sample ID、Y-up 米制坐标和源
  SHA256；输出使用临时目录完成后原子发布，同盘动作使用硬链接，跨盘才复制。
- 别名边界：评分表共有 3,163 个 `score=1` 项；AIST++ 的
  `gBR_sBM_cAll_d04_mBR0_ch02_armfix.npz` 属于机器人重定向阶段别名，与基础人体动作
  评分一致，因此显式折叠并记录到 `index/aliases.jsonl`，最终得到 3,162 条唯一人体动作，
  不把同一人体轨迹重复用于后训练。
- 数据产物：服务器1发布到
  `/data0/user/liwei/datasets/music_dance_review/music_only_4set_manual_q1_human_v1`。AIST++、
  AIOZ-GDANCE、FineDance、CoMPAS3D 分别为 963、1,978、149、72 条；合计 3,589,146
  帧、30 FPS、33.2328 小时，保留原 split 为 train/val/test = 2,792/174/196。
- 兼容性/功能边界：备用包只物化人体 NPZ、完整评分表、选择索引和来源 metadata，不含
  WAV、EDGE35 或可直接训练的四库 manifest；`selected.jsonl` 保留 `music_key`、原 split
  与 `source_manifest_row`，以后可据此重建音乐闭包和正式后训练统计量。全量审阅包、原始
  数据和正在运行的训练均未修改。
- 实际验证：本地 Ruff 通过，`tests/test_music_dance_curation.py` 为 7 passed；服务器1
  预演与正式发布均通过。3,162 条动作全部逐文件 SHA256 正确（`sha_ok=3162,
  sha_bad=0`），源/目标 inode 全部一致（`inode_bad=0`），输出 report/index 哈希已记录，
  `selection_report.json` SHA256 为
  `858f57ffddee41463268f954bbd557f0d8bdc7a6644cc3099458353d2879fd1d`。

## 2026-08-21：统一评测工具与服务器代码同步约定

分支：`main`，合并 BUMI feature 分支前的本地集成提交。

- 修改目标：收回此前只存在于服务器1工作树的 checkpoint 顺序评测工具，并明确本地、
  GitHub 与两台训练服务器之间唯一且可追溯的代码同步流程，避免同一路径在多处各自修改。
- 主要实现：新增 `run_selected_checkpoint_eval.sh`，以同一口径顺序评测指定 SMPL GENMO
  checkpoint 的 EMDB_1、EMDB_2、3DPW 和 RICH 指标；新增
  `summarize_selected_checkpoint_eval.py`，完成指标完整性检查、局部/全局/稳定性归一化排名、
  CSV/JSON/文本报告和最佳 checkpoint 相对软链接。两个新文件均补充了中文用途、输入输出和
  功能边界说明，Python 类型标注同步到当前版本规范。
- 同步约定：扩充 `AGENTS.md`，规定 GitHub 为代码、配置和文档的统一来源；常规修改先在
  本地集成、测试、记录、提交和推送，服务器只用 `git pull --ff-only` 快进同步。服务器
  紧急修复必须及时分支、提交、推送后再回到本地合并。`.gitignore` 新增 `logs/*.log`，
  防止训练运行日志再次污染服务器工作树；已纳入版本管理的历史文件不受影响。
- 兼容性/功能边界：评测脚本默认复用成功结果，`FORCE=1` 才会覆盖重跑；它只管理评测
  输出，不启动或停止训练。同步规则不把数据集、checkpoint、W&B 文件和其他运行产物
  纳入 Git，也不改变当前训练进程已加载的代码。
- 实际验证：Python 汇总器通过 Ruff 检查与格式检查，Shell 评测器通过 `bash -n`；
  评测日志解析、科学计数法读取和并列平均排名冒烟测试通过，`git diff --check` 通过。

## 2026-08-21：合并 SMPL main 与 BUMI-native 开发线

分支：本地 `main` 合并 `origin/feature/bumi-music-only@8230c98`，合并完成后同时发布到
远端 `main` 与 `feature/bumi-music-only`。

- 修改目标：结束 SMPL GENMO 与 BUMI GENMO 长期分支开发状态，把人体 151D、
  GENMO→GMR→GMT/TensorRT 部署链路和机器人 93D、数据筛选/转换、训练、Demo/ONNX
  能力统一到同一可测试版本，作为两台服务器后续同步基线。
- 主要实现：保留 BUMI feature 的生产运动学配置、50 Hz 自动物理筛选、0.25 rad 数据
  版本配置、30 Hz qpos28/在线 93D 数据构建、四库显式采样、BUMI loss/metrics、
  checkpoint adapter、Demo、ONNX 导出与 parity 验证；同时保留 main 的 SMPL 默认模型、
  物理时序损失、增量 30→50 Hz trajectory 和常驻部署实现。通用 GEM/Pipeline 以
  `motion_backend` 和 endecoder motion dimension 区分两套后端，未配置时仍默认 SMPL。
- 冲突处理：`CHANGELOG.md` 同时保留 BUMI Unreleased 项和 main 的 2026-08-18 发布项；
  `AGENTS.md` 保留并强化统一同步约定；`记录文本.md` 不选边覆盖，而是按日期插入 feature
  的八段 BUMI 历史并保留 main 既有记录。`gem/gem.py` 与
  `gem/pipeline/gem_pipeline.py` 由 Git 无冲突合并，另逐项核对其后端选择与维度逻辑。
- 兼容性/功能边界：现有 SMPL 配置和 checkpoint 继续走 `motion_backend=smpl`；BUMI
  实验必须显式选择机器人后端及对应 93D 配置。训练数据、checkpoint、日志和服务器运行
  产物没有被纳入 Git；合并和后续快进拉取不停止、重启或改变已运行进程内加载的模块。
- 实际验证：所有变更 Python 文件 Ruff 检查通过，`git diff --cached --check` 通过；
  BUMI、进度条和人体筛选专项为 `61 passed, 1 skipped`，全仓测试为 `566 passed,
  1 skipped, 20 warnings, 3 subtests passed`。警告均为既有 Torch AMP 弃用提示、短音频
  librosa FFT 提示及一条测试字符串转义弃用提示，无新增失败。

## 2026-08-21：清理服务器测试产物并建立自动清理约定

分支：`chore/test-artifact-cleanup-policy`，策略提交将同步到当前远端 main 与 BUMI
feature；两条业务分支是否恢复为独立代码线另行决定，不在本次做非快进改写。

- 修改目标：清理由此前代码验证产生、但长期残留在两台训练服务器上的 smoke、one-step
  preflight、临时数据集、临时评测、环境审计和缓存，只保留正式训练实验、生产数据、
  checkpoint 与必要训练记录；同时把测试结束自动清理设为仓库级强制约定。
- 主要实现：服务器1按精确路径删除 4 个 smoke 训练目录、5 个 smoke 转换数据目录、
  2 个 smoke 审阅/筛选目录、旧模型下的 `eval_selected` 和 2 份 preflight JSON，合计约
  521 MB；服务器2删除 3 个 smoke、4 个 one-step preflight、1 份 smoke 代码副本、
  1 份环境审计和 5 个 smoke 数据目录，合计约 142 MB。两台服务器仓库内的 pytest、
  Ruff 和 Python 缓存一并清除。
- 协作规则：扩充 `AGENTS.md`，要求测试前确定独立输出路径，禁止把验证结果写进正式
  `version_*`、`checkpoints/` 或生产数据；测试无论成功失败都要在同一任务内删除临时
  产物和缓存并检查残留。仓库 `tests/` 源代码明确不属于清理对象；需跨任务保留诊断产物
  时必须记录绝对路径、原因和清理条件。
- 兼容性/功能边界：本次未删除正式训练目录、正式数据集、checkpoint、训练日志或生产
  BUMI 资产证明。服务器1 SMPL physics_v2 与服务器2 BUMI random_v1 进程均未停止或
  重启；删除的临时目录没有回收站副本。
- 实际验证：清理后两台服务器指定输出/数据根均未再发现名称含 smoke、preflight 或
  pytest 的生成产物；服务器1正式 checkpoint 保留到 `s135000.ckpt`，服务器2保留到
  `s420000.ckpt`，两个 8 GPU 训练主进程仍在运行。

## 2026-08-21：服务器1切换为人工高质量 SMPL 轨迹微调

分支：`main`；服务器1代码在操作开始时为 `0503880`，本次不修改模型实现或既有训练配置，
只新增可审计的数据发布、停止旧正式作业并以独立实验续接最新模型权重。

- 旧训练进度：`gem_smpl_music_only_4set_physics_v2/version_0` 在停止前的 TensorBoard
  最后记录为 step 158499、epoch 9323、学习率 `7.5e-5`、epoch loss `5.25215435`、
  physics total loss `0.0124859838`，目标为 170000 step，完成约 93.2%。最近完整落盘
  checkpoint 为 `s155000.ckpt` 和当时的 `last.ckpt`；前者大小 2,570,404,846 bytes，
  metadata 为 global step 155000、epoch 9117、316 个 state-dict key，并包含一份 optimizer
  与 scheduler 状态。
- 停止边界：向 `tmux:genmo_physics_v2` 正常发送 Ctrl-C，8 个 DDP rank 退出后又识别出
  18 个自 8 月 18 日启动、父进程已经变成 PID 1 的孤儿训练/DataLoader 进程；这些进程的
  完整命令均属于同一 physics_v2 实验，并仍占用 GPU 1、2、7 各约 53 GB，因此按精确 PID
  发送 SIGTERM。最终旧实验匹配进程为 0，8 张 GPU 均降至 3～4 MiB 基础占用。
- 高质量数据发布：依据已验证的
  `/data0/user/liwei/datasets/music_dance_review/music_only_4set_manual_q1_human_v1`
  选择索引，把 BUMI 人工评分 `score=1` 的 3,162 条唯一人体轨迹标为 keep，其余 4,124 条
  仅从新发布中排除。完整 decisions 保存为
  `reports/train_decisions_score1.csv`，SHA256 为
  `035541acae0791db7003ca8e1e1cffd5d967e8408ffc5661179fca721ba587f8`；新训练根原子发布到
  `/data0/user/liwei/datasets/music_dance_curated/music_only_4set_manual_q1_human_v1`。
  AIST++、AIOZ-GDANCE、FineDance、CoMPAS3D 分别保留 963、1,978、149、72 条，
  train/val/test 为 2,792/174/196，总计 3,589,146 帧、33.232833 小时；有效音乐闭包为
  1,814 个 EDGE35，1,049 个零引用音乐进入独立 quarantine。源审阅包、转换四库和原始
  音频均未改写。
- 续接语义：新任务使用 `gem_smpl_music_only_4set_curated` 的标准 music-only diffusion
  与几何监督，并从 physics_v2 的不可变 `s155000.ckpt` 加载全部模型权重；由于训练数据与
  目标实验已经切换，未恢复旧 optimizer/scheduler/global step，而是以新 optimizer 做真正
  的高质量数据微调，避免把接近旧任务末期的调度状态误带入新数据。微调参数为 8 卡、每卡
  batch 128、全局 batch 1024、AdamW `2e-5`、50,000 step、16-bit mixed precision、
  W&B 关闭；输出独立写入
  `/data0/user/liwei/GENMO_outputs/gem_smpl_music_only_4set_manual_q1_finetune_v1/version_0`，
  正式日志为同级实验根的 `formal_train.log`，tmux 会话为 `genmo_smpl_hq_finetune`。
- 功能边界：该微调入口继承 curated 基线，不继续计算 physics_v2 的显式 physics loss；
  physics_v2 已学权重作为初始化保留，但新阶段专注人工高质量人体/音乐对。旧 physics_v2
  输出和 checkpoint 均保留，未覆盖也未删除。训练数据发布使用硬链接/独立索引与隔离区，
  删除新发布不会删除源数据；人工高质量与动力学安全并不等价，后续仍需 checkpoint 评测。
- 实际验证：curation dry-run 与正式原子发布均为 `final_pass=true`；严格全量校验为 0 error，
  DataLoader smoke 得到 music `[4,120,35]`、target `[4,120,151]`、mask 正确且 target 全部
  finite。Hydra 解析核对四库根、diffusion-only、`in_attr=[encoded_music]`、checkpoint、
  batch、学习率和输出目录均为预期。新训练成功加载 `s155000`，8 个 NCCL rank 全部注册，
  前两个 epoch loss 均约 5.52，每 epoch 23 step，稳定约 2.0 step/s，显存约 23.5 GB/GPU；
  未发现 traceback、OOM 或非有限值。仅出现项目既有 AMP 弃用与 scheduler 调用顺序警告。
  本次配置解析临时文件已删除，仓库源码 `__pycache__`、`.pytest_cache`、`.ruff_cache` 已清理
  并完成残留检查；生产数据报告、正式日志和训练输出按用途保留。

## 2026-08-21：按两天训练窗口将高质量 SMPL 微调延长至 300k

分支：`main`；接续上一节已经验证的人工高质量四库和 `s155000` 权重初始化，不修改代码、
数据内容或旧 physics_v2 checkpoint，仅调整正式长任务的运行参数与隔离输出。

- 调整原因：原 50,000 step 微调按服务器1实测约 2 step/s 只需约 7.3 小时，未充分利用
  用户给出的两天训练窗口。停止时旧短任务最后 TensorBoard 记录为 step 759、epoch 33、
  学习率 `2e-5`、step loss `5.5007205`、epoch loss `5.4687376`；尚未到首个 10,000 step
  checkpoint，因此没有可用于完整训练状态 resume 的新 checkpoint。该短任务
  `gem_smpl_music_only_4set_manual_q1_finetune_v1/version_0` 及日志保留，不删除或伪装成
  长任务结果。
- 长任务参数：重新从不可变 physics_v2 `s155000.ckpt` 加载模型权重，在同一高质量数据根
  上启动 300,000 step 权重续接微调；保持 8 卡、每卡 batch 128、全局 batch 1024、
  AdamW `2e-5`、16-bit mixed precision 和 W&B 关闭。step 级 MultiStepLR 在 70,000 与
  100,000 step 各乘 0.5，后续学习率为 `5e-6`；checkpoint 仍每 10,000 step 保存一次。
  按稳定单 batch 约 0.43～0.51 秒估算约 43～49 小时，首轮包含初始化的进度 ETA 为
  2 天 1 小时，后续会随稳定吞吐收敛，符合“两天左右并尽量拉长”的目标。
- 隔离与查看：新实验名为 `gem_smpl_music_only_4set_manual_q1_finetune_300k_v1`，输出写入
  `/data0/user/liwei/GENMO_outputs/gem_smpl_music_only_4set_manual_q1_finetune_300k_v1/version_0`，
  正式日志位于实验根 `formal_train.log`，tmux 会话改为 `genmo_smpl_hq_300k`。旧短会话
  在确认所有匹配训练进程归零、8 卡显存释放后删除；旧输出目录保持可追溯。
- 实际验证：300k Hydra 解析严格核对 max steps、输出名、四库数据根、8 卡、batch、学习率、
  两个 scheduler milestone、`s155000`、`ckpt_path=null` 和 `resume_mode=null` 均正确；一次
  错误层级断言在创建 tmux 和输出前由 `set -e` 安全终止，修正为真实
  `scheduler.scheduler.milestones` 后重新校验通过。新任务的 8 个 NCCL rank 全部启动，
  前 3 个 epoch loss 约 5.52，随后 epoch 4 loss 为 5.437，TensorBoard step 99 的 batch
  时间约 0.433 秒；显存约 23.5 GB/GPU，GPU 利用率约 73%～94%，错误扫描无 traceback、
  OOM 或非有限值。配置解析临时文件、源码 `__pycache__`、`.pytest_cache`、`.ruff_cache`
  已清理并完成残留检查。

## 2026-08-24：导出服务器 1 高质量 SMPL 模型 ONNX 并完成四库完整音乐网页验收

分支：`main`；模型来源为服务器 1 `ZP-NC578` 的 `main@aa76e07` 正式 300k 微调实验，
本次不使用服务器 2、BUMI-native checkpoint、BUMI qpos 或机器人数据。

- 口径纠正与数据边界：服务器 1 的训练 Hydra 快照和正式高质量根均只包含 AIST++、
  AIOZ-GDANCE、FineDance、CoMPAS3D 四个人工评分为 1 的 SMPLX 数据集，不存在可用于
  该模型验收的“第五套高质量 SMPLX 数据集”。任务中途曾按此前五库 BUMI 上下文错误地
  把服务器 2 自建 BUMI 作为第五库；发现后立即停止进程，删除该库的本地暂存和相关契约，
  最终 `selection.json`、`summary.json`、40 条结果和网页均复核不含 BUMI/服务器 2 字段。
- ONNX 导出：使用本地回传的服务器 1 `s300000.ckpt`（2,570,404,846 bytes，global step
  300000，SHA256 `c2aaff2b48d00602a4bd433056721b263b7bb2d9176c9390ba543a51d78ec529`）
  与 `gem_smpl_music_only_4set_curated` 配置，导出 opset 18 模型到
  `outputs/onnx/smpl_music/s300000_hq4_finetune/music_only_denoiser.onnx`。文件大小
  874,074,147 bytes，SHA256 为
  `20f668bc3303e588b7c456a09d6815b1f0a369d0cc33b469e6a508f335b69712`；输入为动态 batch、
  固定 120 帧的 noisy motion/timestep/EDGE35/length/CFG scale，输出 151D motion、camera
  和 static logits。边界仍是一个带 CFG 的神经去噪步，DDIM 调度、长序列拼接、后处理和
  SMPL 解码不封入 ONNX。
- ONNX 数值验收：真实 AIST++ EDGE35 条件下使用 CUDAExecutionProvider，单步三个输出
  均 allclose；pred motion/camera/static logits 最大绝对误差分别为 `0.00132978`、
  `0.00008618`、`0.01208687`。相同初始噪声的 50 步 DDIM 最终 151D 动作最大绝对误差
  `0.00153041`、RMSE `0.00018234`，在 `atol=rtol=0.02` 下通过。报告归档于
  `outputs/smpl_hq4_s300000_full_20260824/onnx_validation/validation_report.json`，SHA256
  为 `12f5785e9e8e937730e4d3be58aa1e6709ecfcef3ba8a54e81a19fe50b4e96ed`。
- 完整音乐生成与渲染：新增 `build_smpl_hq4_full_comparison.py`，每库固定选择 10 个高质量
  音乐/原始动作，种子为 `20260824..20260863`；生成使用 checkpoint 的完整 PyTorch
  DDIM 50 步、CFG 2.5、600 帧窗口与 120 帧重叠，不截断音乐。`demo_music_only.py` 新增
  `--exp` 以按 checkpoint 的实际 Hydra 实验重建模型，并修复长序列规划在已有尾窗覆盖到
  结尾后仍可能附加冗余短窗的问题。原始和生成 SMPL 均分块执行 body model，仅保留 22
  关节并逐帧流式送入 ffmpeg，避免数分钟序列缓存完整 vertices/RGB；网页保留原始片段真实
  长度，AIST++ 短原始动作不循环、不拉伸。
- 正式结果：输出根为 `outputs/smpl_hq4_s300000_full_20260824`，清单/汇总 SHA256 分别为
  `e011cf9b04a273882b96b880c01ea39e1d8fe9aa47e73c9e634a54876632cfe4` 与
  `b6e21efbab1855919d784a0391a782cad04dfaf2daf25c1ec6953ccead1fcd6d`。40 条全部完成，
  40 条生成动作的根轨迹/朝向物理粗检全部通过；80 个最终 MP4 全部为单 H.264 + 单 AAC、
  30 FPS，并逐文件通过目标时长 `±0.15s` 校验，媒体总大小 212,640,088 bytes。四库所选
  完整音乐时长分别为 AIST++ `394.246979s`、AIOZ `410.343040s`、FineDance
  `1558.960245s`、CoMPAS3D `1507.699999s`，总计 `3871.250263s`；生成动作总时长
  `3872.4s`，取整差来自 EDGE35 的 30 FPS 帧边界。
- 网页与实际验证：网页由本机 `0.0.0.0:8020` 只读提供，局域网
  `http://10.120.0.195:8020/` 和 summary 均实测 HTTP 200；页面支持四库筛选、原始/生成
  双视频、同长度同步播放和真实时长提示。新增四库清单/投影专项测试并扩展长窗回归测试，
  最终 Ruff 通过、专项为 `9 passed`、`git diff --check` 通过；测试临时目录及 pytest、
  Ruff、Python 缓存已按仓库约定清理，不删除生产网页、动作、视频和 ONNX 验收报告。

## 2026-08-24：纠正骨架展示并将四库 40 条验收全部重渲染为 SMPL-X 完整人体

分支：`main`；本节是对上一节“仅保留 22 关节并流式渲染”展示口径的后续纠正，不重写
既有历史。用户指出验收对象应当是带表面的 SMPL 人体而不是骨架后，重新审查原项目渲染
代码，确认旧的正确展示链路实际使用 Open3D 构建 SMPL-X 三角网格，并未使用 MuJoCo；
本次不重新运行 diffusion、不改变服务器 1 模型输出，只替换原始/生成两侧的可视化层。

- 主要实现：`build_smpl_hq4_full_comparison.py` 改为通过 `make_smplx("supermotion")`
  解算 SMPL-X neutral 的 10,475 个顶点和固定 20,908 个三角面，使用同一紫色材质、棋盘
  地面与静态相机逐帧渲染完整人体。长序列按块在 GPU 上计算顶点及基于面邻接累加的顶点
  法线，写入 float32 memmap 后流式送给 Open3D/ffmpeg，避免在内存中同时保存整段人体
  网格和 RGB；H.264 静音候选通过校验后再复用原音轨合成 AAC 最终视频。
- 并行与故障边界：增加 `--rerender-mesh`、`--only-web-id`、分片参数和
  `--worker-no-manifest`。分片 worker 只写每条独立的 `*_mesh_ready.json`，不竞争全局
  `selection.json`、`completed_items.json`、`summary.json` 或网页；每条视频均在独立 `_work`
  目录完成并经 ffprobe 校验后原子替换，成功后清理顶点、法线和静音候选。实测 6 路 EGL
  并发会降低总吞吐，正式过程据实调整为 3 路，最后按分片自然收敛到 2 路；中断的未完成
  候选全部清理，没有覆盖正式视频。37 个并行 sidecar 与 3 个早期已完成条目最终合并为
  40 条统一清单。
- 最终数据：40/40 条 `render_style` 均为
  `smplx_neutral_full_mesh_open3d_streaming_v1`，四库各 10 条；80 个原始/生成 MP4 总大小
  176,400,808 bytes、总媒体时长 7,440.660 秒。按原始/生成合计，FineDance 为
  3,118.506 秒、80,016,301 bytes，CoMPAS3D 为 3,015.741 秒、57,382,510 bytes，
  AIOZ-GDance 为 820.807 秒、24,309,301 bytes，AIST++ 为 485.606 秒、14,692,696 bytes。
  AIST++ 原始短片仍保持真实长度，生成侧保持完整音乐长度，没有循环或拉伸原始动作。
- 清单与网页：最终 `selection.json`、`summary.json`、`completed_items.json`、`index.html`
  的 SHA256 分别为 `33b8a41678c342903ba8c74ee2fd747e7b7e427f18eb8283bd573dcd0c3fb448`、
  `81ddcff36aeaef2bfd98d23ceeab32388ffc06b55c3cd6db461964c4a0faa4c6`、
  `dd2a325fa06fbd2764010a44e6d4e0f7c5b01e8092846ff7496392b65cf09793`、
  `6ea4f240bd544d7193893669b67ae98a60eb9eba3a7f7d781c272be5b5f2bbb4`。网页明确说明原始与
  生成均为 Open3D 渲染的 SMPL-X neutral 完整表面，MuJoCo 未参与；本机
  `0.0.0.0:8020` 服务继续提供 `http://10.120.0.195:8020/`。
- 实际验证：逐个 ffprobe 80 个 MP4，全部恰好包含 H.264 视频与 AAC 音频、30 FPS，记录
  帧数与目标帧数完全一致，容器时长误差均不超过 0.12 秒；网页首页和抽样视频均返回
  HTTP 200。人工抽查 FineDance、CoMPAS3D、AIOZ-GDance、AIST++ 各一组原始/生成代表帧，
  8 张均为完整紫色人体表面与棋盘地面，不是关节点骨架。Python 编译、Ruff、
  `git diff --check` 均通过，定向 pytest 为 `9 passed`；抽帧、pytest 临时目录、`_work`、
  `.pytest_cache`、`.ruff_cache` 和源码 `__pycache__` 已清理并完成残留检查，生产网页、
  80 个视频、SMPL 参数、ONNX 及其验收报告按用途保留。

## 2026-08-25：放宽音乐机器人安全桥的无音乐双臂站姿

分支：`main`。

- 修改目标：解决 `GENMO + GMR + GMT` 安全桥正常运行但未播放音乐时，合成站姿双臂过于
  贴近身体的问题；只调整空闲目标，不改变舞蹈生成或重定向配置。
- 主要实现：`synthetic_idle_motion` 新增受限的 `arm_open_degrees` 参数，在原双臂下垂
  SMPL-X 姿态上对称减小肩部 Z 轴旋转，使手臂向身体外侧打开；不传参数时仍保持原有
  贴身行为，避免影响其他调用方。`demo_music_robot_bridge.py` 新增
  `--idle-arm-open-degrees`，默认使用 `10°`，允许在 `0..45°` 内通过启动参数继续仿真微调，
  原启动命令无需增加参数。文档同步说明该参数的作用域。
- 兼容性/功能边界：修改只影响安全桥启动、无音乐、主动 `stand`、音乐结束及故障回退时
  使用的合成空闲 SMPL-X 目标；实际 BUMI 21 关节角仍由既有 GMR IK 解算。GENMO 模型
  输出、`smplx_to_bumi3_auto.json`、机器人 XML、GMT policy、Redis 协议和其他默认调用的
  空闲姿态均未改变；人体肩部打开 `10°` 不保证每个机器人关节恰好变化 `10°`。
- 实际验证：新增双臂对称角度、参数范围、Bridge 默认值和非法值拒绝测试；
  `tests/test_motion_streamer.py` 与 `tests/test_gmt_trajectory.py` 合计 `34 passed`，Ruff 和
  `git diff --check` 均通过。测试临时目录已删除，仓库源码缓存和检查器缓存按约定清理。

## 2026-08-25：服务器1人工高质量 SMPL 数据强化 physics_v3 微调

分支：`main`；配置曾在同基线的临时分支完成隔离验证，正式集成、服务器同步与训练统一以
`main` 为代码来源。本实验继续使用服务器1的人工评分 `score=1` 四库人体 SMPL 数据，
不涉及服务器2、BUMI 93D、GMR/GMT 或机器人资产。

- 修改目标与初始化选择：新增
  `gem_smpl_music_only_4set_manual_q1_physics_v3_100k` 独立实验，按用户要求显式恢复
  physics_v2 的 9 项 Root/FK/关节时序与脚底穿地损失，并将每项权重统一提高为
  physics_v2 的 2 倍。选择人工高质量微调完成的 `s300000.ckpt` 作为权重初始化，而不是
  回退到 `s155000`：前者已经吸收 300k step 人工高质量人体/音乐监督，再用 physics
  强化阶段约束物理质量，可以保留更多人工筛选收益。两份 checkpoint 均完整，选定文件
  大小为 2,570,404,846 bytes，SHA256 为
  `c2aaff2b48d00602a4bd433056721b263b7bb2d9176c9390ba543a51d78ec529`。
- 训练配置：保持原 151D 网络和全部基础扩散/几何/静止关节监督；physics 权重从
  physics_v2 的 `[0.20,0.040,0.004,0.10,0.020,0.20,0.040,0.008,0.020]`
  提高到 `[0.40,0.080,0.008,0.20,0.040,0.40,0.080,0.016,0.040]`，归一化
  scale、GT 异常速度 mask 与 1 cm 脚底容差不变。为适配更强权重和每卡 batch 192，使用
  15,000 step 线性升权；正式预算为 8 卡、全局 batch 1536、100,000 optimizer step、
  AdamW `1e-5`，在 60k/85k step 各衰减一半，每 5k step 保存 checkpoint。
- Ground sidecar：人工高质量发布原先没有 physics sidecar，已在服务器1用同一
  SMPL-X v437 脚底代理为 train split 逐条生成并执行 ID、帧数、源动作 SHA256 强校验。
  AIST++、AIOZ-GDANCE、FineDance、CoMPAS3D 分别为 924、1694、136、38 条，合计
  2792 条，与正式 train 划分一致；`ground_valid` 分别为 734、1656、136、38。四个
  `ground_v1.jsonl` 的 SHA256 依次为
  `08963c91c3d951ebbb0c73654d9f87a4219e2fc0431eaf35dfb80bf56a01a30c`、
  `d677cb6419aacc62f5b500c0f6d46758a60223ddeb751adc5fb01584358dab27`、
  `428916fd8910ebe1bbf2c1d02f1a0f7f3ec18922e0ef1ec2be9274b23cb6bdfc`、
  `b8c060ad3cd635af8c207b5da52f069d28ea801de7ab2ffbcccdf5412abad65e`；构建日志保存在
  数据根的 `reports/physics_v3_sidecars_20260825/`。
- 兼容性/功能边界：本阶段通过 `pretrain_ckpt` 仅加载 `s300000` 模型权重，明确不恢复其
  optimizer、scheduler 或 global step；新输出与旧 physics_v2、人工高质量 300k 输出隔离。
  `ground_valid=false` 的样本只屏蔽脚底穿地项，仍参与基础损失和其余 8 项时序损失。
  更强训练损失不是实物机器人安全证明，最终 checkpoint 仍需固定种子生成质量、脚滑、
  穿地、速度/加速度/jerk 以及后续 GMR/GMT 跟踪评测。
- 当前验证：新增 Hydra 配置回归测试，核对人工高质量数据根、9 项 2 倍权重、15k warmup、
  batch 192、8 卡、100k step、scheduler、强制 sidecar 与网络结构不变；完整
  `tests/test_smpl_physics_v1.py` 为 `14 passed`。测试使用禁写 bytecode/pytest cache 的
  方式运行，残留检查确认仓库源码范围没有 `.pytest_cache`、`.ruff_cache` 或
  `__pycache__`；`.venv` 依赖缓存不属于本次测试产物，未删除。
- 首轮启动故障与分层定位：正式作业在 8 个 rank 加载 `s300000`、注册 NCCL 后、首个训练
  batch 前触发 `CUDA illegal memory access`，因此没有产生任何 optimizer step 或
  checkpoint；失败现场移到
  `/data0/user/liwei/GENMO_outputs/gem_smpl_music_only_4set_manual_q1_physics_v3_100k_failed_nccl_default_20260825/`。
  8 张卡分别运行最小 CUDA kernel 全部正确，2 卡和两组独立 4 卡 NCCL all-reduce 也通过，
  但未显式绑定设备的 8 卡 collective 可稳定复现同一错误；这把问题定位到当前
  PyTorch/NCCL 八卡 communicator 初始化，而不是数据、checkpoint、batch OOM 或 physics
  loss。单卡同配置、batch 192、完整前向/反向 1 step smoke 实测 loss `4.98`、峰值显存
  26.0 GB 并正常结束，进一步证明训练图可运行。
- DDP 初始化修复：`scripts/train.py` 新增显式 NCCL 初始化函数，把
  `torch.cuda.current_device()` 同时传给 `dist.init_process_group(..., device_id=...)` 和首个
  `dist.barrier(device_ids=...)`。新增 monkeypatch 回归测试锁定 backend、device 和
  barrier 参数。显式 device binding 与 `NCCL_ALGO=Ring` 能让标量 all-reduce 在
  8 卡上得到正确总和 `36.0`，但后续大张量验证表明这不足以代表模型同步
  可用。修复后完整 physics 专项为 `15 passed`，Ruff、
  `git diff --check` 与缓存残留检查通过；该修复只影响多卡进程组创建，不改变单卡训练、
  模型、损失和 checkpoint 语义。
- 八卡大张量通信定位：显式绑卡后的 Ring 默认协议仍在 DDP 模型首次同步时失败，
  无 optimizer step/checkpoint 的现场保留于
  `/data0/user/liwei/GENMO_outputs/gem_smpl_music_only_4set_manual_q1_physics_v3_100k_failed_nccl_ring_default_proto_after_device_bind_20260825/`。
  显式绑卡修复前单独尝试 Ring 的现场另存于
  `/data0/user/liwei/GENMO_outputs/gem_smpl_music_only_4set_manual_q1_physics_v3_100k_failed_nccl_ring_before_device_bind_20260825/`。
  在服务器1当前 PyTorch `2.7.1+cu128`/NCCL `2.26.2`、跨两个 NUMA 节点的 8 卡拓扑上，
  `NCCL_PROTO=Simple` 的 all-reduce 实测 1 KB 通过而 64 KB 非法访存；每次失败后都用
  8 卡独立 CUDA kernel 确认并恢复单卡状态。改用 `NCCL_ALGO=Ring NCCL_PROTO=LL`
  后，32 MB 与 256 MB 的 all-reduce/broadcast 在 8 个 rank 均数值正确，覆盖 DDP
  约 250 MB 的初始同步桶。诊断用 `/tmp/genmo_nccl_*` 日志均已精确清理。
- 正式训练启动验收：服务器1以 `main@a4d1b1942b98b911e503888769c794077ded8b1a`、
  `NCCL_ALGO=Ring NCCL_PROTO=LL` 启动 tmux `genmo_smpl_hq_physics_v3_100k`，正式输出在
  `/data0/user/liwei/GENMO_outputs/gem_smpl_music_only_4set_manual_q1_physics_v3_100k/`。实际已越过
  8 卡 DDP 初始同步并训练到 step 39/Epoch 1；TensorBoard 同时存在 9 项 physics
  raw/normalized/weighted 标签，step 39 的 `train/loss=5.1738`、
  `physics_total_loss=7.2421e-5`、单 step `0.7497 s`，8 卡显存约 32.2 GB，无
  CUDA/OOM/NaN。该非零 physics total 表明 15k warmup 期的物理损失已参与反向；
  单卡 smoke 输出已按测试产物约定清理，上述失败现场仅保留用于通信问题追溯，
  待本 physics_v3 阶段结束且无需复盘 NCCL 时再精确清理。

## 2026-08-26：physics_v3 s100000 四库 SMPL/BUMI 可续跑验证流水线

分支：`main`。本条记录覆盖训练完成验收、checkpoint/ONNX 交付边界、四库验证清单和首条
SMPL→GMR→BUMI 端到端冒烟；60 条正式批量结果完成后另行追加最终数量与媒体验收记录。

- 修改目标：为服务器1完成的人工高质量 SMPL `physics_v3` 模型建立一次可复现的四库视觉
  验证。新增 `scripts/validate_smpl_hq4_gmr.py`，把服务器八卡 SMPL 生成、本地同步 GMR-CPP
  重定向、SMPL-X/Open3D 与 BUMI3/MuJoCo 流式渲染、音频封装、并排对照、sidecar、汇总和
  HTML 索引整合为按阶段和分片可续跑的命令；新文件开头写明完整中文数据流与边界。
- 训练与模型来源：服务器1正式日志确认 `max_steps=100000 reached`，8 个 rank 均正常结束，
  无在跑训练进程；`s100000.ckpt` 与 `last.ckpt` 均为 2,570,404,846 bytes，SHA256 同为
  `98d70a145fb8f430ab557cdd0bde4af5f71e881b16f6976b385db6d172683136`。回传后的
  `s100000.ckpt` 再次核对同一哈希，并验证 `global_step=100000`、316 个 tensor 全部有限。
- ONNX：使用精确实验 `gem_smpl_music_only_4set_manual_q1_physics_v3_100k` 导出固定
  `T=120`、动态 batch、opset 18 的 CFG 单步去噪器；DDIM 调度和 EnDecoder/SMPL 解码仍在
  图外。ONNX 大小 874,074,147 bytes，SHA256 为
  `dcbed23134e2b4578a7c7aca92d9416392287372b658a58a97e67f53466e35c6`。以真实 AIST
  音乐跑 CUDA ONNX Runtime：单步 motion/camera/logits 最大绝对误差分别为
  `0.002261/0.000265/0.010916`，在 `atol=0.015, rtol=0.005` 下均通过；20-step DDIM
  motion 最大绝对误差 `0.005223`，在 `atol=rtol=0.02` 下通过，最终报告 `final_pass=true`。
- 清单与兼容边界：从人工评分 `decision=keep` 的冻结 `accepted_master.jsonl` 按
  `music_key` 去重，优先 test、其次 val、最后 train，同级按固定 seed 和 key 的 SHA256
  稳定排序；精确选择 AIST++ 5、AIOZ-GDANCE 20、FineDance 30、CoMPAS3D 5 首。AIST、
  AIOZ 与 CoMPAS3D 全部来自 test；FineDance 的 test 只有 13 首可用，因此其余 17 首由
  train 补足。每首从原始 WAV 的 0 秒起截取 20 秒并重新提取 EDGE35，统一生成 600 帧；
  不重复或拉长 AIST test 中不足 20 秒的冻结特征。清单记录源音频、20 秒音频、人工清单、
  seed、split 和哈希。
- 服务器推理预处理边界：服务器1当前环境从 WAV 进入 librosa/音频特征入口时，单进程和
  八进程均在写出 Python 日志前触发内核记录的空地址 `SIGSEGV`；同环境直接读取已经生成的
  `[600,35]` feature 则完整推理通过，排除 checkpoint、CUDA 推理图和八卡显存问题。为避免
  依赖差异污染正式条件，60 首均在本地标准 GENMO `.venv` 从上述冻结 WAV 一次性提取并保存
  EDGE35，每个 tensor 的路径、形状和 SHA256 加入 selection；服务器只读这些固定 tensor，
  音频仍用于本地两种视频的同源封装。失败 staging 保留到服务器正式生成验收后精确清理。
- GMR 功能边界：本工具只读使用用户指定的
  `/home/weili/GMR-CPP_e1jump_lowdpi/`，不修改其中的脏工作树。当前 batch server、IK JSON、
  BUMI3 MJCF SHA256 分别为
  `01ed599fc02da8fa76bb5bf7e55532b216ffc1bf9d7dc8a7e50532cd72446935`、
  `5276c08f9d54bd72fbf91cc71f16ce02a9849aac491fd6e814ad8f815335b36e`、
  `041c81e8176c7f375302796deca28b141891a3c097d8e341e8d967b735466edf`；真实同步 batch
  模式逐帧执行 SMPL-X FK、SMP1 编码和 C++ IK，每首显式 reset，不走 Redis，也不把人体
  关节直接伪装成机器人 qpos。输出固定为 30 Hz MuJoCo 原生 `qpos[T,28]`、wxyz。
- 首条实际验证：`aistpp_01/mWA0` 以 s100000、seed 20260826、50-step DDIM 完成 600 帧，
  SMPL sanity 通过；GMR qpos 形状为 `[600,28]`、全有限，根四元数最大范数误差
  `5.96e-8`。SMPL、BUMI 和 1280×360 并排视频均为 H.264/AAC、30 FPS、600 帧、20.000 秒，
  抽取第 10 秒画面确认左侧为完整紫色 SMPL-X 网格、右侧为可见的完整 BUMI3 机器人。
  Python 编译、Ruff 与 GMR `smplx_bumi3_config_test` 均通过；视觉冒烟临时目录已删除。

## 2026-08-26：physics_v3 s100000 四库 60 条 SMPL/BUMI 最终验收

分支：`main`；本条是上一条验证流水线的正式批量完成记录。生产结果根目录为
`outputs/smpl_physics_v3_s100000_hq4_validation_20260826/`，checkpoint 和 ONNX 分别位于
`outputs/gem_smpl_music_only_4set_manual_q1_physics_v3_100k/version_0/checkpoints/s100000.ckpt`
与 `outputs/onnx/smpl_music/physics_v3_s100000/music_only_denoiser.onnx`。

- 服务器八卡生成：服务器1同步 `main@2cef1aba7e7620a8ba7af1b34e9d6b04389b0168` 后，以 8 个
  独立 GPU 分片完成 60/60 条、每条 20 秒/600 帧、50-step DDIM 的 SMPL 生成。四库数量为
  AIST++ 5、AIOZ-GDANCE 20、FineDance 30、CoMPAS3D 5；每条均核对 checkpoint step
  100000、selection seed、冻结 EDGE35 SHA、`music_shape=[600,35]`、
  `generated_shape=[600,151]`、四个 SMPL 字段形状与有限值，60 条 `final_pass` 全为 true。
  正式参数与日志已回传本地；最初 WAV 解码段错误产生的 9 个空 staging、1 个旧候选和直接
  feature 冒烟目录已在确认不含 checkpoint/ONNX 后精确删除，服务器只保留 60 条正式结果。
- 本地 GMR 与渲染：在单张 RTX 4090 上用 3 个互不重叠分片完成 60/60 条真实同步 GMR、
  SMPL-X 网格、BUMI3 MuJoCo 和横向对照渲染。每条 GMR qpos 都是 `[600,28]`、30 Hz、
  全有限、MuJoCo 原生 21 关节顺序和 wxyz；60 条根四元数最大范数误差为
  `1.1920929e-7`。C++ solver 的逐条平均耗时再汇总为 `2033.63 us/frame`，条目平均值范围
  `1004.14..2637.90 us/frame`。每首动作开始都同时 reset 人体参考原点和 C++ IK 状态。
- 媒体验收：最终共有 60 个 SMPL、60 个 BUMI、60 个横向对照 MP4，合计 180 个、
  157,738,028 bytes。逐个用 ffprobe 解码计帧，全部恰好是 H.264 视频 + AAC 音频、30 FPS、
  600 帧、20.000 秒；单独视频为 640×360，对照视频为 1280×360。AIST++、AIOZ、
  FineDance、CoMPAS3D 各抽一条第 10 秒画面，均确认左侧是完整紫色 SMPL-X 人体、右侧是
  完整可见且采用 2.2 m 跟随相机的 BUMI3 机器人，不是骨架或占位渲染。
- 交付与哈希：`selection.json`、`summary.json`、`index.html`、`final_validation.json`
  SHA256 分别为 `7190ad56f4975c0a7b56908ff135d8e8ab0dfea6bc9bcfff2b329f04a3ea67e3`、
  `2254d40c03e27dda56b2eab6dbd8aafd88bf0ff8c721d753b544f991ea5d8505`、
  `9769a4953f820c9dd5bdc70717f1323017f0d99acf9c0363dd6fe744df49e88a`、
  `360bb2c066bb91abfbef5d21f206974d44e03f2ae37b067d1874fb9fe417c1f1`；最终独立验收报告
  `errors=[]`、`final_pass=true`。结果根共约 416 MB，ONNX 约 834 MB，checkpoint 约 2.4 GB。
  本地抽帧目录、空 `_render_work`/`_generation_staging`、Ruff/Python 缓存均已清理并完成
  残留检查；正式音频、冻结特征、SMPL、qpos、视频、日志和报告按交付用途保留。

## 2026-08-26：physics_v3 s100000 训练集完整音乐 SMPL/BUMI 重验证

分支：`main`。本条响应网页播放器过小、旧清单混入 test/val 且所有音乐被固定截为 20 秒的
问题；旧的 20 秒正式结果目录保持不变，新结果根为
`outputs/smpl_physics_v3_s100000_hq4_train_full_20260826/`。

- 修改目标与选择边界：扩展 `scripts/validate_smpl_hq4_gmr.py` 的 `prepare` 阶段，只接受人工
  高质量清单中同时满足 `decision=keep` 和 `split=train` 的样本。AIST++ 按真实 music ID、
  AIOZ-GDANCE/FineDance/CoMPAS3D 按训练特征文件名对应的真实 WAV 键去重，同库使用固定
  `selection_seed=20260826` 的 SHA256 顺序稳定选择 5/20/30/5 首；不再优先 test/val。
- 完整音乐实现：复制每首训练样本对应的完整源 WAV，从 0 秒到文件结尾重新提取 30 Hz
  EDGE35，并把动态 `[T,35]` 形状、音频/特征 SHA256、split、人工 review ID、seed 和实际
  时长冻结到 v2 selection。`generate` 阶段取消所有 600 帧硬编码，按每首实际 `T` 调用
  `demo_music_only.py`，沿用已经验证的 600 帧 diffusion window、120 帧 overlap-add 和完整
  序列统一解码；SMPL wrapper、GMR `qpos[T,28]`、音频封装、ffprobe 帧数/时长检查均改为
  动态 `T`。完整指源 WAV 从头到尾，不对数据集原生短音频循环、拉伸或外推。
- 网页与复用边界：HTML 不再使用自适应多列网格，每个条目独占整行，横向 SMPL-X/BUMI3
  对比播放器宽度使用整行、画面比例为 32:9，并显示完整帧数/时长和 train 来源。渲染复用
  除条目 ID/合同外新增动态帧数、完整音频 SHA256 和生成 SMPL SHA256 检查，避免重选音乐
  后误用同编号旧视频；旧 v1 selection 仍可读，但新 v2 结果使用独立目录和合同。
- 实际准备与验证：已从本地正式 `accepted_master.jsonl` 冻结 60/60 首，四库数量严格为
  5/20/30/5、split 全为 train、源 WAV 缺失数为 0。完整条件合计 177,493 帧/
  5,916.43 秒（98.61 分钟），单首范围 212–10,130 帧（7.07–337.67 秒），总音频约
  1,008,371,964 bytes。新增 `tests/test_validate_smpl_hq4_gmr.py` 覆盖 train/keep 门、真实
  音频去重、稳定选择、四库数量、动态帧兼容和一行一首网页合同；与长序列 demo 合同合跑
  共 11 项通过，Ruff 与 Python 编译通过。测试生成的目标 pyc、空 `__pycache__` 和
  `.ruff_cache` 已精确清理；正式完整音频/EDGE35 按交付用途保留。
- 服务器完整生成完成：代码先提交并推送到 `main@03763f97f198e8dad65b4524c672eb1dc312ce42`，
  服务器1仅执行 `git pull --ff-only`，再在数据盘新目录以 8 个 GPU 分片完成 60/60 首。
  全量审计为 177,493 帧、383 个 diffusion window、四库 5/20/30/5、split 全 train、
  checkpoint step 全为 100000、60 条 `final_pass=true`，SMPL 四字段形状/有限值与日志错误
  扫描均通过；正式 artifact 和日志已回传本地。
- GMR/媒体验收完成：本地使用用户指定 GMR-CPP binary/IK/MJCF 的同一组 SHA256，以动态
  `qpos[T,28]` 完成 60/60 首真实同步重定向和渲染。最终 60 个 SMPL、60 个 BUMI、60 个
  横向对比视频，共 180 个 MP4、788,893,359 bytes；逐帧解码检查了 532,479 个视频帧，
  全部为 H.264 + AAC、30 FPS，单独视频 640x360、对比视频 1280x360，帧数和完整音乐
  时长逐条匹配。最大音频/EDGE35 时长差 0.033334 秒，BUMI 根 wxyz 四元数最大范数误差
  `5.9604645e-8`。四库各抽一首中间帧视觉检查，均可见紫色完整 SMPL-X 与完整 BUMI3。
- 网页交付修正：实际浏览器连接暴露出 60 个长视频使用 `preload=metadata` 会在不支持 HTTP
  Range 的简易服务下预取约 789 MB；因此最终改为 60 个播放器全部
  `preload=none playsinline`，保持一行一首、整行 32:9 播放器，点选时才加载目标视频。
  对应轻量合同测试 4 项再次通过，Ruff 通过；HTTP 首页和最长 FineDance 对比视频均返回
  200。最终 `selection.json`、`summary.json`、`index.html`、`final_validation.json`
  SHA256 分别为 `213dd43fb62bc7065f89606c64914f974c0c37d8d19dfa2e4d3df223375f74fb`、
  `c294d1907441652e9b129f47881130bc877d6d5603bafaa4403291f87f8fcc58`、
  `edb7e29484067e49123ab757f084f7ce8cafdd3661e6ea1eb1f9b2702eb9bd8c`、
  `d0a19bd6cd9d64fb00c681291b41295c3f2d9f71f5f6e72a5be817631fefd6c8`；最终报告
  `errors=[]`、`final_pass=true`。并行队列汇合时精确终止的 3 个重复渲染只留下独立
  `_render_work/<id>-<suffix>` 临时目录，已逐一确认不含正式 artifact 后删除；最终
  `_render_work` 为空，正式视频和报告保留。

## 2026-08-27 14:28 CST：修复 FineDance batch 接触卡顿/悬浮并重新渲染

- 所属分支与修改目标：GENMO 位于 `main`；GMR-CPP 同样位于 `main`。针对首次启用
  batch 脚接触约束后 BUMI 出现逐次卡顿和约 5 cm 悬浮的问题，按“接触生命周期 →
  源/目标地面拆分 → 地面漂移跟踪 → 检测滞回 → 因果输出保护 → 完整视频复验”的
  顺序修复。没有修改 SMPL 生成模型、checkpoint、训练数据或已生成的人体动作。
- GMR 修复已提交并推送到 `origin/main@44ad351`；GENMO 本节脚本、测试和记录按同一
  验证结果提交到 `main`，不放入 BUMI 专用开发线。
- GMR 主要实现：`smplx_bumi3_batch_server` 只在 RESET 做一次 contact-only 初始化；
  stance 新进入时先释放该脚并跟随当前人体帧一次，再捕获当前机器人锚点，从下一帧
  渐进启用约束，移除每次落脚的多轮 settle 和同一输入帧重复推进。人体源地面新增
  独立 `--source-ground-clearance`；可靠支撑期以 `0.04 m/s` 慢跟踪，普通腾空冻结，
  连续 45 帧且重新稳定后以 `0.12 m/s` 渐进重捕获。接触进入/退出使用 4/2 帧滞回，
  移动双支撑弱侧保持一致；最终根节点 XY 使用 `0.75 m/s` 因果限速。鞋底 stance
  仍保留中心 `1--3 mm`、前后/左右高度差 `3 mm`、不穿地和可靠支撑 XY 锚点。
- GENMO 主要实现：`scripts/validate_smpl_hq4_gmr.py` 将目标和人体源 clearance 默认值
  都改为 `0`，显式向 batch server 传入两项参数；qpos 和视频复用身份新增 batch
  binary、IK JSON、MJCF SHA256、两项 clearance 与 reset iterations，避免配置变化后
  误用旧的悬浮视频。`tests/test_validate_smpl_hq4_gmr.py` 新增零间隙默认值回归测试。
- 数值验证：GMR 完整 CTest `9/9` 通过；GENMO 对应 pytest `5/5` 和 Ruff 通过。
  `finedance_01` 的 qpos 为有限 `[1851,28]`；修复前 5 cm 输出中双脚同时高于地面
  20 mm 的帧为 `1350/1851`，修复后为 `0/1851`；两脚最低点逐帧最小值
  P50/P95 为 `0.707/1.511 mm`，无可测穿地。根节点单帧位移由修复前最大
  `8.89 cm` 降为 P50/P95/P99/最大 `0.663/1.578/2.140/3.841 cm`，XY 最大
  `1.500 cm`。曾试验整套 qpos/根 Z 输出后插值，但脚底平面误差会放大到约 4 cm，
  因而未保留该错误方案；Z 继续由同帧 IK 与接触 QP 联合求解。
- 正式渲染与交付：使用修复后的 GMR binary/IK/MJCF，以
  `--ground-clearance 0 --source-ground-clearance 0` 覆盖生成
  `outputs/finedance_gmr_contact_validation_20260827/videos/finedance/` 下的完整
  SMPL、BUMI 和横向 comparison 视频。三段均为 1851 帧、30 FPS、61.7 秒并包含完整
  音乐；分辨率分别为 640x360、640x360、1280x360，全量 ffmpeg 解码无错误。对
  160、453、1011/1012、1661/1662、1800、1850 帧进行画面抽查，BUMI 可见落在地面，
  相邻高位移帧未见整机瞬移或悬空。comparison SHA256 为
  `e8bea0e9ca2c3250b8e93231f96dff922c55c09ba7cc6b4bded5728e5a87b5da`，HTTP 链接
  返回 200。
- 兼容性/功能边界：旧 `bumi_qpos30.pt` 和旧视频因身份不匹配会自动重算；显式关闭
  foot-contact 时仍可走旧路径。上述结果验证运动学接触、媒体完整性和离线可视效果，
  不等同于 GMT 的 COM/支撑多边形/动力学稳定性或实机安全验证。视觉检查临时目录
  `/tmp/genmo-gmr-visual-T9Q0L6` 已精确删除；正式视频和 qpos 按交付要求保留。

## 2026-09-09：MotionMillion SMPL 纯文本扩散训练 v1 工程闭环

- 所属分支与基线：从未移动的
  `main@966e4a2968c263593d7597b61d60fcf277ec622a` 新建
  `codex/feature-smpl-text-motionmillion`。修改目标是在不合入
  `feature/smpl-music-only-genmo` 或 BUMI 变更的前提下，只使用 MotionMillion
  官方发布动作、文本和 split，从零训练 GENMO 现有 151D SMPL GEM/DDIM 文本动作模型。
- 数据来源与许可边界：数据源固定为 Hugging Face
  `InternRobotics/MotionMillion` 的不可变 resolved revision，官方代码固定到
  `VankouF/MotionMillion-Codes@8a2a7dfa66ecb6a1533d3d9cb49c743a697e1e1c`；数据为
  gated、CC BY-NC-SA 4.0，且部分上游数据不可再发布。新增下载器只接受用户通过
  `hf auth login`/环境提供凭据，先下载并审计 metadata，再逐 motion 归档恢复下载；
  每个归档完成大小、LFS/SHA256、tar 安全性检查并原子更新进度。原始数据、转换结果、
  embedding、checkpoint、视频均位于已忽略的 `inputs`/数据盘边界，不进入 Git。本轮未
  确认用户 gated 表单，也未下载任何真实 MotionMillion 文件。
- 272D→SMPL 与 release 实现：新增流式 tar 构建器和 SQLite metadata 索引，只接收
  同时命中官方 split 与非空文本的 `[F,272]`。heading 严格按官方
  `R_rel @ R_previous` 累积，root rotation 为 `R_heading^-1 @ R_local_root`，第
  `t>0` 帧平面速度用前一帧 inverse heading 转回世界系，translation Y 使用局部 root
  高度；输出 30 FPS、米制、Y-up 的 `pose[F,66]`、`trans[F,3]`、零 `beta[10]`。
  60--300 帧之外、shape/finite/旋转异常均进入持久化拒绝表。原 ID/镜像 base ID
  跨 split 会阻断；pilot 范围外 ID 记为 `unresolved_by_scope`，不冒充
  `unavailable_by_release`。motion shard 以每归档边界和每 512 条原子落盘，v1 mmap
  索引一条 motion 一行，120--300 帧由 Dataset 每次随机裁连续 120 帧，避免同动作跨
  DDP rank 重复；60--119 帧尾补齐但保留真实 `length/mask.valid`。
- 文本与 DataLoader 契约：新增 T5-3B 固定 revision 的离线提取器，对模型/分词器全部
  文件做 SHA256，最大 150 token、FP16，只保存有效 token 与 caption offset；motion 与
  embedding shard 顺序和 hash 一一对齐。`MotionMillionDataset` 训练时不加载 T5，均匀
  选择 caption，返回 `[150,1024]` float32、`[150]` bool attention mask、来源归档、
  source subset、motion/text ID 与真实有效长度。shard-aware sampler 使用
  `seed+epoch` 先乱序 shard、再乱序 shard 内样本，在 1/2/8 rank、`drop_last=True`
  下无未声明重复；旧 music-only 自定义 collate 不强制新增 mask，保持兼容。
- 模型与配置：新增 `text_only` pipeline 和
  `gem_smpl_motionmillion_text_only` 实验。正式解析值为只含 MotionMillion train/val、
  `train_modes=[diffusion]`、`in_attr=[]`、`encode_text=true`、150×1024 文本、
  `pretrain_ckpt=null`、`ckpt_path=null`。空逐帧条件显式生成
  `[B,L,1024]` 的 `f_cond/f_uncond/f_empty` 零张量；文本 padding mask 传入全部
  cross-attention layer，正式有文本全 padding 会提前失败。整段文本 CFG dropout 只在
  denoiser 保留 0.1，逐帧/pipeline/音乐/音频/图像/相机 dropout 均为 0，并记录实际
  batch 丢弃比例；151D 去噪、3D joint/vertex、世界位移和 static/contact 人体监督保留。
  新 checkpoint 自描述 150-token text-only 契约，旧 checkpoint 继续按 50 token 解析。
- 训练参数和监控：新增 AdamW 2e-4 的 5,000-step 线性 warmup + 300,000-step cosine
  到 2e-6；默认 8 卡 BF16、每卡 64、累积 1、全局 batch 512、clip 0.5、每 5,000 step
  验证、每 10,000 step 保存。日志新增裁剪前全模型 L2 gradient norm、文本 CFG 实测比、
  数据等待、batch 时间、全局 samples/s、稳定窗口 ETA 和 rank-0 CUDA 当前/峰值显存；
  GPU 利用率由 W&B system metrics 记录。OOM 时 32×累积2、16×累积4 的降级仍保持全局
  batch 512，但本轮没有获准运行 20/100-step GPU smoke 或正式 300k 训练。
- 预检、推理与评测：新增现有 151D stats 的 1,000 条通道分布预检、固定类别覆盖的
  32 条 pilot 渲染、动态 50/150-token SMPL 文本 demo/resident 推理。自动评测可把
  SMPL+22-joint FK 转回 272D，流式物化官方 val 最小数据视图，并冻结官方代码、
  `epoch=199.ckpt`、mean/std、eligibility 指纹。当前固定官方 loader 实际只接受
  60--200 帧，此资格与 60--300 帧训练范围分开记录。新增常驻 T5/GEM 的完整 val
  生成器、官方 evaluator wrapper 单 seed FID/Diversity/R@1/2/3/Matching 运行器、20-seed
  均值/95% CI 汇总和 checkpoint 决胜规则；126 条无 GT prompt 使用独立三项人工评分页，
  不混入 FID。
- 实际验证：MotionMillion 专项 CPU fixture `26/26` 通过，覆盖官方 NumPy 独立恢复
  parity（translation `<=1e-6 m`、rotation geodesic `<=1e-5 rad`、22-joint FK MPJPE
  `<=1e-5 m`）、SMPL↔272D、长度/泄漏、T5 FP16、tar→shard→resume、官方 evaluator
  物化、文本 mask、空条件、sampler、配置、评测数学和 checkpoint 契约。与旧 SMPL 文本、
  resident、多模态、music-only、Motion-X++、HumanML3D、T5 的兼容回归 `208/208`
  通过；变更文件 Ruff、所有新增 CLI `--help`、Python 编译和 `git diff --check` 通过。
  全仓 `ruff check .` 仍报告 51 个基线既有问题，均位于本分支未修改的 YOLOX、旧 ONNX/
  benchmark/curation 等文件，因此未越权批量改写；不把该结果表述为全仓 Ruff 通过。
  本轮产生的仓库 `.pytest_cache/.ruff_cache/__pycache__` 和 `/tmp/pytest-of-weili` 会话
  目录均已移入系统回收站，残留检查为 0；2026-07/08 已存在的正式 smoke 输出未改动。
- 待核验与完成边界：真实 metadata 文件树/ID 统计、10,000 条 MotionGV pilot、32 条真实
  视频、1,000 条真实 stats、在线 T5-3B FP16 parity、DataLoader 吞吐、GPU 显存、八卡
  DDP、全量约 308 GB release、300k 训练、官方 20-seed 指标和 126 条视频均保持
  `待核验`。这些阶段分别受 gated 许可、数据下载和 GPU/训练单独授权约束，代码完成与
  Git 同步不会自动启动下载、推理、训练或停止任何现有服务器进程。

## 2026-09-09：MotionMillion 功能分支按用户要求更名

- 所属分支与修改目标：用户明确要求将已实现的 MotionMillion SMPL 纯文本功能分支从
  `codex/feature-smpl-text-motionmillion` 更名为 `feature/smpl-text-only`，不再以
  `codex/` 为当前分支前缀。本次保留上一条记录中的原始名称作为历史事实，不重写既有
  记录；当前技术文档、Git 本地分支和后续远端/服务器同步统一使用新名称。
- 主要实现：使用 Git 分支重命名保留完整提交历史，将
  `docs/MOTIONMILLION_TEXT_ONLY.md` 的当前分支声明更新为
  `feature/smpl-text-only`。没有修改 MotionMillion 数据契约、272D→SMPL 公式、模型、
  训练参数、评测接口或既有测试逻辑。
- 兼容性/功能边界：原远端 `codex/feature-smpl-text-motionmillion` 仅作为已推送的历史
  引用暂时保留；依据仓库高风险 Git 约定，本次不在没有单独删除授权的情况下删除远端
  分支。服务器 1 的分支切换只会在用户已授权停止的现有训练进程完整退出后执行。
- 实际验证：本地分支名检查为 `feature/smpl-text-only`，分支 HEAD 仍保持原实现提交
  `2c7209f4c1d88b3eb2fd6ab626407b9583e61fc5`；文档修改完成后执行
  `git diff --check` 并在新分支提交、推送和复核远端 SHA。

## 2026-09-09：修复 Hugging Face 新版文件树目录项误判

- 所属分支与修改目标：`feature/smpl-text-only`。服务器 1 首次运行 MotionMillion
  metadata 下载时，16 个目标对象完成传输后在校验阶段报错
  `下载结束后缺少文件: data_process/AIST`；目标是修复清单解析，而不重复下载或放宽
  文件完整性检查。
- 主要实现：`huggingface_hub==1.25.1` 的 `RepoFile` 与 `RepoFolder` 均没有旧版
  `type` 字段，但目录具有 `tree_id`、文件具有 `blob_id`。下载器 `_remote_file_row`
  现在显式排除带 `tree_id` 的目录项，只让真实文件进入大小、SHA256 和 tar 校验清单；
  原有固定 revision、逐归档恢复和原子 manifest 契约保持不变。
- 兼容性/功能边界：仍兼容提供 `type=file` 的旧版 API 和现有 fixture；不使用文件后缀
  推断目录，不跳过真实文件，也不删除第一次运行已经落盘的 Hugging Face 缓存。网络继续
  通过只绑定服务器回环地址的 SSH 反向代理访问官方 `huggingface.co`，不把 gated token
  发送到第三方镜像或写入仓库/命令参数/下载日志。
- 实际验证：新增 `RepoFolder(type=None, tree_id=...)` 被忽略、
  `RepoFile(type=None, blob_id=...)` 被保留的回归测试；MotionMillion 专项测试更新为
  `27/27` 通过，变更文件 Ruff 和 `git diff --check` 通过。修复提交同步服务器后将复用
  已下载对象重新执行 metadata 校验，并以实际生成的 manifest 为下载完成依据。

## 2026-09-09：隔离官方 split 中跨集合的镜像增强条目

- 所属分支与修改目标：`feature/smpl-text-only`。真实
  `version1/t2m_60_300` 审计发现官方 split 会把部分原动作及镜像增强随机分到不同
  train/val/test，例如 `MotionGV/folder0/000005` 位于 train，而
  `Mirror_MotionGV/folder0/000005` 位于 test。目标是在不关闭泄漏门、不把样本重分配到
  其他 split 的前提下，满足本分支锁定的镜像 base ID 零跨 split 契约。
- 主要实现：新增 `split_exclusions` SQLite 表和
  `mirror_cross_split_exclusions.jsonl` 报告。每个跨 split base 以非镜像原动作所在的
  官方 split 为唯一 canonical，保留该 split 内的原动作及同 split 变体，把其他 split
  的镜像条目从训练 eligibility 移入排除表；如果无法找到唯一 canonical split，则拒绝
  猜测并阻断。metadata 报告同时保留原始官方 split 数、隔离数和最终 eligible 数，后续
  转换、Dataset 和 evaluator 只读取隔离后的 `split_entries`。
- 兼容性/功能边界：没有改写官方 `split.tar.gz`，没有将 val/test 镜像挪入 train，也
  没有把被隔离条目误报为缺失发布数据。mirror base 规范化改为保留路径大小写，因为
  tar/Unix ID 大小写敏感，官方数据存在仅 `Up/up` 不同的独立路径；镜像前后缀识别仍为
  大小写不敏感。构建契约版本从 1 提升到 2，防止旧 eligibility 与新隔离策略混用。
- 实际验证：新增跨 split fixture，确认 canonical 原动作保留原官方 split、跨集合镜像
  进入排除表、最终 split 无 base 泄漏；同时覆盖大小写不同 ID 不被错误合并。
  MotionMillion 专项测试 `28/28` 通过，变更文件 Ruff 与 `git diff --check` 通过。
  真实全量排除数量将在修复同步服务器并重新构建 metadata 后以审计报告为准。

## 2026-09-09：MotionMillion 全量下载恢复跳过已验证归档

- 所属分支与修改目标：`feature/smpl-text-only`。服务器已完成 32/60 个动作归档、
  `180602505303` 字节的 SHA256 与 tar 校验后，因 Xet 直连自适应带宽下降重启下载；发现
  原实现虽不重复联网下载，却会从第 1 个归档重新做哈希和 tar 全遍历。目标是直接复用
  原子进度中的已验证前缀，同时继续拒绝任何身份漂移。
- 主要实现：新增 `_reuse_verified_progress`，恢复时逐项核对 repo ID、不可变 revision、
  motion pattern、远端 motion 总数、文件顺序、路径、远端大小、blob ID、LFS SHA256、
  本地文件大小以及既有 SHA/tar 完成状态；全部一致才把已验证结果合并回当前文件清单，
  从下一个归档继续下载。新下载 ETA 只使用本次恢复后的新增字节吞吐计算，避免把历史
  完成字节除以很短运行时间而产生虚假高速 ETA。
- 兼容性/功能边界：缺少 progress 时仍从第 1 个文件开始；任一 header、远端身份、顺序、
  本地大小或校验状态不一致均明确报错，不静默跳过、不混用 revision。已完成文件仍以此前
  实际计算并与远端 LFS 对齐的 SHA256 为依据，未完成的第 33 个归档继续由 Xet range
  cache 恢复；不删除任何正式原始数据。
- 实际验证：新增完整 progress 前缀复用 fixture，并注入 blob ID 漂移确认严格阻断；
  MotionMillion 专项测试 `29/29` 通过，变更文件 Ruff 与 `git diff --check` 通过。

## 2026-09-09：full 下载纳入官方 assets 文件树

- 所属分支与修改目标：`feature/smpl-text-only`。首轮 full 下载已完成全部 60 个动作
  归档和训练所需 metadata，共 76 个文件；与官方 revision 的 77 个文件比较后，唯一未
  选择项是 `assets/motionmillion_teaser.png`（`1777362` 字节）。虽然该图片不参与训练，
  用户要求完成整个数据集下载，因此将 full release 扩展到官方文件树 77/77 对齐。
- 主要实现：新增 `FULL_STATIC_PATTERNS`，metadata 阶段仍只下载 split/text/mean_std/
  data_process 等审计输入；full 阶段额外选择 `assets/**`，并在逐 motion 归档前下载、
  校验完整静态文件集合。最终 full manifest 因而可与 `remote_repository_file_count` 和
  `remote_repository_total_bytes` 完全一致。
- 兼容性/功能边界：不把 teaser 作为训练样本或评测输入，不改变 60 个动作归档的转换
  eligibility；已有 `download_progress_full.json` 仍按 60 个 motion 文件复用，不重新
  哈希约 307 GB 已验证动作包。metadata-only 的 16 文件契约保持不变。
- 实际验证：新增 assets 在 metadata 阶段不匹配、full 阶段匹配的断言；MotionMillion
  专项测试仍为 `29/29` 通过，变更文件 Ruff 与 `git diff --check` 通过。同步服务器后仅
  补下 teaser 并重新生成 77 文件 full manifest。

## 2026-09-09：服务器 1 完成切支与 MotionMillion 77/77 全量下载

- 所属分支与执行目标：按用户明确授权，将服务器 1 上运行的
  `gem_smpl_music_only_4set_manual_q1_physics_v4_resume_100k` 八卡训练停止，再将
  `/home/user/liwei/GENMO` 从 `feature/smpl-music-only-genmo` 切换并快进到
  `feature/smpl-text-only`。停止前最近完整 checkpoint 为
  `/data0/user/liwei/GENMO_outputs/gem_smpl_music_only_4set_manual_q1_physics_v4_resume_100k/version_0/checkpoints/s110000.ckpt`
  及对应的 `last.ckpt`；SIGINT 后确认 launcher、8 个 rank、DataLoader 子进程与 GPU
  占用全部退出，未把中断中的 batch 记作新 checkpoint。
- 数据下载与身份：用户完成 Hugging Face gated 网页授权后，服务器以标准凭据缓存登录，
  token 未进入仓库、命令参数或下载日志。官方数据 revision 冻结为
  `007582c4fc9637a3f36e548a67be1ef6eaf881a5`，完整 77/77 文件、
  `307655361973` 字节下载到
  `/data0/user/liwei/datasets/MotionMillion/raw_hf`；其中 60 个 motion tar 为
  `306985351080` 字节。仓库软链接为
  `/home/user/liwei/GENMO/inputs/MotionMillion -> /data0/user/liwei/datasets/MotionMillion`。
- 完整性与 metadata 结果：77 个文件均计算 SHA256，65 个 LFS/Xet 文件与远端 SHA
  不一致数为 0；62 个 tar 共完整遍历 `2932197` 个成员，缺失文件和大小不一致均为 0。
  full manifest SHA256 为
  `cae9b4eacb7d127a855868a04ba7dcc612ee834d886d8667b55920ff46f705a2`，progress
  manifest 为 `c0a0f46346deb6802c99fee0eb8735d579bf161f097ab0bc013d2eac46009fba`。
  官方 split 原始 train/val/test 为 `700903/44044/131819`；143800 个跨 split mirror
  base 组隔离 `160177` 条后，eligibility 为 `624554/21211/70824`，其中 3 条缺文本，
  带文本候选共 `716586` 条。metadata audit SHA256 为
  `7bb14ccf4d16e0c47a66180466e838dd8f8cca013eb9dd282cda4f9639625bd9`。
- 网络与清理：服务器直连 `huggingface.co` 受 DNS/SNI 网络限制，主站 API 使用仅绑定
  服务器 `127.0.0.1` 的 SSH 反向代理，官方 `*.hf.co`/`*.xethub.hf.co` 数据端点由服务器
  直连；未使用第三方镜像。Xet 两次降速后通过严格 progress 前缀恢复完成，没有重下或
  重哈希已验证归档。中途失败的 3.30 GB SQLite、HTTP fallback 探针和 4 个共约 1.68 GB
  `.incomplete` 缓存均在正式制品核验后精确删除；最终 incomplete/lock 残留为 0。
- 功能边界与最终状态：本次只完成原始下载和 metadata 审计，没有启动 272D→SMPL
  转换、T5-3B embedding、pilot、smoke 或正式训练。终检时服务器仓库工作树干净，
  分支为 `feature/smpl-text-only`，GPU compute process 为 0；`/data0` 尚余约 2.6 TB。

## 2026-09-09：MotionMillion 转换 release 增加真实时长统计

- 所属分支与修改目标：`feature/smpl-text-only`。为满足全量转换后可审计地统计有效数据
  时长，将时长作为正式数据契约的一部分，而不是根据官方宣传规模或平均长度做估算；并按
  用户明确授权删除远端旧分支 `codex/feature-smpl-text-motionmillion`，远端 `main` 与
  当前 `feature/smpl-text-only` 均未移动。
- 主要实现：构建契约提升到 version 3；每个 split manifest 新增 `total_frames`、
  `duration_seconds`、`duration_hours`，顶层 `dataset_release.json` 同步给出三个 split
  的总和。统计逐条累加真正通过 272D→SMPL 转换、校验并写入 motion shard 的原始帧数，
  固定按官方 30 FPS 换算。
- 兼容性/功能边界：60--119 帧样本训练时补齐到 120 帧的 padding 不计入时长；被镜像
  泄漏策略隔离、缺文本、未发布、shape/finite/旋转/位移校验失败的记录同样不计入。旧
  数据 release 仍可被旧代码读取，但正式全量转换必须使用 version 3，避免把估算值误写
  成真实时长。
- 实际验证：fixture 中 train/val/test 各 60 帧，验证 split 时长为 2 秒、总时长为
  180 帧/6 秒；MotionMillion 专项测试 `29/29` 通过，变更文件 Ruff、`py_compile` 与
  `git diff --check` 全部通过。服务器 1 终检 8 张 RTX 6000D 均为 0 MiB、0% 利用率，
  无训练/转换进程，原始数据 287 GiB、磁盘可用约 2.6 TiB；pilot 与全量转换将在本提交
  同步服务器后执行。

## 2026-09-09：修复 MotionMillion 官方分卷来源命名空间解析

- 所属分支与修改目标：`feature/smpl-text-only`。服务器 1 首次真实 10,000 条 pilot
  完整扫描 10 个 MotionGV 分卷、共 559924 个 NumPy member 后接收数为 0；archive
  counters 全部为 `not_in_closed_release`。对比真实 tar 与官方 split 后确认：tar member
  是 `folder0/043161.npy`，官方 ID 是 `MotionGV/folder0/043161`，原 fixture 带了现实中
  不存在的 `motion_data/vector_272/MotionGV/` 前缀，因此静态测试没有暴露问题。
- 主要实现：新增 `_archive_identifier_candidates`。对位于
  `motion_272rpr/<来源>/<分卷>.tar.gz` 的 MotionGV、MotionLLAMA、MotionUnion 及镜像
  归档，从受控 archive 相对路径补回来源命名空间；根目录 PhantomDance 仍使用 member
  自带的完整数据集名。修正闭环 fixture，使 member 与真实 MotionGV 一样从 `folder0/`
  开始，而 split/text ID 保留 `MotionGV/`。
- 兼容性/功能边界：补前缀只生成候选，不会直接接收样本；最终仍要求候选在隔离后的官方
  split 和非空文本 SQLite 中唯一命中，多重命中会阻断。没有降低 shape、长度、finite、
  rotation、位移或 mirror 泄漏校验，也没有按 basename 模糊匹配。
- 实际验证：失败 pilot 明确记录 train/val/test 均为 0、未产生 motion shard，未误报为
  可训练数据；修复后 MotionMillion 专项测试 `29/29` 通过，Ruff、`py_compile`、
  `git diff --check` 通过。T5-3B 同时固定并下载为
  `google-t5/t5-3b@bed96aab9ee46012a5046386105ee5fd0ac572f0` 的 SafeTensors、配置和
  tokenizer，单句 FP16 前向得到 `[1,150,1024]`、输出 finite、峰值显存约 2.33 GiB；
  T5 尚未对 pilot caption 做批量 embedding。

## 2026-09-09：通过 10,000 条 pilot 并增加八卡 T5 分片提取

- 所属分支与修改目标：`feature/smpl-text-only`。真实 MotionGV pilot 在来源命名空间
  修复后完成动作转换、全部 caption 的 T5-3B 特征和 151D 预检；根据实际 caption 规模、
  磁盘占用与 GPU 基准修正全量容量计划，并为全量 embedding 增加互不重叠的多 GPU
  worker 模式。
- pilot 结果：接收 train 10,000 条、拒绝 0 条，共 `1320444` 个原始有效帧；按 30 FPS
  为 `44014.8` 秒/`12.226333` 小时。序列长度 min/p50/p95/max 为 `60/113/261/300`，
  平均 `132.0444` 帧；caption 共 `210823` 条，平均每动作 `21.0823` 条。T5 有效 token
  `6781284`、平均每 caption `32.1658`，仅 2 条发生 150-token 截断。embedding 单卡
  用时 `794.87` 秒，稳定约 `265.23 captions/s`。
- 数据/统计门：pilot motion/embedding SHA、record 顺序、split 与镜像 base 审计通过；
  root 高度均值/标准差约 `0.8304/0.2045 m`，root 速度约 `0.1917/0.2820 m/s`，最大
  约 `4.9490 m/s`；1,000 条样本实际进入现有 151D EnDecoder，`100940` 个有效帧的
  最大绝对 z 为 `5.3035`，`|z|>8` 最大通道比例为 `0`，preflight 状态为 PASS。
- T5 并行实现：新增 `worker-rank/worker-world-size`，按 `shard_id % world_size` 为各卡
  确定可复现且不重叠的 shard；worker 只写原子 shard/meta 与独立 rank 报告，不竞争最终
  manifest。全部 worker 完成后必须由无 worker 参数的单进程 `--resume` 重验所有
  motion/embedding SHA、caption fingerprint 和顺序后发布唯一完整 release。
- 模型身份与镜像修复：固定
  `google-t5/t5-3b@bed96aab9ee46012a5046386105ee5fd0ac572f0`，排除 HF `.cache`
  中含下载时间戳的易变元数据，只指纹化真实模型/分词器文件；pilot 渲染的镜像判断改为
  检查全部 ID 路径分量，覆盖 `Mirror_MotionGV/...` 等来源命名空间。
- 容量修正：metadata 空间报告改为按真实 eligible motion/caption 数分别计算。隔离后的
  caption 为 `15041259` 条，全部 150-token padding 理论上限约 4.20 TiB；pilot 紧凑
  特征为 `13897751474` 字节，按 caption 数外推约 `991541147968` 字节/`923.4 GiB`。
  服务器当前可用约 2.6 TiB，仍满足紧凑特征、动作 shard、checkpoint 与安全余量。
- 性能选择与验证：T5 批量 32/64/128/256 的单卡实测吞吐约
  `342.94/343.32/305.25/297.34 captions/s`，峰值显存约
  `2.717/3.112/3.907/5.491 GiB`，因此全量 worker 使用 batch 64，而非盲目增大。
  多卡分片、缓存指纹、来源命名空间镜像识别均新增测试；MotionMillion 专项测试
  `32/32` 通过，Ruff、`py_compile` 和 `git diff --check` 通过。pilot 视频尚未声称完成：
  首批数据只含非镜像 MotionGV，32 条含镜像的可视化将在全量动作 release 包含
  `Mirror_*` 后生成。

## 2026-09-10：完成 MotionMillion 全量 SMPL 转换并优化八卡 T5 提取

- 所属分支与修改目标：`feature/smpl-text-only`。按用户授权在服务器 1 完成
  MotionMillion 272D → GENMO 66D SMPL pose/3D translation 全量转换，对真实时长、
  拒绝/缺失原因、分片和抽检视频建立可追溯统计；同时优化 T5-3B 全量
  embedding 提取中因“每动作一次小 batch”导致的 CPU 调度瓶颈。
- 全量转换结果：最终闭环接收 `591126` 条动作、`81161176` 帧，固定 30 FPS
  换算为 `2705372.533333` 秒/`751.492370` 小时。其中 train 为
  `510906` 条/`70150203` 帧/`649.538917` 小时，val 为
  `18726` 条/`2559829` 帧/`23.702120` 小时，test 为
  `61494` 条/`8451144` 帧/`78.251333` 小时；输出 1255 个 motion shard，
  占用约 27 GiB。release build fingerprint 为
  `9b955b63cf33d4533aae79e98038a3d79a31f073a56552e829a52fba54181702`，
  `dataset_release.json` SHA256 为
  `2307e6cdd30dc9c4489ccda05294ac66355dad1e14cfc092243c7e7759a0717b`。
- 排除边界：`6175` 条发布动作因实际长度大于 v1 上限 300 帧而显式进入
  `rejected_samples.jsonl`，不静默裁剪；`119288` 条进入
  `unavailable_by_release.jsonl`，其中 `119285` 条缺发布动作、3 条缺文本，
  unresolved 为 0。未把这些条目或 padding 帧计入 751.49 小时。可训练动作
  对应 `12483564` 条 caption。
- 可视化验证：从全量 release 固定抽取 32 条，覆盖走跑 6、舞蹈 6、武术 6、
  地面动作 6、镜像 2 和其他 6；视频保存在
  `/data0/user/liwei/datasets/MotionMillion/work/pilot_render_32_fullrelease`。全部 32 个
  MP4 经 `ffprobe` 确认为 30 FPS 且 0 个解码失败；该目录作为用户授权计划中
  的正式 pilot 审阅产物保留，不属于可清理的临时 smoke 目录。
- 训练前单卡实测：pilot 数据上 batch 2 连续 2 步 loss finite、峰值约 8.1 GiB；
  batch 64 连续 2 步峰值约 21.9 GiB，约 89 samples/s；batch 256 连续
  2 步峰值约 65.2 GiB，约 85 samples/s。这些结果只证明单卡前后向、显存
  量级和 loss finite，不等于八卡 DDP 稳定性、收敛或动作质量；三个 smoke
  目录已精确清理。
- T5 种子与吞吐修复：已使用固定的
  `google-t5/t5-3b@bed96aab9ee46012a5046386105ee5fd0ac572f0` 生成
  train/val/test 各首个完整 shard，共约 1.4 GiB，没有 `.tmp`/`.incomplete`
  残留。首次 8 worker 可正常启动，但平均每动作约 21 条 caption，旧实现按动作
  调用 T5，使 batch 64 长期吃不满且八个进程各自占用大量 CPU。确认尚未写出
  新 shard 后，已仅停止这 8 个本任务进程，GPU compute process 回到 0，没有
  影响其他任务。
- 主要代码修改：新增 shard 级 caption 合批，保持 motion 和 caption 原顺序，将整个
  motion shard 展平后按真实 batch 64 连续编码，再根据 token offset 无损拆回每条
  动作的紧凑 FP16 记录。未改变 150-token 上限、1024 维、attention mask 语义、
  caption fingerprint、shard 对齐、schema 或 resume 契约；旧种子 shard 仍可复用。
- 实际验证：新增跨 batch 边界的 caption 顺序、token offset、动作边界和数值回归；
  MotionMillion 专项测试 `33/33` 通过，变更文件 Ruff、`py_compile` 与
  `git diff --check` 全部通过。全量 T5 将在本提交同步服务器后以 8 worker
  `--resume` 恢复；它完成前不启动正式训练，避免部分 manifest 误进训练。

## 2026-09-10：发布全量 T5 release、通过 preflight 并修复 DDP 分片重复读取

- 所属分支与修改目标：`feature/smpl-text-only`。完成 MotionMillion 全量 T5-3B
  embedding 的单进程恢复发布和全量训练前审计；八卡 smoke 暴露当前 sampler 虽然
  没有重复样本，但会让所有 rank 重复读取同一大型 embedding shard，因此在正式训练前
  修正 I/O/CPU 局部性，并把用户最新的每卡 micro-batch 目标提升到 256。
- T5 正式 release：train/val/test 分别为 `510906/18726/61494` 条、
  `1027/75/153` 个 embedding shard，合计 `591126` 条动作、`12483564` 条 caption、
  `382426092` 个有效 token；150-token 截断 42 条，`.partial` 为 0。模型固定为
  `t5-3b@bed96aab9ee46012a5046386105ee5fd0ac572f0`，embedding release SHA256 为
  `30cf1239f9d51d8188b0748bfde5663d39ced6663935393826c1e476d2c0d981`。
- 全量数据门：开启所有 shard SHA256 校验的 preflight 用时 `867.60` 秒并 PASS，报告
  `/data0/user/liwei/datasets/MotionMillion/genmo_smpl_v1/reports/preflight_full_8d03cc9.json`
  的 SHA256 为
  `744703c41d0758895d301ae6769a886f3f53da5f75245b2c1849833e1f13a391`。151D 统计抽检
  1000 条、102909 个有效帧，最大绝对 z 为 `5.426357`，`|z|>8` 最大通道比例为 0。
- 八卡现象与根因：旧 sampler 将按 shard 排好的全局索引再按 rank stride 切分，一个
  约 512-record shard 会被 8 个 rank 各读一次；每 rank 的 6 个 DataLoader worker
  轮询 batch，又使 worker LRU 无法复用同一 shard。bs64 的 20-step BF16 DDP 虽然
  正常结束，loss epoch 为 `120.9795`、文本 CFG dropout 为 `0.09609`、每卡峰值分配
  `23.456 GiB`，但 20 步墙钟约 5 分 9 秒，周期性 data wait 达 `12.8--16.4` 秒。
  bs128 在首批数据准备阶段即因该已确认的重复解码问题主动终止；只停止本任务 tmux，
  未产生 checkpoint，8 张 GPU 已回到 0 MiB。
- 主要实现：正式大数据模式改为把完整 shard 按大小确定性贪心负载均衡到唯一 rank；
  每个 epoch 只打乱该 rank 的 shard 顺序和 shard 内记录，等长截断仍保证 DDP 无重复、
  不 hang，并显式报告少量丢弃数。shard 少于 rank 的微型 fixture 保留 stride 回退。
  训练 DataLoader 改为每 rank 一个持久 worker，默认每卡 batch 256；同一 512-record
  shard 的连续两个 batch 可在一个 worker 的 LRU 内复用，避免跨 rank 和跨 worker
  重复反序列化。未改变动作、文本、split、loss、151D 或 CFG 契约。
- 静态验证：新增完整 shard 不跨 rank 的 2/8-rank 回归测试，并保留 1/2/8-rank
  样本唯一性与复现测试；MotionMillion 专项测试 `35/35` 通过，相关文件 Ruff、
  `py_compile` 和 `git diff --check` 均通过。修复提交同步服务器后必须重新执行 bs256
  20-step 与 100-step 稳态测试，再按包含数据等待的真实墙钟吞吐换算约四天半正式步数。

## 2026-09-10：确定八卡 batch 256 与约四天半正式训练预算

- 所属分支与修改目标：`feature/smpl-text-only`。用户最终澄清“256”表示优先采用每卡
  micro-batch 256，而非训练 256 个 epoch；训练回合数按服务器 1 的真实端到端吞吐反推，
  目标总墙钟约四天半。正式启动固定设置 `NCCL_CUMEM_HOST_ENABLE=0`、
  `NCCL_IB_DISABLE=1`、`NCCL_SOCKET_IFNAME=lo`、`TORCH_NCCL_BLOCKING_WAIT=1`。
- 八卡容量与健康证据：每卡 batch 256、BF16、梯度累积 1 可稳定完成多个 optimizer
  step，loss 保持有限（约 `119--124`），模型峰值 allocated 约 `66.8 GiB/卡`，
  `nvidia-smi` 峰值约 `73.9 GiB/卡`，低于 80 GiB 门限；因此实际全局 batch 为
  `256 x 8 = 2048`，没有因显存保守地降到 128 或 64。这些短测只证明容量和运行健康，
  不代表模型已经收敛或动作质量合格。
- DataLoader 选择：完整 shard 已由 sampler 固定给唯一 rank。每卡 4 个持久 worker 的
  实测端到端约 `62.5 秒/optimizer step`，显著优于 1 worker；继续提高到每卡 16 worker
  会同时产生 128 个数据 worker，运行约 7 分钟仍不能交付首个 batch，GPU 处于等待，
  因此只终止并精确清理该次 `ddp_smoke_bs256_w16_3a7a192`，没有 checkpoint、没有影响
  其他任务，最终采用每卡 4 worker。
- 正式时长预算：每 rank sampler 提供 `63854` 条记录，batch 256、drop-last 后每 epoch
  为 249 step；配置设为 24 epoch、5976 optimizer step。按 62.5 秒/step，纯训练约
  `103.75` 小时；给 1000-step validation/checkpoint 预留约 4 小时，完整运行目标约
  `108` 小时。总样本窗口曝光量为 `5976 x 2048 = 12238848`，约等于 24 个有效数据
  回合；scheduler 同步使用 warmup 500、total_steps 5976，避免短任务仍沿用 5000-step
  warmup 或 300000-step 余弦周期。
- 日志与保存：训练日志每 10 step，validation 和 checkpoint 每 1000 step；禁用 WandB
  网络上传，保留本地 TensorBoard。仍严格从零训练，`pretrain_ckpt=null`、
  `ckpt_path=null`、`resume_mode=null`，只启用 MotionMillion 文本交叉注意力和既定
  151D SMPL/人体物理监督。
- 视频交付：服务器正式 pilot 目录中的 32 个 MP4 与 manifest 已复制到本地
  `/home/weili/GENMO/outputs/motionmillion_pilot_render_32_fullrelease`；逐文件 SHA256 与
  远端完全一致，32 个视频均为 30 FPS，FFmpeg 全量解码失败数为 0。该用户要求保留的
  审阅产物不作为 smoke 临时目录清理，也不纳入 Git。

## 2026-09-10：阻止 Lightning 对 MotionMillion sampler 二次分片

- 所属分支与发现：`feature/smpl-text-only`。提交 `c8b5040` 的首次正式会话已成功注册
  8 个 NCCL rank、进入有限 loss，且每卡显存约 `73.9 GiB`；但运行进度明确显示每 epoch
  只有 31 个 batch，而不是 `63854 / 256 = 249`。这说明 Lightning 默认把已经按 rank
  分片的 `ShardAwareDistributedSampler` 又做了一次分布式切分。
- 处置边界：发现后立即只停止会话 `mm_text_bs256_24ep_c8b5040`，该会话只运行 3 step，
  没有产生 checkpoint，不能作为有效训练或恢复源；诊断目录
  `/data0/user/liwei/GENMO_outputs/motionmillion_text_only_bs256_24ep_20260910_111330_c8b5040`
  暂时保留用于审计。8 张 GPU 已确认无残留 compute process，未影响其他任务。
- 修复：在文本专用实验配置中显式设置 `pl_trainer.use_distributed_sampler=false`。自定义
  sampler 仍从 Trainer 取得 `rank/world_size`，完整 shard 唯一归属和 rank 等长截断逻辑
  不变；该开关只避免框架重复包 sampler，不改变数据 release、模型、loss 或批量大小。
- 新增验收：Hydra 合同测试要求该开关严格为 false；重新启动后除了核对
  `per_rank_samples=63854`，还必须以进度条每 epoch 249 batch 作为运行时门，防止配置
  看似正确但实际数据曝光缩小 8 倍。

## 2026-09-10：按正确 sampler 稳态吞吐重算四天半正式步数

- 所属分支与修改目标：`feature/smpl-text-only`。二次分片修复后的独立全新运行确认
  每个 epoch 为 249 batch，并连续完成 120 个 optimizer step；据此替换此前受错误数据
  局部性影响的 62.5 秒/step 估算，使训练预算真正以正确数据路径的端到端墙钟为准。
- 120-step 真实结果：进度从 `5/249` 连续到 `120/249`，平均约 `1.7--1.8 秒/step`；
  8 张 GPU 在采样时均达到 100% 利用率，每卡峰值 allocated `66.8 GiB`、
  `nvidia-smi` 约 `73.9 GiB`；loss 从约 124 降至约 55.7，全程没有 OOM、NaN/Inf、
  NCCL error 或 DDP hang。loss 下降仅作为启动健康证据，不解释为已收敛或质量达标。
- 最终预算：保持每卡 batch 256、8 卡、累积 1，即全局 batch 2048；设
  `max_steps=215000`、`max_epochs=864`，后者只是覆盖总步数的上界，最终按 step 停止。
  纯训练按实测约 `101.5--107.5` 小时，另给验证和 checkpoint 留出余量，目标总墙钟约
  四天半。样本窗口曝光量为 `215000 x 2048 = 440320000`，相当于约 863.45 个 sampler
  epoch；长动作每次随机裁剪、每动作 caption 均匀随机选择，因此每轮并非固定重复同一
  文本窗口配对。
- 调度与保存：恢复与长训练匹配的 warmup 5000、cosine total_steps 215000、每 5000
  step 验证、每 10000 step checkpoint、每 10 step 日志。预算探测会话
  `mm_text_bs256_24ep_3c55ce6` 在 120 step 后按计划停止，未产生 checkpoint，目录保留
  为吞吐审计证据，不能用作正式恢复源；GPU compute process 已清空。

## 2026-09-14：回传服务器1 MotionMillion s190000并审计文本动作验证入口

- 所属分支与目标：`feature/smpl-text-only`。按用户要求下载服务器1当前最新已完整保存的
  SMPL纯文本模型，不等待训练结束，并分析现有验证脚本。选择时本地、服务器及GitHub均为
  `eb1ebda315401558b1303662030c31e3e991efb1`，工作树干净；本次不修改训练、生成或评测源码。
- 模型来源：服务器1 `ZP-NC578`，实验目录
  `/data0/user/liwei/GENMO_outputs/motionmillion_text_only_formal_bs256_215k_20260910_112945_eb1ebda`，
  文件 `version_0/checkpoints/s190000.ckpt`。2026-09-14 14:01锁定最新完整编号，
  远端计算SHA256期间大小和mtime不变，无临时checkpoint兄弟文件。
  14:06的训练事件仍更新到198609步；下载固定190000步，不追逐训练内存进度或last.ckpt。
- 本地归档：`/home/weili/GENMO/inputs/checkpoints/motionmillion_text_only_s190000_20260914/`，
  模型为 `s190000.ckpt`，大小 `5278575218` 字节，14:21完成下载和验收。
  两端SHA256一致：`478796fdc6bba0f2b267a344388fd0914bb69402fe7a7bfdbf2ad0ad5d3d395e`。
  单连接较慢，保留本轮108724224字节前缀后改为8条交互认证SSH连接分块只读传输，
  合并后按整个原始checkpoint哈希验收；没有转换、裁剪或覆盖旧模型。
- 配套归档：`training/.hydra/`、`training/version_0/`、`training/releases/`共7份配置及
  dataset/T5 embedding release均与服务器逐文件哈希一致。保存capture/transfer/checkpoint/config/T5/
  验证脚本审计JSON与中文README，便于复查来源、边界和单条文本生成命令。
  完整数据集、训练日志和T5权重不随本次checkpoint重复复制。
- 实际模型验收：本地 `.venv/bin/python`（Torch 2.6.0+cu124）CPU mmap读取通过，
  `global_step=190000`、`epoch=763`、550项state_dict、502970528个张量元素均有限，
  保留optimizer_states与lr_schedulers。现有
  `scripts/demo/demo_smpl_text.py:validate_text_generation_checkpoint`对真实文件通过；
  契约为schema1、150 token、1024维、text_only=true、pipeline_in_attr=[]，
  正确选择 `gem_smpl_motionmillion_text_only`。
- 兼容性检查：本地demo按MotionMillion实验组合的model.model_cfg、network、pipeline、
  endecoder与服务器实际保存配置逐项一致；归一化/编解码源码哈希一致。
  本地T5快照 `bed96aab9ee46012a5046386105ee5fd0ac572f0` 的4个文件（含11.4GB
  model.safetensors）均与训练embedding release的SHA256一致。
- 验证脚本结论：已有单条文本demo、完整val预测生成、SMPL转272D、官方evaluator数据准备/
  指纹化、FID/Diversity/R-Precision@1/2/3/Matching Score、20-seed均值与95%CI汇总、
  126提示人工评分网页等入口；上述8个相关Python脚本的 `--help` 全部通过。
  `--dry_run`仅构造默认占位输入，不读取checkpoint，不能代替本模型150-token契约验证。
- 当前评测缺口：训练每5000步仅验证16条，VisText明确跳过dataset_id=motionmillion；
  14:06实际TensorBoard共29个scalar tag，无val/*或validation/*，不构成质量报告。
  `configs/global/task/test_text.yaml`默认是HumanML3D，不是MotionMillion官方评测。
  本地未配置完整MotionMillion数据集；服务器约定的official_evaluator位置缺少code、
  epoch=199.ckpt、eligibility.json、evaluator_identity.json及126提示文件。
  完整官方指标需先补齐资产，按原始val 60--200帧资格集合、120帧生成和20个不同seed运行。
- 验证边界：本次完成下载、完整性、CPU可读性、权重有限性、真实文本契约、配置/T5身份及
  脚本入口检查；没有启动GPU推理、视频渲染、完整验证集生成或官方指标，不宣称文本动作
  质量、泛化或最佳checkpoint已验证。服务器训练未停止、重启或调整。
- 清理：传输分块、rsync partial及assembling文件已精确删除并检查无残留；
  本轮临时目录 `/tmp/genmo-s1-capture-20260914.6W3MdG` 确认为空后删除。
  `--help`及CPU检查禁用pyc写入，没有生成pytest/ruff缓存或正式实验内测试目录。
  模型与README/JSON是本任务交付物，保留在上述忽略目录，不纳入Git；本条为唯一受跟踪修改。

## 2026-09-14 16:13：实现 GENMO 本地文本动作网页并完成真实 GPU 与浏览器验收

- 所属分支与目标：`feature/smpl-text-only`，基于 `8fff648`。实现用户确认的本机中文
  工作台，选择文本 GENMO checkpoint、输入 prompt / num_frames / ddim_steps，生成
  SMPL 动作与可手动播放的视频，保留历史并支持回看及参数复用。
- 页面与入口：新增 `scripts/demo/demo_smpl_text_web.py` 及
  `gem/runtime/text_motion_web/` 的 Flask 接口、HTML/CSS/JavaScript。深色参数区、
  大尺寸预览、阶段进度及耗时、历史缩略图和下载入口；仅监听 `127.0.0.1:8766`。
  无 Node 构建和云端部署。`setup.cfg` 新增可选 `web` 依赖 Flask 3.1–3.x，并声明
  页面静态资源；`docs/text_motion_web.md` 说明启动、接口、产物、恢复及边界。
- 模型发现：后台扫描 `inputs/pretrained`、`inputs/checkpoints`，按真实路径去重。
  CPU mmap 读取 checkpoint，复用文本层/交叉注意力契约，再检查 SMPL 151 维输出和
  扩散条件层，排除已知 BUMI、音乐及回归模型。文件身份、大小、mtime/ctime 用于
  缓存失效，生成前复核文件身份。默认 s190000；本地路径注册后原子持久化。
  将 demo 中原有检查主体提取为 `validate_text_generation_payload`，原文件校验函数
  仍按原方式读取与清理，避免网页为检查元数据重复读取数 GB 权重。
- 常驻推理：HTTP 与 GPU 通过 spawn 进程及消息队列隔离；任务锁同时只接受一个任务，
  提交时冻结四项参数。同模型复用 T5/GEM；切换模型先 close 再初始化，自动处理
  50/150 token。`ResidentTextMotionEngine.set_ddim_steps` 使用原 generation_lock，
  更新 test 与 gen_only 两处 respacing 并重建采样器；相同步数不重复初始化，初始化
  失败尝试恢复旧采样器。原 CLI、stdin/ZMQ 请求字段和默认参数未更改。
- 固定参数：T5 路径为用户指定的本地 `bed96aab9ee46012a5046386105ee5fd0ac572f0`
  快照，local_files_only=true；FPS=30、CFG=2.5、seed=42、shape_mode=zero，启用
  原默认后处理，1280×720。帧数限制 1–900，DDIM 限制 2–1000，拒绝布尔/小数/
  字符串冒充整数及额外覆盖字段。任务保留 prompt 原文；T5 输入沿用原服务 strip。
- 渲染与成功门槛：每任务独立子进程复用 Open3D 人体/地面/相机，转为 H.264、
  yuv420p、faststart MP4，生成 JPEG 缩略图。检查动作各数组形状与有限性、零体型、
  FPS；视频逐帧解码，核对帧数、FPS、分辨率及编码后才 done。原渲染函数仅 warning
  且未产视频时必须失败。渲染超时/原生崩溃隔离到任务，GPU 工作进程退出后可重建；
  Linux 父进程退出会结束 GPU 和渲染子进程，避免残留显存占用。
- 持久化与访问：`outputs/text_motion_web/tasks/<id>/task.json` 原子保存阶段及参数，
  动作和视频在独立 artifacts 子目录；history.json 为原子索引，重启从每任务记录恢复。
  中断任务标 failed，不自动重跑；文件锁禁止两个服务争用同一结果根目录。原动作 READY
  契约保持原义，网页 done 额外等待视频。媒体只按任务 ID 映射，支持 HTTP Range，
  限制回环 Host 和同源 JSON，不提供任意本地文件读取接口。
- 自动测试：新增 `tests/test_text_motion_web.py`，补充常驻引擎 DDIM 测试。最终运行
  `test_text_motion_web.py`、`test_resident_text_motion.py`、
  `test_text_motion_server_protocol.py`、`test_demo_smpl_text.py`、
  `test_motionmillion_text_only.py`，127 passed（12.54 秒），3 条既有 autocast
  弃用警告。覆盖参数边界、真实小权重契约、文件变更、路径去重、并发拒绝、冻结原文、
  加载/渲染失败、worker 退出后重建、历史恢复、媒体约束、DDIM 复用/更新/回滚。
  初次 124 passed / 1 failed 原因为小夹具同长度快速写入处于同一文件系统时间 tick，
  测试显式推进 mtime 后通过；没有放松文件变化检查。新增模块 Ruff 与 git diff --check
  通过，启动入口 --help 通过；环境无 node CLI，JavaScript 实际加载和交互由浏览器验证。
- 实际 GPU 验收：本地 RTX 4090、仓库 `.venv`、真实 T5 与 checkpoint，通过完整 HTTP
  任务链路生成并渲染以下 5 条，全部成功。前 3 条通过浏览器按钮提交：
  1. s190000 / 150 token，步行文本，120 帧、20 DDIM：17.415 秒，首次加载 10.366 秒。
  2. 同模型抬手文本，240 帧、50 DDIM：10.835 秒，engine_reused=true，更新约 0.002 秒。
  3. 切换官方 gem_smpl / 50 token，右手挥手文本，120 帧、50 DDIM：19.134 秒。
  4. 再切回 s190000，站立文本，1 帧、2 DDIM：10.809 秒。
  5. 同模型慢走文本，900 帧、2 DDIM：25.928 秒，engine_reused=true。
  5 条的动作数组有限、体型为零，视频均为 1280×720 H.264/yuv420p、30 FPS，
  解码帧数精确等于请求值；各视频 Range bytes=0-1023 均返回 HTTP 206 和 1024 字节。
- 浏览器验收：真实页面确认模型列表/默认项、生成按钮禁用和阶段状态，视频默认 paused=true、
  currentTime=0、autoplay=false；手动播放、暂停、进度拖动成功。下一任务生成期间旧 src
  保留，新 240 帧视频完成后 duration=8、暂停并归零。历史回看/参数复用、页面刷新恢复、
  真实旧模型路径注册去重、空白文本错误保留已验证。服务真实重启后 5 条历史及视频恢复，
  未自动生成；浏览器控制台未发现 JavaScript 错误。
- 清理与交付运行：全部测试输出集中在系统临时目录
  `/tmp/genmo-web-acceptance-9lcgOZ`；完成验收后先关闭该服务，复核路径及小型 checkpoint
  夹具后精确删除整个目录，残留检查通过。测试禁用 pyc/pytest/ruff 缓存，无本轮新增源码
  pyc；15:22–15:24 已存在的 pyc 非本轮产物，保留。正式服务随后以后台进程启动，
  PID 写入 `outputs/text_motion_web/server.pid`，日志写入同目录 `server.log`，初始历史为空，
  首页与模型接口已检查正常；正式用户生成结果持久保留。浏览器正式页面保持打开。
- 验证边界与同步：本轮证明网页功能、模型加载/切换和动作视频链路可用，不等价于官方
  FID/R-Precision 质量评测或训练收敛结论；未运行完整验证集。同步前已只读确认服务器1
  工作树干净、对应分支为 feature/smpl-text-only；按长期授权提交推送后仅同步该分支代码，
  不启动、停止或调整服务器训练；服务器2的 BUMI 分支不纳入此次网页修改。

## 2026-09-15：只读分析服务器1已完成训练并回传最新SMPL文本模型s210000

- 所属分支与目标：`feature/smpl-text-only`。按用户要求，只读分析服务器1的训练状态、
  完整日志、TensorBoard 和保存配置，将最新实际落盘的 SMPL 文本生成动作模型传回本地。
  本次开始时本地和服务器1均为 `b4e03453bb9e989ef0533c05d2bf5db6e16e67e8`，工作树干净。
- 正式实验：`motionmillion_text_only_formal_bs256_215k_20260910_112945_eb1ebda`，目录位于
  `/data0/user/liwei/GENMO_outputs/`。日志明确记录 `max_steps=215000` reached，以及8个
  `End of script`；训练于2026-09-14 22:15正常结束，用时4天10小时44分52秒，最后epoch索引863。
  2026-09-15 14:59只读快照中无对应训练进程/tmux/GPU计算进程，8张RTX 6000D均为0 MiB、0%。
  服务器本次启动于9月15日06:50，晚于训练结束，未据此误判为训练中断。
- 保存缺口：最新编号文件为 `s210000.ckpt`，并无 `s215000.ckpt`；CPU mmap读取确认
  `last.ckpt` 同为step 210000、epoch 843，且与s210000大小及SHA256完全相同。
  保存配置每10000步触发；`SimpleCkptSaver` 没有训练结束补存钩子，未设置按epoch保存。
  当前回调源码与启动提交eb1ebda一致，故最后5000步约2.5小时训练没有落盘，不能将last
  当作215000步最终权重。未修改回调或尝试重启训练。退出标记文件实际为字面`b'0n'`，
  正常结束结论采用主日志及8个rank结束标记，不把该非规范标记单独解析为整数退出码。
- 正式本地归档：
  `/home/weili/GENMO/inputs/checkpoints/motionmillion_text_only_s210000_20260915/`，
  模型文件 `s210000.ckpt` 大小5278575218字节，SHA256为
  `c07c148f9a0ddad7393e820a932c56e62a7b1aa555ee4c997b818a7164eadb15`。
  9月15日15:11完成传输和CPU验收。下载使用SSH密钥，先8路读取并保留已完成的512 MiB，
  再提高为最多24条错峰建立的只读连接继续传输；完整SHA通过后将 `.downloading` 原子改名。
  仅管理本次自建下载进程，未干预训练进程；没有持久化任何凭据。
- 文件检查：下载后再次计算远端源文件SHA并检查mtime/大小/最新编号，确认源文件稳定；
  本地SHA与远端完全一致。CPU mmap成功读取550项state_dict、502970528个张量元素，
  全部模型权重有限，优化器与调度器状态存在；现有文本demo真实payload校验器通过。
  文本契约为schema 1、150 token、1024维、text_only=true、pipeline_in_attr=[]。
  当前保存配置与上次本地s190000归档逐字节一致。
- 配套归档与实现：复制完整28 MB TensorBoard事件、约1.4 MB主日志、Hydra配置、
  dataset_release和embedding_release等10个文件，逐一比对远端/本地大小与SHA。
  正式归档内新增有中文文件说明的下载、完整事件分析和CPU检验脚本；新增中文README、
  capture_manifest、传输/契约/服务器快照/训练分析JSON、全部417917条标量压缩JSON及
  中文训练曲线PNG/PDF。动作/文本release指纹、三个split记录数与150×1024契约一致。
  这些文件是用户要求保留的模型分析产物，位于Git忽略目录，不将模型、日志或数据纳入Git。
- 实际训练配置：纯文本MotionMillion从零训练，train/val/test为510906/18726/61494条；
  120帧、30 FPS，SMPL/GVHMR 151D；预计算T5-3B FP16文本嵌入、150 token、1024维。
  8卡每卡batch256、累积1，全局2048，BF16 mixed；分片感知sampler且禁用Lightning
  二次分片，每epoch249个batch。AdamW最大学习率2e-4，5000步预热，余弦降至2e-6。
- 完整数值审计：29项scalar、417917点全部有限；完整日志无Traceback、NaN、OOM、
  CUDA/RuntimeError或NCCL失败，只有6种去重后的autocast弃用提醒。按已完成更新次数
  换算TensorBoard的0基step，45001–50000/95001–100000/145001–150000的loss均值为
  13.709156/13.171252/12.853675；185001–190000/205001–210000/210001–215000分别为
  12.649207/12.650479/12.621397。最后epoch loss为12.622966；末两个5000步窗口仅下降
  约0.23%，属于平台附近的小幅改善，不能据此宣称比s190000生成质量更好。
- 其他指标：实际文本CFG丢弃率全程均值9.993%；末5000步裁剪前L2梯度范数均值8.6394，
  500个记录均大于0.5阈值，但没有持续上升或非有限值。真实末段wall time为8933.39秒/
  5000步，即1.7867秒/步、约1146全局样本/秒；计算计时约1.237秒、数据等待约0.510秒。
  PyTorch峰值allocated显存66.775 GiB，明确区别于驱动总显存占用。
- 质量边界：有43次完整验证循环，但每次只有16条；VisText对motionmillion直接返回，
  TensorBoard没有val/validation质量标量、FID/文本检索指标或图像。结论限定为预算完成、
  训练数值稳定、后期loss变化较小，不认定最佳checkpoint、不过拟合或语义/泛化质量。
  本次未执行GPU推理或官方质量评测。现有文本网页首页HTTP 200，可通过本地路径框添加
  此模型；本次未改变网页默认模型或启动生成任务。
- 验证与清理：三个归档脚本AST语法/中文说明检查通过；事件计数、43轮验证、8个结束
  标记、215000最终步数、全部标量有限和10文件指纹断言通过；真实CPU模型检查通过；
  训练曲线PNG已实际查看，标注和中文正常。Matplotlib/CPU检查临时目录使用系统tmp并
  自动删除，归档内无新增pyc、pytest/ruff缓存，无遗留下载中间文件；正式模型与分析产物
  持久保留。`git diff --check`通过后，按长期授权提交中文记录、推送并同步服务器1对应分支，
  不涉及服务器2分支、训练任务或参数变更。

## 2026-09-15：将s210000注册到正在运行的本地文本动作网页

- 所属分支与目标：`feature/smpl-text-only`。用户确认网页能否选择刚传回的模型；实时读取
  `http://127.0.0.1:8766/api/models`，发现运行中的列表原先仅含s190000和官方gem_smpl。
- 操作与原因：通过网页已有POST `/api/models` 接口注册本地
  `inputs/checkpoints/motionmillion_text_only_s210000_20260915/s210000.ckpt`，
  由已有CPU权重检查器验证并写入正式运行目录 `outputs/text_motion_web/models.json`；
  不修改应用源码，用户刷新页面即可载入新的下拉选项，默认项仍为s190000。
- 实际验证：注册返回HTTP 201；随后GET模型列表出现唯一s210000记录，global_step=210000、
  max_text_len=150；模型路径已持久化。检查前端loadModels在页面初始化时读取接口并生成
  下拉选项。未提交生成任务或重启服务；此次未产生临时测试文件。按同步约定提交本条记录。

## 2026-09-16：复核服务器1收敛状态、最新模型及文本动作网页

- 所属分支与目标：`feature/smpl-text-only`。按用户要求重新检查训练是否完成、是否收敛，
  给出下一步建议，并确保最新模型在本地与文本生成动作网页可用；本次开始时两端均为
  `fc7ef2ceb28bcfba482d11408aad493b8f4a2bfa`且工作树干净。
- 2026-09-16 11:07–11:12实时核验：服务器1无训练/GPU计算进程、8卡0%利用率，约3 MiB
  驱动显存占用；原正式MotionMillion实验正常完成215000步，日志及事件没有新增，最新
  落盘仍为s210000，last同为210000且SHA相同，没有s215000。保存间隔和最终补存缺口
  保持此前结论，未通过重新命名或重新启动训练伪造最终模型。
- 新增正式审计产物：`/home/weili/GENMO/outputs/server1_smpl_convergence_20260916/`，
  包含本次服务器快照、文件/模型/事件/网页复核JSON及中文README。报告解释训练平台与
  泛化/生成质量收敛的证据区别，并列出候选checkpoint对照、补齐验证链路、条件性续训、
  学习率与保存逻辑建议；报告属于用户要求保留的产物，不纳入Git。不修改训练/推理源码。
- 实际完整性与模型检查：重新计算远端s210000/last及本地s210000的完整SHA256，均为
  `c07c148f9a0ddad7393e820a932c56e62a7b1aa555ee4c997b818a7164eadb15`，大小5278575218；
  本地已经是最新正式副本，因此未重复下载5.28 GB或创建重复checkpoint。
  配套保存配置、完整事件、主日志亦逐文件与远端SHA/大小一致；CPU mmap再次读取550项
  状态、502970528个张量元素，全部有限；真实demo契约检查通过，150 token、1024维纯文本。
- 收敛复核：重新读取29项标量、417917个记录点，全部有限；末epoch loss=12.622966；
  185001–190000、205001–210000、210001–215000窗口均值为12.649207/12.650479/12.621397。
  末两个5000步窗口仅改善0.2299%，190k与210k附近均值几乎相同。学习率最终2e-6，是
  峰值2e-4的1%；s210000优化器中实际lr=2.2768240790631672e-6，scheduler.total_steps=215000。
  判定限定为当前学习率日程下进入后期训练平台，不宣称数学最优或生成质量最佳。
- 验证缺口与建议：训练有43次16条验证循环，却无val/validation损失或FID/文本匹配指标；
  服务器约定official_evaluator目录的code、epoch=199.ckpt、eligibility、identity和126提示
  资产本次仍不存在。建议先固定文本/采样配置比较s190000与s210000、再补齐独立验证与
  官方多seed指标，依据质量变化决定续训。只增加max_steps会在215000步以后保持最小lr，
  续训须单独检查调度器恢复；下一轮应补最终checkpoint保存。没有实际启动训练或评测。
- 网页接入：对运行中的POST `/api/models`执行幂等注册，返回201；GET确认唯一s210000条目、
  global_step=210000和150-token契约，models.json已持久保存，首页HTTP 200。核验时历史
  共61条完成任务，其中20条来自s210000；这些记录仅证明已有生成链路可用，不作质量评分。
  用户刷新后可选择 `motionmillion_text_only_s210000_20260915 / s210000.ckpt · step 210000`，
  默认仍为s190000；未自动提交新任务或重启网页。
- 实际验证与清理：模型/文件指纹、完整事件计数与有限性、43轮验证、215000最终步数、
  网页去重与持久化等断言通过。仅生成正式审计结果，没有测试缓存、下载中间文件或新pyc。
  `git diff --check`后按长期同步授权提交记录、推送并在服务器1对应分支ff-only同步；
  保留全部已有模型及用户网页结果，不涉及服务器2或训练参数调整。

## 2026-09-16：固定测试与渲染复用/清理规则，清理九个临时目录并确认使用本地 SMPL 音乐 v3

- 修改目标与分支：按用户要求记住临时测试必须清理、同一功能复用原测试/渲染脚本、同一
  功能仅保留最新一套有效测试文件/视频/网页，不同功能保留各自模型与结果。当前分支为
  `feature/smpl-text-only`，起始 HEAD `7e51251`，任务开始时工作树干净；未切换或合并分支。
- 规则实现：新增本地 `agent.md`，登记九个指定目录、脚本参数化复用、完整结果集的保留
  范围、先验收新结果再清理旧结果以及 checkpoint/训练日志/生产数据保护；`AGENTS.md`
  追加相同公共核心规则并要求读取本地 agent.md。按其他功能分支 agent.md 的既有本地
  保留约定，在 `.git/info/exclude` 增加精确 `/agent.md`，不改变公共 `.gitignore`。
  同时按用户明确的记忆更新要求新增记忆补充笔记；没有改写既有记忆或历史记录。
- 临时目录清理：逐项解析绝对路径、确认非软链接、没有 Git 跟踪文件、checkpoint、ONNX、
  TensorRT engine 和 TensorBoard 事件后，删除以下九个用户点名目录：
  `outputs/AIOZ-GDANCE-extracted-smoke3-local/`、`outputs/CoMPAS3D-smoke5-local/`、
  `outputs/FineDance-smoke10-local/`、`outputs/FineDance-smoke10-renders/`、
  `outputs/aistpp_official_dryrun/`、`outputs/beat2_build_dryrun/`、`outputs/humanml3d_test/`、
  `outputs/gem_smpl_music_only/inference_smoke/`、`outputs/gem_smpl_music_only/eval_smoke/`。
  共删除 120 个临时文件、72591274 字节；九项残留检查全部不存在。没有删除 tests/ 源码、
  父实验目录、正式模型或其他功能交付；全量历史网页/视频未在本次泛化批量清理。
- 模型选择与取消回传：先前只读服务器检查发现 SMPL 音乐 physics_v4 的最新保存文件为
  s110000，曾开始下载到新建的 `inputs/checkpoints/smpl_music_physics_v4_s110000_20260916/`。
  用户随后明确改为使用本地 physics_v3 s100000、不回传服务器模型。本轮立即核对并终止
  自己发起的分段下载进程；配套日志 rsync 已退出，删除本次下载目录中的 checkpoint 部分
  文件及元数据，检查对应 SSH/rsync 下载进程和本地下载目录均无残留。没有改动服务器训练。
- 本轮采用模型：`outputs/gem_smpl_music_only_4set_manual_q1_physics_v3_100k/version_0/checkpoints/s100000.ckpt`，
  大小 2570404846 字节，重新计算 SHA256 为
  `98d70a145fb8f430ab557cdd0bde4af5f71e881b16f6976b385db6d172683136`。
  CPU mmap 读取通过，global_step=100000、epoch=2941、316 个 state_dict 条目，浮点模型
  参数无非有限值。原文件原地保留，不重复复制，不将本轮选用版本称为服务器最新版本。
- 既有 ONNX：`outputs/onnx/smpl_music/physics_v3_s100000/music_only_denoiser.onnx`，
  大小 874074147 字节，SHA256 为
  `dcbed23134e2b4578a7c7aca92d9416392287372b658a58a97e67f53466e35c6`，与旁边 `.onnx.json`
  中的大小和哈希一致；manifest 对应 physics_v3、step=100000、151D 音乐动作模型。
- 实际验证与边界：只读检查使用 `python -B` / `PYTHONDONTWRITEBYTECODE=1`，不生成测试
  缓存；路径残留检查、下载进程残留检查、模型 CPU 加载/有限性、ONNX 清单大小与哈希、
  文档 `git diff --check` 均通过。没有修改 Python 实现，没有运行新的推理、渲染、模型质量
  评测或训练；未把文件/模型静态检查等同于新一轮 ONNX 数值一致性或生成质量验证。
- 源码定位说明：长期实现位于 `scripts/`、`scripts/demo/`、`tools/eval/` 和 `gem/`；
  `tests/` 验证实现及契约，`tools/eval/` 面向真实模型/动作执行检查、指标或渲染。规则登记
  不代表已经给所有旧入口增加了自动清理逻辑，也未为此次模型创建重复的测试/渲染脚本。

## 2026-09-16：开放文本动作工作台的匿名公网 HTTPS 入口

- 修改目标与分支：`feature/smpl-text-only`，起始 HEAD `7e51251`。用户明确选择“外网公开，
  任何人无需密码即可使用”。保留开始时已有的 AGENTS.md 测试复用/清理规则及上一条记录，
  检查后纳入本次常规同步；本地忽略的 agent.md 不强制加入 Git。未改动服务器训练任务。
- 主要实现：新增 `gem/runtime/text_motion_web/share.py` 和可复用入口
  `scripts/demo/share_smpl_text_web.py`，用 Waitress 在 127.0.0.1:8768 启动轻量网关，
  仅转发到既有 127.0.0.1:8766；不创建第二份 GPU 引擎或历史。复用原 HTML/CSS/JS，
  模板区分本地与公开入口，app.js 兼容公开列表无本地路径、禁止注册模型的能力声明。
  公网保留模型选择、文本/帧数/DDIM、生成状态、历史、视频播放和下载，本地保留路径注册。
- 接口与功能边界：显式路由白名单；删除公开 JSON 中的模型路径、任务目录、权重指纹与
  内部异常详情；不向上游转发访客 Cookie、Authorization、代理头。上游只允许回环 HTTP，
  禁用环境代理和重定向；公开 POST 校验 Origin、JSON 与四个参数字段，prompt 最多
  4096 字符，请求体最多 16 KiB。媒体流式转发 Range、HEAD、ETag 和条件请求。
  所有访客共用原服务的单任务互斥和历史，页面明确提示文本/历史公开；原推理参数、
  50/150-token 契约、默认 s190000、最新可选 s210000 和现有 CLI 不变。
- 运行依赖与交付：setup.cfg 的可选 web 依赖增加 waitress>=3.0,<4，本地安装 3.0.2；
  从 Cloudflare 官方 release 下载用户目录 cloudflared 2026.9.1，完整 SHA256 与官方
  asset digest 相同：03f1f25d1cc93b9ad6c60569d44060bc4f17ed97075760ed8cfca4b12dcd68cc。
  已运行 Quick Tunnel 和网关，公开入口为
  `https://updating-desperate-injured-buried.trycloudflare.com`。启动信息、PID、日志保存于
  `/home/weili/GENMO/outputs/text_motion_web/share/`，作为当前正式交付配置保留；
  重建分享入口时按新 PID/URL 更新，停止使用后可删除该分享目录，不删除用户历史。
  docs/text_motion_web.md 补充启动/停止命令及边界：需本机和三个服务持续运行，随机
  临时域名在隧道重建时可能变化；固定域名需另行配置正式隧道。未配置开机自动启动。
- 实际接口测试：新增 tests/test_text_motion_web_share.py，以系统临时媒体和回环 HTTP
  假后端验证转发、并发 409、字段限制、路径隐藏、管理拒绝、错误泛化、Range/HEAD/304/416
  及禁止重定向；连同已有 test_text_motion_web.py 共 43 项通过（17.17 秒）。新 Python
  文件 Ruff 检查通过；测试运行禁用字节码、pytest/ruff 缓存。临时根目录
  `/tmp/genmo-web-share-tests-h_co0a1p` 已清理并检查不存在。
- 实际公网验证：本机和服务器1外部网络均在无认证状态获得首页、模型、历史 HTTP 200；
  本机 bytes=0-1023 与外部 bytes=1024-2047 的真实视频请求均返回 HTTP 206、正确
  Content-Range 和 1024 字节。公开 HTML/模型/历史未包含 /home/weili 本机路径，静态
  JS/CSS 均 200；公开提交合法格式的不存在模型返回 400，未产生新任务。
- 视频与证据边界：从公网完整下载既有 s210000 成功任务 2c1063bcfe99491aa706e008c3a4de4c
  的视频，ffprobe 确认 H.264、yuv420p、1280×720、30 FPS、120 帧；ffmpeg -xerror 完整
  解码通过。临时目录 `/tmp/genmo-web-share-media-z89g9i5j` 已清理并检查不存在。
  浏览器已打开公开页面，但自动 DOM/截图控制反复超时，本轮不声称完成手动播放、暂停
  或拖动 UI 验收。没有提交新的真实 GPU 生成或重做质量评测；生成转发由 HTTP 契约测试
  覆盖，既有 61 条完成历史（其中 20 条 s210000）保持不变，正式服务未重启。
- 保留与同步：用户原计划明确要求持久保留网页生成历史，因此
  `/home/weili/GENMO/outputs/text_motion_web/tasks/` 和 history.json 不按临时测试清理。
  没有新下载 checkpoint、修改模型或触碰本机其他训练进程。检查差异和记录后按长期
  授权中文提交、推送当前分支，并在服务器1同分支以 pull --ff-only 同步代码；服务器仅
  同步源文件，不安装分享服务或更改训练环境。

## 2026-09-16：实现 MotionMillion s210000 保留 AdamW 动量的八卡续训阶段

- 修改目标与分支：`feature/smpl-text-only`，起始 HEAD `481df28`。用户明确授权按
  s210000 追加 20000 步、1000 步升温至 1e-5 后余弦降至 2e-6，并在服务器1启动八卡。
- 主要实现：在 gem/utils/lr_scheduler.py 增加 ContinuationWarmupCosineLR，完整恢复
  AdamW 后从真实 LR 开启新阶段；阶段内恢复严格校验阶段身份。新增专用继承配置，
  max_epochs=-1、目标 230000、每 5000 步保存；SimpleCkptSaver 增加可选结束补存，
  默认关闭以兼容旧实验。原训练数据、loss 权重、batch、BF16、dropout 和 clip 保持。
- 质量监控：新增 gem/callbacks/motionmillion_quality.py，复用 GEM 原训练/生成路径
  和 SMPL FK；固定 val 32 条 × 4 seed，保留基线/最新整套动作、指标历史及 cohort
  指纹。按动作配对 bootstrap 验证 loss 改善，并以多样性、脚滑代理和接触覆盖守卫，
  连续三次无可靠改善即正常早停。阶段恢复保存监控状态，固定 RNG 不污染训练随机流。
- 功能边界：服务器 official_evaluator 目录为空；本轮指标是固定验证 loss 与运动学
  代理，不是官方 FID、文本检索、动力学或硬件验证。候选 s210000 不宣称官方最优。
  baseline.npz 是本阶段固定对照，latest.npz 原子替换；history.json 属于训练记录。
- 验证与运行：新增 tests/test_training_continuation.py 覆盖真实 Lightning CPU 状态
  恢复、阶段再次恢复、末尾补存、RNG、指标/早停和原子交付；新增可复用的八卡 CUDA/
  大张量通信检查 tools/train/preflight_distributed.py。操作说明见
  docs/motionmillion_continuation.md。实际本地测试、服务器 smoke/清理及正式启动证据
  在完成后按时间顺序追加，不将实现完成视为已启动或质量通过。

- 本地实际验证：续训 CPU 回归与原 MotionMillion 契约共 42 项通过（4.60 秒），
  包括真实 Lightning 完整恢复时 AdamW exp_avg/exp_avg_sq/step 逐值一致、从旧 scheduler
  转入新阶段、阶段内再次恢复与非周期最终保存。Ruff 与 git diff --check 通过。
  `/tmp/genmo-continuation-cpu-jf67poke` 已自动清理并确认不存在；本次 Ruff 格式化缓存
  经精确路径检查清理。仅有既存 autocast 弃用提示及 Lightning 中途 epoch 恢复提示；
  后者说明 DataLoader 随机游标不承诺位级复现，不影响本任务要求的模型/动量/步数恢复。

### 2026-09-16：服务器续训预检、eval 损失接口修正

- 首轮独立 CUDA 八卡均通过，但未设置原正式训练的固定 NCCL 环境时，大张量通信出现
  illegal memory access。核对历史正式运行记录及服务器环境后，统一使用
  NCCL_CUMEM_HOST_ENABLE=0、NCCL_IB_DISABLE=1、NCCL_SOCKET_IFNAME=lo、
  TORCH_NCCL_BLOCKING_WAIT=1；重新实测八卡 CUDA、32 MiB/256 MiB all-reduce 与
  broadcast 全通过。两个隔离目录 `/tmp/genmo-continuation-nccl-091465a2`、
  `/tmp/genmo-continuation-nccl-fixed-_m9u9oi6` 均已删除并复核；没有 core 文件遗留。
- 首轮真实恢复短测确认八个 rank 每卡 63854 样本、global_step=210000、532 组 AdamW
  状态及真实 LR=2.2768240790631672e-6；质量基线随后被原 forward_train 的 train-mode
  断言阻止，未执行正式续训。临时 `/tmp/genmo-continuation-smoke-h0jnkajc` 已清理。
- 修正 gem/network/gem_diffusion.py：仅当显式 _validation_loss 标志且 eval+no_grad
  同时满足时，允许沿用训练噪声分布计算验证损失；不切回 train，不开启文本 dropout，
  不改变正常训练/推理接口行为。监控制品补充实际样本索引、派生 seed 与完整指标。
  新增 CPU 回归覆盖该守卫与训练噪声分布；连同前述测试共 43 项通过，Ruff/差异检查通过。
  `/tmp/genmo-continuation-cpu-k0l5j8px` 已清理；文档补充服务器1必须沿用的通信环境。

### 2026-09-16：八卡真实续训短测通过

- 服务器1在代码 `b2a9e6b` 上，以原 s210000 完整恢复、8×256、BF16 实际运行 4 步，
  按隔离测试参数用 8 条动作 × 2 seed 在 210000/210002/210004 完成固定质量检查。
  八个 rank 均为 per_rank_samples=63854；三次验证 loss 为 11.9542298、11.9496556、
  11.9466594，脚滑/多样性/接触指标有限。此处只证明流程可运行，不认定质量改善。
- 编号 checkpoint 和 last 的 CPU mmap 实际读取确认 global_step=210004，调度器
  保留本阶段 20000 步计划，LR=2.3077167827469145e-6；恢复审计的 AdamW step=210000、
  状态数 532 及动量片段与源 checkpoint 相符。日志达到 max_steps 正常退出，8 个
  rank 均输出 End of script；阶段状态、质量历史、baseline/latest 整套动作均存在。
- 测试目录 `/tmp/genmo-continuation-smoke-7dtm19ux`（含约 15 GB 临时 checkpoint）
  已自动删除并确认不存在；本地 pytest/Ruff 缓存和两次 CPU 测试根目录均无残留。
  下一步使用正式 32 条 × 4 seed、5000 步间隔、三次无可靠改善早停，并保留新实验目录。

### 2026-09-16 13:03 CST：服务器1正式开启 210000→230000 八卡续训

- 启动身份：启动提交 `a4f4bb7d366d5a897aebf47a516f710a835a5016`，分支
  `feature/smpl-text-only`，cwd `/home/user/liwei/GENMO`，Python 为仓库 .venv，
  PyTorch 2.7.1+cu128 / NCCL 2.26.2。启动前本地/GitHub/服务器同 SHA，工作树干净，
  八卡无其他 compute 进程；使用前述已经大张量验证的四个固定 NCCL 环境变量。
- 再次顺序读取源 s210000 验证 5278575218 字节、大小/mtime 稳定，SHA256 为
  `c07c148f9a0ddad7393e820a932c56e62a7b1aa555ee4c997b818a7164eadb15`，原实验未改动。
  新输出根目录 `/data0/user/liwei/GENMO_outputs/motionmillion_continue_s210000_20k_20260916_130323_a4f4bb7`，
  tmux `mm_text_continue_20k`；命令、环境、起点指纹、日志/exit-code 路径记在 launch.json。
  日志为同级 launch_logs 下同名 `.log`；TensorBoard 和 checkpoint 位于新 version_0。
- 13:04:34 实际核验：GPU 进程 PID 3425506–3425513 对应 rank 0–7，cwd/源 checkpoint/
  输出参数逐一匹配；显存约 73829–74267 MiB。已记录更新 210010，loss=12.4445724、
  LR=2.3463326e-6、裁剪前梯度范数=9.27778，正式日志未见 Traceback/OOM/NaN/NCCL 错误。
  恢复审计确认 532 组 AdamW 状态、step=210000 与源动量片段一致，阶段目标为 230000。
- 正式固定基线已完成：32 条 val 动作 × 4 seed，共 128 条生成，验证 loss=11.4892184、
  根相对多样性=0.2303176 m、脚滑代理=0.1373907 m/s、近地接触覆盖=0.7432132。
  这些只是本阶段对照基线，不能据此宣称质量改善、官方 FID/语义达标或动力学安全。
  第一轮正式新质量对照/保存将在 215000 步；之后 220000/225000/230000 或早停结束补存。
- 正式产物保留：新 version_0/quality_monitor 下 baseline.npz/latest.npz 各约 3.54 MB，
  作为本阶段固定对照及最新完整生成结果；cohort.json、resume_audit.json、history.json
  属于正式运行证据。被下一套验收结果替代时按完整集合清理；不删除训练 checkpoint。

## 2026-09-16 14:07 CST：文本动作网页最多保留 60 条历史并自动清理旧产物

- 修改目标与分支：`feature/smpl-text-only`，基于 `7b67c0e`，工作树开始时干净。按用户
  最新明确指令“最多保存60条记录，历史的自动删除”实现实际磁盘清理；此要求替代此前
  网页历史不自动删除的约定，仅作用于文本动作网页，不扩展为训练/模型或其他功能清理。
- 实现：storage.py 固定 HISTORY_LIMIT=60，service.py 在任务完成/失败及服务启动时按
  创建时间保留最近 60 条已结束记录；正在生成的任务不参与淘汰，因此生成期间可临时
  显示 60 条历史加 1 条活动任务，结束后回到限额。历史条数包含成功和失败记录。
  超额任务的记录、动作数据、视频、缩略图和日志一并删除，不只隐藏网页条目。
- 删除与恢复边界：仅根据合法 UUID 构造本站 tasks 下的任务目录，不信任记录中的
  task_dir/output_dir；拒绝任务根目录、任务目录及回收目录软链接，不跟随内部软链接。
  任务目录先原子移入 .expired_tasks，再更新原子历史索引并删除；中断后启动继续清理，
  已移出的记录不重新出现。删除失败保留尚未删除的文件、报告错误，并在后续任务结束或
  重启时重试。保留模型 checkpoint、T5、训练日志、生产数据及其他网页结果。
- 页面和接口：本地/公开 history 接口增加 history_limit；共用模板注明 60 条规则。
  app.js 在当前预览记录被淘汰时清除失效链接，并切换到保留的最新成功视频，仍手动播放；
  无成功视频时显示空预览。更新视频失效提示及 docs/text_motion_web.md 的保存/恢复说明。
- 实际测试：原网页和公网网关测试共 51 项通过（18.98 秒）；新增覆盖启动时 61→60、
  重启不恢复淘汰记录、成功/失败结束时清理、保护进行中任务、物理文件删除、删除失败
  重试、记录路径伪造及软链接隔离。Ruff 与 git diff --check 通过。测试均使用系统临时根
  `/tmp/genmo-history-retention-tests-m4_rjh3f`，结束后已删除；pytest/Ruff 缓存及本次涉及
  模块/测试的 __pycache__ 残留检查均为空。没有运行真实 GPU 测试或写入正式测试任务。
- 实际生效：核对原服务 PID、命令、cwd、日志和无活动任务后，仅重启本地网页后端
  （676926→47053）及公开网关（3861943→47077），更新各自 PID 文件和 share.json；
  cloudflared 保持运行，原公网 URL 不变。重启加载新规则后自动删除最旧任务
  `e682b738a8944119a72285f3930157cb`（2026-09-14 创建，文件大小合计 408966 字节），
  删除前逐文件确认仅为网页动作、视频及元数据；另外 60 个任务 ID 集合完全一致。
- 实际验收：本地与公网 history 均为 60 条、history_limit=60、active_id=null、无恢复错误；
  HTML 均含新保留规则。保留视频 Range 返回 206/1024 字节，被删视频返回 404；tasks 中
  恰有 60 个 task.json，history.json 恰有 60 条，回收目录为空。三个模型仍可选，契约
  为 150/150/50 token；结果目录当前实际占盘 27688960 字节。未进行浏览器播放交互验收。
- 同步：检查本地/远端差异和服务器1同分支干净状态后，按长期授权中文提交、推送，服务器1
  pull --ff-only；只同步源码与记录，不重启或调整正在运行的训练任务。

## 2026-09-17：整理 MotionMillion 训练文本中英对照 200 条

- 所属分支 feature/smpl-text-only。参照根目录 HumanML3D训练集文本_中英对照_200条.txt 的分组、连续编号和中文在前/英文在后的格式，新增 MotionMillion训练集文本_中英对照_200条.txt，覆盖行走、跑爬、跳跃、蹲坐、手势、伸展、日常操作和舞蹈体育8类，每类25条。
- 数据来自服务器1 /data0/user/liwei/datasets/MotionMillion/genmo_smpl_v1/reports/metadata.sqlite3 的 text_entries/built_entries，限定正式train已构建条目，排除Mirror_前缀。200个不同motion_id、200条不同英文；来源为MotionGV 114条、MotionLLAMA 52条、MotionUnion 34条。按动作内容精选并剔除关键词比喻误匹配，非用于统计总体分布的随机样本。
- 英文逐字保留原caption（包括标点、大小写与原有编号），中文为本次人工整理的简短释义，非官方中文标注。文末附200条样本ID、从0开始的caption_index与原始文本成员路径；保留双人/物体场景的原文含义，不把这些示例写成单人SMPL模型的生成能力保证。原HumanML3D文件及训练/推理代码不变。
- 验证：本地检查200个连续编号、完整中英配对、8组各25条和文本去重；回读服务器只读SQLite逐条确认split=train及caption精确一致，再对照正式train manifest下各shard的.meta.json确认全部200个动作和caption索引有效。正式train共有510906个记录，build_fingerprint=9b955b63cf33d4533aae79e98038a3d79a31f073a56552e829a52fba54181702。本任务不运行生成、训练或GPU测试。
- 临时候选、翻译中间表和核验JSON仅写入 /tmp/motionmillion-bilingual-f2bq7ae_，交付文件和本记录保存后精确删除该目录并确认无残留；不保存额外重复示例集。

## 2026-09-17：下载 MotionMillion evaluator 并说明自动与人工评测

- 修改目标与分支：`feature/smpl-text-only`，起点37f09ca、工作树干净。回应正式指标状态、evaluator组成/下载及MotionMillion-Eval人工评分设计；本次不执行完整验证集生成或训练，不修改服务器训练任务。
- 正式资产：下载至 `/home/weili/GENMO/inputs/MotionMillion/official_evaluator`，占盘约1003 MiB；官方代码固定 `8a2a7dfa66ecb6a1533d3d9cb49c743a697e1e1c` 且源码未修改。官方Google Drive的 `epoch=199.ckpt` 为764972866字节，SHA256=`0e46ad8e7eeec72f4bab0f522ed55be655b223e46f626a67cc2754c1877f3f8c`，内含epoch199/global_step87800/414个state_dict项；这不是GENMO checkpoint的步数。下载DistilBERT固定revision `12040accade4e8a0f71eabdb258fecc2e7e948be` 和其本地tokenizer/config/safetensors；取得官方126条prompt。从服务器1只读复制官方release `007582c4fc9637a3f36e548a67be1ef6eaf881a5` 的Mean.npy/Std.npy，按wrapper要求落为mean_std/vector_272下小写文件名，并逐字节SHA256对照。
- 实现与文档：新增 `docs/motionmillion_evaluator_and_human_review.md`，解释DistilBERT/272D动作的512D双编码器、各自动指标含义、官方1–4分人工评审和建议的匿名多评审设计；在 `docs/MOTIONMILLION_TEXT_ONLY.md` 增加入口。资产目录的 `asset_manifest.json` 记录18个关键文件大小/指纹、来源、版本、CPU离线验证和未准备项，不冒充绑定GT资格集合的evaluator_identity。本次未改生产Python代码，不重构官方evaluator或GENMO推理。
- 环境兼容：沿用现有Python3.10/.venv，torch2.6.0+cu124、pytorch-lightning2.6.5、transformers5.13.1、numpy1.23.5、scipy1.15.3、omegaconf2.3.1；未安装官方整份旧训练依赖，下载gdown5.2.0使用uv隔离工具环境。官方checkpoint含训练对象，weights_only默认加载失败；仅对已核验来源及指纹的官方文件，在独立验证进程设置TORCH_FORCE_NO_WEIGHTS_ONLY_LOAD=1兼容旧wrapper。没有全局修改环境或关闭其他加载检查；该环境加载通过不等于论文原始环境的数值复现。
- 实际验证：全部414个state_dict中的tensor有限，mean/std均为272维有限数组且std全正；CPU、HF_HUB_OFFLINE=1和TRANSFORMERS_OFFLINE=1模式下，以两条英文和重复官方mean构成的合成动作（补齐300帧、有效长度120/200）调用原wrapper，文本/动作均输出有限[2,512]，strict权重加载成功。合成输入只证明加载/编码链路可用，不产生模型质量结论。18项资产SHA256复验、官方git干净、文档本地链接和git diff --check均通过。
- 边界与后续：本地未发现该文本模型的正式指标报告，服务器1原official_evaluator目录经只读检查为空；本次没有同步评测资产到服务器。当前仍缺完整val GT视图、eligibility、正式evaluator identity和GENMO预测。指出现有prepare脚本要求的mean/std原始路径与真实release大小写/层级差异，以及固定120帧生成与官方4帧单位裁剪的协议差异，未擅自改服务器数据。现有人工HTML支持三项打分/JSON导出，多评审盲法、随机化、一致性统计仅为文档建议，未宣称已经实现或已经完成126条评分。
- 清理与交付：临时CPU检查目录 `/tmp/motionmillion-evaluator-smoke-5wjjd0ty` 已自动删除；本次使用PYTHONDONTWRITEBYTECODE，下载目录无.part和__pycache__残留。inputs正式下载资产持久保留且被Git忽略，仅提交说明和本记录到当前分支并推送GitHub，不上传大权重，不改其他分支。

## 2026-09-17：增加固定小批 MotionMillion 评测入口并通过本地验证

- 分支 `feature/smpl-text-only`。用户明确要求128条固定验证样本完成真实读取→GENMO生成→272D→evaluator→指标输出。本地4090有其他训练和网页占用，服务器1 GPU5无计算进程，计划使用同一s210000权重在空闲卡执行，不停止已有任务。
- 复用 `prepare_motionmillion_official_eval.py`，增加max-samples/selection-seed/source-archive选择，按seed与motion_id的SHA256稳定排序；范围、候选数量及样本指纹写入eligibility，受限集合显式标记subset_smoke，默认全量行为保留。兼容官方HF的Mean.npy/Std.npy路径，双路径指纹冲突、非有限统计量和非正std均拒绝；提取某归档所需成员完毕后提前结束读取，恢复时拒绝改变样本协议。
- `run_motionmillion_official_metrics.py` 保留六项原有生成指标，增加同批真实动作的R@1/2/3、Matching和Diversity参考及评测范围；`summarize_motionmillion_metrics.py` 拒绝把subset_smoke汇成完整验证报告。补充固定样本顺序、范围标记、统计量路径冲突和小批汇总拒绝测试，未改生成模型、T5或官方evaluator权重。
- 实际验证：`tests/test_motionmillion_text_only.py` 38项通过；涉及源码Ruff和git diff --check通过。测试输出仅位于 `/tmp/genmo-mm128-tests-76ho5fy7`，TemporaryDirectory退出自动删除并确认无残留；禁用pytest缓存及Python字节码。服务器真实运行工作目录已明确为 `/tmp/genmo-mm-eval128-9wkt3d_p`，本条仅记录准备完成，真实GPU结果将在后续记录补充；该目录在本次验收结束后清理。
- 同步顺序：本地源码、测试、记录先中文提交推送，再在服务器1干净的同分支pull --ff-only，禁止直接修改服务器工作树。临时评测资产由本地已固定官方下载目录传入任务临时目录，不占用服务器正式训练输出目录。

## 2026-09-17：s210000完成128条真实验证样本的生成与自动指标链路

- 执行分支/代码：`feature/smpl-text-only@3f7a81f`，服务器1先pull --ff-only至同一提交；GENMO使用已有正式s210000，5278575218字节，SHA256=`c07c148f9a0ddad7393e820a932c56e62a7b1aa555ee4c997b818a7164eadb15`，与本地归档一致。使用服务器1 GPU UUID `GPU-c9bda0e6-905a-936c-dde1-93074571bacf`，不停止已有训练/服务；GENMO生成进程PID3807261正常退出并回收。T5固定bed96aab快照/150 token/FP16，120帧、30FPS、DDIM50、CFG2.5、默认后处理、shape_mode=zero。
- 样本：完整官方val18726条，60–200帧合格15217条。限定MotionGV/folder9、MotionLLAMA/trumans、MotionUnion/haa500三个原始归档中的1027个候选，以selection_seed42的SHA256顺序固定128条，来源96/6/26；选中ID指纹=`c882923e7fd9bea315ce35a2590e57ef83c06952eedd7cbfa2f981b2481ee338`。128条逐一核对split=val，训练集同ID和镜像归一化ID交集为0；不声称排除所有跨来源内容近重复。GT长60–199帧、平均99.4921875帧，生成统一120帧；subset_smoke标记贯穿报告。
- 实际运行：官方evaluator代码、权重、DistilBERT等18项资产SHA256复核通过。第一次准备遇到已复制mean/std的拒绝覆盖保护，随后用--resume逐项验证复用，没有覆盖不同数据。128条生成和272D转换全部成功，T5和GENMO各加载一次，生成阶段含初始化及转换共167.729秒。evaluator使用CPU离线执行，旧checkpoint加载兼容仅设置在独立评分进程；没有升级服务器环境。
- 实际指标（1个global seed42，batch32×4，128条均参加，0条丢弃）：R@1=0.453125（58/128），R@2=0.6015625（77/128），R@3=0.6953125（89/128），Matching=29.140757083892822，FID=364.3581975551458，Diversity=43.94527816772461。同批真实动作参考：R@1=0.7109375、R@2=0.859375、R@3=0.921875、Matching=20.864595413208008、Diversity=42.299434661865234。
- 验证：全部128条GT与生成272D的shape/finite/SHA256通过；全部生成SMPL的body_pose=[120,63]、global_orient/transl=[120,3]、betas=[120,10]且为零，30FPS，全部READY存在。评分六项有限，报告中128个唯一ID指纹与固定集合一致，CSV六行与原始JSON数值逐项一致。运行环境torch2.7.1+cu128、transformers5.14.1、pytorch-lightning2.6.5、numpy1.23.5、scipy1.15.3；源码测试38项和Ruff已在前一条完成，本次只补报告/说明并检查文档链接与git diff --check。
- 交付：新增 `docs/reports/motionmillion_s210000_eval128.md/.json/.csv`，保存实际分数、同批真实参考、模型/协议/数据指纹与128条样本身份（caption仅记录索引和SHA，不把数据集全文加入Git）。更新evaluator说明中的小批结果入口和已修复统计量路径。评测输出为集合级指标及可追溯记录，不是视频、人工三项分或物理/部署验证；本次未渲染视频、未进行126条人工评分。
- 功能边界：128样本少于512维特征，FID协方差估计不稳定；来源受限、单seed且GT长度与生成长度不同，未复制官方4帧裁剪，不作论文排名/模型收敛结论，不提供20-seed置信区间。完整评测和多checkpoint比较仍须统一正式协议。
- 清理：结果与身份已写入正式说明后，复核生成进程退出、临时目录不含正式s210000/训练日志/生产数据且无软链接，删除服务器 `/tmp/genmo-mm-eval128-9wkt3d_p`（约1.1GiB，含128条动作、GT和evaluator副本）；本地 `/tmp/genmo-mm128-local-zazjh3_w` 仅五个下载中间清单，也已删除。两个路径及先前pytest临时目录确认不存在。保留本地inputs中的官方evaluator资产与所有正式训练checkpoint/数据。当前报告与记录中文提交推送后，服务器1同分支仅pull --ff-only同步文档。

## 2026-09-18：实现路线 A0，完整文本对应完整 MotionMillion 动作

- 分支与起点：`feature/smpl-text-only@9c6d40f55d3a5f94baa10324ace9db470a587eae`，开始时工作树干净；读取 AGENTS.md/agent.md，提交前复核 GitHub 同名分支仍为该起点，GitLab 尚无该同名分支。修改限定于文本训练、推理、评测、测试和说明，不操作服务器，不停止现有训练。
- 数据读取：MotionMillionDataset 新增 `sequence_mode=full`、`pad_to_frames=300`、独立 `caption_sampling=random/first`。完整保留原始 60—300 帧、30 FPS，crop_start=0，length/valid_length=F；训练随机 caption、验证第一条，caption/offset/embedding/mask 严格对应。full 模式仍按 `motion_frames=120` 核对原 release 元数据，保留 schema/split/fingerprint/shard 顺序与 SHA 绑定、索引及实际长度/shape/finite 校验；追加一动作一索引检查和独立数据身份，不改 manifest/fingerprint，不在每次读取哈希大型分片。
- 组批和派生量：真实 F 上执行既有空间/相机增强和派生量，之后按表示尾部延拓到 300、bool mask 补 False；文本维度仍为 150，缺失模态和条件张量时间维一致。动作和文本缓存不原地修改。BaseDataset 相机每十帧抽样后 repeat 显式截回 F，修复 97/183/299 等长度多出帧的问题。DataModule 继续 shard-aware/drop_last，新增每 rank micro-batch、累积及 epoch 尾部更新数日志，不增分桶/动态 batch/采样权重，不让 Lightning 二次分片。
- 注意力：denoiser 新增按真实 F 构造的 `valid_length` 模式，主配置 max_len=120；短于120的有效帧完整可见，长动作在真实边界上复用原局部窗口。RoPEAttention 合并局部/key padding mask，对结构性全遮蔽行在 softmax 前安全处理，之后概率及投影输出归零；不开放 padding key，不用全局 nan_to_num。真实 motion RoPE 4096 缓存和前后向参与测试。计算仍为稠密300帧，不宣称维持原120帧显存。
- 损失：新增 `valid_per_sample` reduction，先按每条样本广播后真实有效元素数求均值，再乘扩散 importance weight 并 batch 平均；空项为可求导零，FP32 累加。覆盖原 simple、关节、平移、投影、顶点、世界平移、static 分类及可选 shape 监督；权重和151D归一化不变。末真实帧无下一帧观测，根速度/static 标签及监督使用成对有效 mask，测试也覆盖二阶差分边界。不增加语义/动力学目标；明确新的有效样本权重与旧整体 mean 不数值等价，旧配置默认 legacy。
- 独立配置：新增 `gem_smpl_motionmillion_text_fullseq` 及 train/val fullseq YAML；保留 T5-3B/150×1024、text-only、151D、原网络规模和优化器/loss权重。默认从零，候选每卡64×8卡×累积4=2048标记待显存验证；统一 training_budget 连接 max_steps/scheduler total_steps/warmup，max_epochs=-1。允许同数值修复的crop120对照，主实验没有改成max_len300。
- checkpoint/推理：新增序列契约模块和训练入口预检查；保存 sequence/min-max F/padding/FPS/attention/reduction/caption策略，数据身份独立保存。跨契约完整 resume 被拒绝，fit 时比较真实 release 身份；weights-only 仍走原 pretrain_ckpt 路径，不恢复优化器/步数。demo/resident 读取 checkpoint 契约覆盖推理设置，新 fullseq 请求60—300帧、30FPS，旧50/150-token模型保留兼容。padding验证batch按有效F逐条生成和后处理；常规单条请求直接生成F，输出NPZ/SMPL/metadata对应F，未新增时长预测器。
- 评测：现有生成/评分工具增加 fixed/gt 长度策略，gt记录 `full_sequence_matched_length_v1`，逐条按GT原始F生成，不截掉非4倍数尾帧。进度恢复、逐条长度检查、汇总和候选对比绑定长度协议与资格集合。官方资格仍60—200帧，201—300只能另做长动作诊断；官方权重、272D mean/std、特征定义和既有资格集合未改，历史 s210000 fixed120 报告未改，不称论文同协议复现。
- 预检与文档：扩展原 preflight，full模式检查旧release契约，报告实际读取记录的长度分布、有效帧总数、padding比例和裁剪计数，非零裁剪失败；`--config-only` 不读数据/不建模型，明确 GPU 和实际制品未验证。新增 `docs/MOTIONMILLION_FULLSEQ_A0.md`，包含 CPU、真实数据预检、单卡/八卡 smoke、从零训练、完整恢复、240帧文本生成、固定128条gt评测命令及crop120对照；旧说明只加入口。
- 实际测试：最后一次 CPU 回归为 **158 passed / 1 skipped / 0 failed**（12.44秒）：`test_motionmillion_fullseq.py`、`test_motionmillion_text_only.py`、`test_demo_smpl_text.py`、`test_resident_text_motion.py`、`test_training_continuation.py`、`test_text_motion_server_protocol.py`。覆盖完整帧/缓存/随机caption/数据拒绝、混合长度组批、真实RoPE有效输出和梯度不变性、loss边界与空mask、checkpoint写读及跨契约/数据漂移拒绝、checkpoint覆盖demo YAML、精确F输出、gt生成控制流和长度协议隔离、实际Lightning loader防二次分片代码。测试rank为CPU替身，没有启动DDP。
- 实际集成和验证边界：本机真实SMPL-X资产参与 CPU 小网络集成，走实际Dataset/相机/EnDecoder151D/扩散/全部原loss，97/183混合pad300前后向有限且辅助loss随padding不变；Transformer缩为32维2层4头。resident和评测生成测试使用明确替身，实际写出NPZ/JSON/临时272D，不冒充真实GPU推理/evaluator/视频验收。正式MotionMillion动作/T5分片本机缺失，相关测试明确skip：**源码支持复用，实际制品待核验**。没有重做全量动作转换/T5编码，没有写旧checkpoint、生产数据或旧正式评测。
- 配置与静态检查：默认与短预算 config-only 实际输出 CONFIG_PASS；单卡/八卡smoke、crop120对照、resume、weights-only命令的Hydra覆盖组合均在CPU解析通过。新Python模块和新增测试Ruff通过，全部变动Python AST、文档9段bash语法、git diff --check通过。现存AMP弃用和Lightning微型恢复夹具DataLoader提示保留，不据此宣称真实完整模型恢复或吞吐通过。
- 清理：各轮pytest使用 `TemporaryDirectory`，禁用字节码与pytest缓存、MPL缓存写入同一临时根；最终 `/tmp/genmo-a0-releasecheck-l1tbggxx` 及此前本任务临时目录均自动删除。检查 `/tmp/genmo-a0-*` 无残留，根目录 `.pytest_cache`/`.ruff_cache` 不存在，新增模块无pyc残留。正式数据、训练checkpoint、日志和历史报告保留。
- 未验证事项与交付边界：真实完整分片、全网络GPU/BF16/显存/吞吐、多卡通信/采样、真实fullseq恢复、真实60—300帧推理/后处理/视频和gt指标待后续授权；本次没有正式训练、GPU测试、全量推理、全量评测或大规模数据处理，没有根据单元测试声称动作质量改善。按长期授权中文提交并推送当前分支至GitHub/GitLab；本任务明确不执行训练服务器同步。


## 2026-09-18 17:44：[feature/bumi-text-only] BUMI文本完整序列训练、网页与导出部署第一阶段

- 目标与起点：用户明确要求实施三阶段方案，本次完成转换数据到来前的代码和CPU验证。新分支从 `feature/smpl-text-only@460c2c9260b10ebb28f36da3c989ace05c7f4c8f` 创建，独立工作树 `/home/weili/GENMO-bumi-text`。起始原工作树存在未跟踪 `tools/data/humanml3d/export_umr_npz.py`，为隔离既有工作没有切换或覆盖原工作树。机器人qpos30/contact/FK/v5损失选择性取自 `feature/bumi-music-only@9046e36`；查看器资源及IPC来自部署分支 `0b850df`，XML保持fe934 SHA，资源清单绑定本分支训练kinematics。未合并整条音乐分支，保留SMPL A0注意力与文本路径。
- 数据接入：新增 `gem/datasets/pure_motion/bumi_text.py` 和 `tools/data/bumi/prepare_bumi_text.py`。机器人qpos[F,28]、30FPS、wxyz、原生21关节，F60–300完整读取、pad300，length/valid_length=F。caption随机/固定开关独立，文本保持150×1024及mask；不经过SMPL BaseDataset、不镜像、不缩放、不裁剪。动作release与原T5关联清单分离，核验原schema/split/build fingerprint、shard顺序与SHA、motion_id/text_index/原caption、原完整动作F及实际qpos；大分片stat变化才重算SHA，LRU返回训练副本。原SMPL/T5文件不改写、不重做全量人体转换或特征。新150-token特征工具只为embeddings为空的记录写新分片，已有引用复用，拒绝50token伪装150。
- 来源/地面/统计：清单要求原始划分、来源区间、重定向版本、地面对齐依据及整条固定Z偏移；相同canonical_source_id的train与held-out重叠排除，精确重复排除并报告，未确认跨库来源单独列出。超长记录报告排除，不偷偷截短。HumanML3D需由转换阶段保留时间字段形成的完整片段，不重复切分。低姿态/大倾角仅预检报告，不应用舞蹈站姿筛选。stats仅统计相应联合/单集训练记录有效30D元素，最后真实帧XY差分不计入，统计量绑定release和运动学SHA。preflight报告长度分布、padding比例、真实有效帧及裁剪计数0；正式数据未读取，实际制品待核验。
- 模型和损失：提取 `BumiGEM/BumiPipeline` 公共机器人部分，保留原音乐类名，新增薄层 `BumiTextGEM`。30D运动+2D接触，T5-3B/150/1024，默认1024维16层8头、valid_length/max_len120、单处0.1文本CFG dropout；没有音乐/相机/体型预测。基础权重完整沿用v5基础表，新增valid_per_sample路径：逐样本真实元素平均后乘扩散时间权重再batch平均，top-k/max仅选有效位置；空mask返回可导零，一至三阶差分依赖连续2/3/4真实帧。最后真实帧XY无下一帧不监督，高度/旋转/关节仍监督；接触末帧用最后真实帧对，padding不制造静止标签，非线性旋转/FK使用有效姿态补齐。保留legacy归约；新归约数值不等同旧音乐训练，不新增语义/动力学目标，不对合法坐卧统一拉直。
- 配置和恢复：新增联合 `exp=gem_bumi_text_fullseq`、MotionMillion单集及HumanML3D单集入口。215000步、LR warmup5000、辅助warmup10000，AdamW2e-4；batch64×8×累积4=2048只是待显存验证候选。短预算必须同步两种warmup。多子集分片索引视图复用shard-aware rank/epoch/drop_last，禁止Lightning二次分片。checkpoint携带文本、序列、30D/2D、关节顺序、资产、数据、损失及网络/扩散配置；父类resume/fit检查继续执行。只允许从零或同契约完整resume，阻断SMPL/音乐/93D与weights-only初始化入口。已有SMPL配置/旧50、150token文本推理回归保留。
- 推理、网页与预览：新增同一 `demo_bumi_text.py` 单条/常驻入口和 `ResidentBumiTextEngine`，共享T5本地常驻、DDIM及三种去噪后端；请求60–300，输出精确F，保存raw/post qpos、contact logits、机器人/模型指纹。默认DDIM50/CFG2.5/seed42/30FPS，足锁只修正根XY。既有文本网页按实际checkpoint识别SMPL/BUMI、显示长度、后端派发；保持串行/旧视频/Range/历史恢复/60条上限，匿名分享白名单传递后端与长度但不暴露路径。BUMI独立MuJoCo查看器只写qpos、mj_forward，无音频、网络或控制器，无Bridge站姿门限；离线视频独立子进程输出1280×720/30FPS并完整解码。模型资产可同SHA搬至checkpoint的assets目录。没有替换现有8766服务或隧道。
- 导出与部署：新增 `tools/export/bumi_text.py` export/build/validate/package，文本六输入、固定对外batch1/计算300帧、运行时length，双输出30D/2D。ONNX及外部权重、TensorRT engine/元数据、stats/kinematics和MJCF/mesh指纹绑定；构建记录GPU、TensorRT版本和精度策略，数值对照按相同有效噪声检查7种长度的单步、完整DDIM和解码误差。部署包显式运行依赖闭包，不带训练checkpoint/Dataset/Lightning/Hydra；runtime入口延迟导入以保留原API并避免无关后端依赖。提供64项固定运行依赖、TensorRT10.13.3.9锁、包内install.sh/run.sh/deployment.ini及中文README；T5另放本地，配置可编辑，模型资产严格校验。未生成真实训练模型的ONNX/engine或正式部署包，未执行新电脑安装。
- 评测与文档：新增 `tools/eval/evaluate_bumi_text.py`，默认两集各64条固定val、首caption、seed42、GT真实F、DDIM50/CFG2.5；输出GT/raw/post运动学诊断、来源协议报告及人工1–5分表。不用人体evaluator计算机器人官方FID/R-Precision。`docs/BUMI_TEXT_FULLSEQ.md`提供数据JSON/NPZ/T5规范、CPU/预检/统计/单八卡smoke/训练/resume/生成/网页/固定128条/ONNX/TRT/部署命令，明确待授权项目；`docs/BUMI_TEXT_DEPLOY.md`为包内运行说明，README增加分支入口。
- 实际验证：最后主CPU回归 **259 passed / 1 skipped / 0 failed，42.09秒**，覆盖MotionMillion A0/text-only、SMPL demo、resident、服务协议、网页与分享、BUMI文本完整序列/推理/集成、迁入qpos30编解码/v5损失/旧适配器。唯一skip为本机无正式MotionMillion motion/T5分片；真实SMPL-X及映射资产临时只读挂接后CPU小网络前后向通过，真实fe934 codec也通过。新增微型Lightning CPU实际训练2步并完整恢复到第3步，优化器/scheduler/global_step和fit/data hooks通过；没有进行正式训练。随机测试网络32维2层4头且打开非零attention门，验证padding输入不影响有效结果、前后向有限、逐项loss不变、末帧/接触/时序边界、合并采样等。实际CPU ONNX导出/Runtime支持60/97/120/183/240/299/300变化length，单步及小步DDIM对照通过；移动后的测试部署包无需训练checkpoint、未导入Hydra/Lightning。随后共享网页/模型元信息和新增包README的补充回归 **23 passed**；weights-only拒绝补充复验 **1 passed**。CPU媒体测试使用真实MJCF/FK、明确标记纯色Renderer替身及真实H.264编码/完整解码；窗口和worker隔离用替身，不冒充GPU画面验收。2条合成cohort的真实小网络DDIM50/运动学报告链路通过，不是正式128条质量评测。
- 静态检查与清理：41个变动Python文件AST、新增Python F类Ruff检查、64项依赖版本约束闭包、安装/运行shell及文档13段bash语法、三个实验及单/八卡短预算Hydra配置、6个CLI/help/config入口均通过；git diff --check通过。测试统一TemporaryDirectory、禁用pytest/Ruff缓存和字节码，临时人体资产链接仅测试期间创建并逐一删除；检查 `/tmp/bumi-text-*` 与本工作树cache目录无残留。AMP/ONNX静态shape追踪等已知提示保留，length动态图由实际ORT对照验证；微型恢复仍提示非可恢复DataLoader游标，未宣称中途逐样本复现。
- 边界与同步：没有真实BUMI文本转换数据、真实训练checkpoint、完整网络GPU/BF16显存吞吐、DDP/NCCL、真实T5编码/桌面/EGL画面、TensorRT engine数值或新电脑安装验收；没有据单元测试宣称语义质量/动力学/实物改善。没有GPU操作、服务器登录/同步、全量处理或停止既有训练，没有修改GMT、旧数据、checkpoint或正式评测报告。完成检查后按用户授权中文提交并推送新分支到GitHub origin与GitLab gitlab；原音乐部署包保持不变。


## 2026-09-18 17:48：[feature/bumi-text-only] 补充训练FK与真实MuJoCo运动学对照

- 目标：进一步核对文本训练与动画使用的fe934机器人运动学一致性，不仅检查有限性。扩展 `tests/bumi/test_bumi_text_integration.py` 既有站立/行走/跳跃/下蹲/躺卧五个用例，对每种动作的首、中、末帧，将21关节及根qpos写入真实MJCF，CPU执行mj_forward，逐有序连杆比较位置与四元数。位置/四元数点积容差2e-6，四元数允许等价正负号。
- 实际验证：5 passed / 18 deselected / 0 failed，3.48秒；Ruff F和git diff --check通过。没有调用mj_step、渲染器或GPU，也不作为动力学/实物跟踪证明。TemporaryDirectory已清理，源码测试保留。
- 前一主提交 `ff8dae9c3fafb9d2e3249e062fe2bffc739e22f4` 已推送GitHub/GitLab；本补充测试与记录独立中文提交后同步两端，不改写历史。


## 2026-09-20 11:17：[feature/bumi-text-only] MotionMillion UMR全量数值检查与文本动作质量筛选

- 目标与分支：针对服务器2 `/data2/user/motion_dataset/millionmotion/umr_change/all` 的559924条原生UMR动作，参考音乐分支30Hz筛选实现，完成文本分支的可恢复预处理与PASS构建接口。本地工作树 `/home/weili/GENMO-bumi-text` 从已同步的 `b345cf0` 起步；服务器2从GitHub建立独立 `/home/user/liwei/GENMO-bumi-text` 工作树，原 `/home/user/liwei/GENMO` 音乐分支不改动。
- 实现：在已有 `tools/data/bumi/prepare_bumi_text.py` 增加 `filter-umr` 和 build的 `--quality-report`。新增 `umr_text_preprocess.py` 分目录核对summary/文件集合、完整NPZ读取CRC/有限性、qpos28/21关节名字、30Hz/frame_ids及源SMPL-X身份/数值/Y-up契约；SQLite增量事务、有限2×worker队列、进程锁、双输入SHA续跑、配置/代码/资产/库版本绑定、ERROR重试及分片JSONL/候选/汇总原子发布。输入目录外写报告，不修改生产数据。
- 质量逻辑：`umr_text_quality.py` 复用音乐 `filter_sonic_npz_motions.evaluate_motion`，以真实MuJoCo模型逐帧计算完整脚网格、水平脚滑、连续悬空及非相邻凸包碰撞；有效限位取XML与UMR配置交集，包含膝0.1rad。接触候选不使用水平速度预筛；碰撞保留原始深度与默认姿态逐刚体对基线。配置 `quality_filter_umr_text_30hz_v1.yaml` 默认低姿态仅诊断；区分PASS/REVIEW/REJECT/INVALID/ERROR，完整60–300帧PASS才是训练候选。
- 构建边界：仅完整无ERROR的报告可进入build；重验动作和源人体SHA，绑定原MotionMillion文本ID及镜像canonical来源后执行原split重叠排除，重排qpos并派生接触标签，隔离staging成功才发布release。未带质量报告时保持原标准NPZ/文本构建接口。质量候选不自动创建caption/T5/split，原272D来源文件只保留身份引用；源Y-up不重复旋转，动作不自动平移、不裁剪、不循环补长。阈值改动需要新报告目录重新计算；离线通过不代表动力学/实机验收。
- 运行交付：`scripts/prepare_bumi_text_umr.sh` 默认16个CPU worker、单线程、预期559924条、报告 `/data0/user/liwei/dataset_reports/motionmillion_umr_bumi3_v1`；使用服务器2既有UMR解释器。说明及启动/tmux/恢复/build命令见 `docs/bumi_text_umr_preprocessing.md`。未启动正式全量筛选或训练。
- 本地验证：新增筛选回归 + 原文本fullseq回归 + SONIC筛选核心回归 **31 passed，13.73秒**。覆盖NaN/Inf、损坏文件、错误FPS/frame_ids/关节、来源up轴、短长序列、膝有效限位、四元数正负等价、真实凸包基线、独立脚滑、并行/恢复、源变更、ERROR恢复、partial拒绝、漏文件/多文件、PASS构建、镜像跨split与失败清理。已有AMP弃用及微型Lightning提示未影响通过；不作为真实生成质量证明。Ruff、shell语法、CLI help与git diff --check通过。
- 清理：`/tmp/genmo-umr-text-tests-42d_6lt8`、`/tmp/genmo-umr-text-tests-kxzxrflz` 均由TemporaryDirectory清理并确认不存在；字节码、pytest/Ruff缓存禁用，本工作树残留检查为空。服务器真实数据有界验收将在提交推送后执行，结果另追加记录。


## 2026-09-20 11:22：[feature/bumi-text-only] 服务器2真实UMR有界验收与低姿态支撑保护

- 同步与真实检查：首个实现提交 `6ed1e6c` 先推送GitHub，再由服务器2 `/home/user/liwei/GENMO-bumi-text` 执行 `git pull --ff-only`；既有 `/home/user/miniconda3/envs/ykj_umr/bin/python`（torch2.14.0+cu130/numpy1.26.4/MuJoCo3.3.7）可直接运行筛选CLI，无环境修改。
- 全量清单审计：新程序对10个folder建立559924条索引，folder0–8各55993、folder9为55987，missing=0、unrecorded=0。仅前30条执行真实质量计算：PASS16、REVIEW9、REJECT5、INVALID0、ERROR0，完整60–300帧训练候选8条；已读6908帧，输入800729字节。首次总耗时36.33秒含全量清单建索引，不能外推全库FK耗时。随后同范围resume重新校验文件SHA，30/30结果复用、状态与帧数不变，退出码0。
- 跨目录验证：10个folder分别取首/中/末共30条（与前30条有重叠），共4054帧，长度15–598帧；PASS21、REVIEW6、REJECT3、INVALID0、ERROR0，长度适用PASS17。仅说明真实来源适配和计算链路可运行，不把样本比例解释为全559924条保留比例。
- 文本场景补充：在 `umr_text_quality.py` 的双脚离地诊断后加入低姿态非足部刚体低高度支撑迹象；低姿态已有躯干/手臂/腿部支撑迹象时不把双脚离地直接计为悬空REVIEW，保留诊断。支撑迹象是body代理，不冒充真实接触测量；脚穿地、脚滑、自碰撞独立规则继续生效。补充真实FK与隔离足部信号的回归，并更新预处理说明。
- 清理：服务器测试目录 `/tmp/genmo-umr-text-server-check-y8bl9ky8` 已自动删除并确认不存在；跨目录验证只在内存计算、无输出文件。没有启动正式全量质量筛选、正式数据构建、训练或TensorBoard。最后补充规则的本地回归与服务器复核随本任务继续完成。
- 补充规则本地验证：预处理模块最终回归 **18 passed，9.07秒**；Ruff和git diff --check通过。临时目录 `/tmp/genmo-umr-text-final-amtm6zn1` 已清理，工作树缓存残留检查为空。此前31项相关回归已通过，本次仅改动该筛选模块并重跑其完整回归。


## 2026-09-20 11:24：[feature/bumi-text-only] 最终服务器代码与真实样本复核

- 补充代码 `a46a619` 经GitHub推送后由服务器2独立文本工作树ff-only更新。最终规则重新计算前30条与10目录首/中/末30条：前30条PASS17/REVIEW8/REJECT5、训练候选8；跨目录30条PASS21/REVIEW6/REJECT3、训练候选17。两组去重共59条/10741帧，PASS37/REVIEW14/REJECT8、INVALID0/ERROR0，完整60–300帧候选24。此前首轮前30条的16/9/5被本次最终规则结果17/8/5取代；仅为有界验证，不代表全量质量占比。
- 最终复核只读真实数据、在内存保存结果，无新报告文件。已确认服务器临时测试目录不存在，文本工作树无__pycache__/.pytest_cache/.ruff_cache，分支为feature/bumi-text-only且与origin差异0/0；原音乐工作树仍为feature/bumi-music-only@02e1bce，未更改。正式559924条数值/质量计算、正式release构建和训练均未启动，正式报告目录由用户执行启动器后创建。
- 本条只追加验收记录，筛选代码不再变化；提交推送后继续ff-only同步服务器，保持GitHub、本地和服务器2同一HEAD。


## 2026-09-20 11:32：[feature/bumi-text-only] 统一 BUMI 默认根高并保留旧模型坐标兼容

- 目标与用户选择：以 deploy/bumi-music-only-gmt 的零关节位贴地结果为准，将所有 BUMI 分支的活动默认根高统一为 0.48120910 m；GMT 屈膝待机继续按策略的实际关节角做 FK 对齐。
- 公共实现：gem/robots/bumi/kinematics.py 定义 BUMI_DEFAULT_ROOT_HEIGHT_M；加载后 default_qpos[2] 使用统一值，source_default_qpos 保存来源资产姿态且不进入 checkpoint state_dict。gem/robots/bumi/endecoder.py 支持新 genmo.bumi_qpos30_stats.v4 的显式 root_height_reference_m，并兼容原 v3；只将内存 mean[2] 加上旧基准减新基准，std 与其余通道保持原有规则，拒绝缺失/非法基准或 v3 混入新字段。
- 兼容边界：qpos30 布局、30D→qpos28 固定数学转换、网络权重结构、关节顺序/轴/限位、FK 几何不变。原 MJCF/运动学 JSON 中的 0.55/0.65 是来源 qpos0，保留文件及其 SHA 绑定，不再决定活动默认值；生产数据、旧 stats、checkpoint、ONNX、TensorRT engine 不改写。默认世界 anchor 下旧模型动作高度保持一致，显式 anchor_z 仍由调用者决定；没有逐帧拉平跳跃等生成动作。文档 docs/BUMI_ROOT_HEIGHT.md 说明公式、来源值与新旧程序边界。
- 真实资产复核：部署 s350000 清单的全部六项资产 SHA/大小及跨文件身份校验通过；运动学 SHA c08731704dccece11351b6fa877e30bac5ca2a8d363de30af7ca2ea1398f4029、旧 stats SHA 3bf7251551d07b4e97ec6bd9cc196e3abc3803cf64681cdc035039ba27550dba 保持不变。使用 120 帧合成轨迹和非 GT 高度预测对照旧坐标基准，归一化输入最大误差 0，世界 qpos 最大误差 5.960464477539063e-08；只验证编解码数值兼容，不作为真实模型生成质量证明。
- 真实 FK：读取 GMT model_135000_stage2.onnx 元数据（SHA d2e176657d72d1b0efcb04406abd17145fc45a3a5755b62aae7b866e0a6e3d1b），零位/屈膝根高分别 0.4812090993/0.4754618108 m，最低脚底代理点均约 0.002 m，可见网格最低点约 0.97613/1.01569 mm。MuJoCo 解析 nq=28/nv=27，Torch FK 对照 mj_forward 的最大位置误差分别 1.91e-08/4.76e-08 m。未运行控制策略、mj_step、GPU 推理、仿真或实机。
- 文本分支实现：补入部署分支 make_standing_qpos，修复查看器已有调用缺少对应方法的问题；gem/runtime/bumi_text_viewer.py 的 STAND 姿态改经 FK 贴地，不再直接读取源 JSON qpos0。tools/data/bumi/prepare_bumi_text.py 的完整序列统计写入 v4 根高字段；同步主说明与原 canonicalization 文档。新增根高兼容回归，并在既有完整序列测试核验统计生产契约。
- 实际回归：tests/bumi 全部 134 passed / 1 skipped / 0 failed，26.94 秒；新增统计字段断言后单项复验 1 passed，1.95 秒。包含小型模型前后向、Lightning CPU 两步及完整恢复、可变长度 ONNX/Runtime、真实 MJCF FK、预览接口和 UMR 预处理。唯一 skip 为未设置 BUMI_KINEMATICS_PATH 的条件用例，真实部署资产另按上项执行 CPU 对照；既有 AMP/ONNX tracing/微型 Lightning 提示保留。
- 清理与协作：/tmp/bumi-root-height-text-grwkau0m、/tmp/bumi-root-height-stats-ohbo3uor 已自动删除。7 个修改/新增 Python 文件 AST 与 Ruff F 类检查通过，git diff --check 通过，仓库缓存残留为空。保留并衔接任务期间另行提交的 UMR 预处理修改和日志（最新既有提交 1a4c1d3），不覆盖其历史；不登录或改动训练服务器，不启动/重启正式训练。

## 2026-09-20 12:01：[feature/bumi-text-only] HumanML3D UMR 全量筛选与训练构建适配

- 目标：按用户授权处理服务器2 `/data2/user/liwei/hml3d_umr` 的23,242条机器人动作，报告与训练产物统一输出到 `/data0/user/liwei`。本条记录本地集成和验证；服务器全量结果随后追加，不能将测试通过当作全量已完成。
- 实现：复用 `prepare_bumi_text.py filter-umr` 和 `umr_text_preprocess.py`，增加显式HumanML3D来源模式、交付清单/源manifest/文本集合与SHA核验、旧输出/XML路径映射、原SMPL-X逐条完整数值与帧数检查。共用质量配置把人体源契约按数据集命名，机器人输出仍统一Z-up，质量阈值不变，不重复旋转、不平移、不裁剪。
- 构建：新增原入口 `humanml-conversion` 子命令，只消费完整PASS报告、逐字保留caption和train身份；build再核对文本、双输入SHA及实际区间。镜像共享防泄漏分组但保留独立训练样本，修复仅按时间区间误删镜像的问题。支持母序列不足标注终点的既有导出截取，保存真实区间与原标注区间；数据中4条末端比标注短2帧，母序列148帧能够解释，不将其误判为机器人转换损坏。
- 启动与输出：扩展原 `scripts/prepare_bumi_text_umr.sh`，以 `BUMI_DATASET=humanml3d` 切换来源；`BUMI_BUILD_TRAINING=1` 串联筛选、真实T5特征、完整动作分片、train统计及全量loader核验。编码器增加分片进度，更新原预处理文档。正式源人体补齐目录为 `/data0/user/liwei/datasets/humanml3d_umr_source_v1`；报告为 `dataset_reports/humanml3d_umr_bumi3_v1`；release为 `datasets/bumi_text_humanml3d_umr_pass_v1`，配套T5为同名加 `_t5`。
- 来源与资产：服务器1原人体包全部23,247个文件经内网复制，服务器2执行源包SHA256SUMS全量通过。两端BUMI XML均为fe93472d，asset_manifest与两份重定向配置SHA完全一致。仅补齐文件，不重做UMR；原data2机器人文件不修改。T5-3B使用已有本地模型复制，不用旧50-token缓存冒充150-token。
- 本地验证：UMR回归24 passed；与完整文本构建/小网络前后向/Lightning CPU恢复联合回归 **32 passed，17.17秒**。覆盖HumanML3D路径/来源/文本/SHA拒绝、Z-up根姿态不变、镜像保留、子片段边界、全量报告门禁、正式构建与续跑，原MotionMillion回归继续通过。Ruff F/I及格式、bash语法、git diff --check通过。测试替身文本只用于单测，不进入正式数据。
- 清理与边界：所有测试写入 `/tmp/genmo-hml3d-*tests-*` 的TemporaryDirectory，结束后自动删除，工作树缓存检查为空。一次扩大回归尝试因UMR环境缺hydra未收集，随后使用仓库完整 `.venv` 完成32项联合回归。正式筛选和训练构建待同步后执行；不启动模型训练或TensorBoard。配置和代码指纹变化使既有筛选报告不可续跑，原报告需继续使用原代码或新建报告目录。

## 2026-09-20 12:06：[feature/bumi-text-only] 全量HumanML3D筛选结果与单集统计身份修正

- 服务器2已从GitHub ff-only同步a2a7761。32条真实预检无INVALID/ERROR，临时目录 `/tmp/genmo-hml3d-server-check-vqng_o13` 自动删除；真实T5编码验证150×1024、有限值和bool mask通过。模型权重两端SHA均为a0e6c24ae12dd1c39e454fede314b20311c9e83e9e785b0141e8586010ef7766，config与两个tokenizer文件也逐项一致。
- 正式筛选：tmux `bumi-hml3d-prepare`，32个CPU worker，全量23,242条/5,044,068帧，38.38秒完成；PASS8,048、REVIEW4,976、REJECT10,218、INVALID0、ERROR0。PASS中405条不足60帧、14条超过300帧，最终候选7,629条/1,570,581帧/14.5424小时，保留32.8242%。报告完整且partial_scan=false，fingerprint=b1a0da2aae5898c65b7035377bd52d74732966d9893d395b4003b61ca90bb5d4。
- 文本绑定：7,629条对应20,608条原caption；候选保留原动作3,799条、镜像3,830条，其中子片段752条。原始机器人NPZ共591,122,487字节，合格候选对应183,006,248字节；这些是源NPZ大小，不是最终含T5的训练release大小。当前正在正式T5编码，最终分片和统计完成后另行验收。
- 消费端核查发现BumiTextGEM将stats.dataset=null解释为MotionMillion+HumanML3D联合实验，单HumanML3D数据不能使用该身份。修正现有statistics/preflight默认单来源推断，并在启动器显式传入 --dataset humanml3d；混合来源仍保留原联合统计规则。增加真实构建后的单集统计/预检身份回归，不改质量规则、机器人数据或T5。
- 验证：最终相关联合回归32 passed / 0 failed，17.35秒，Ruff F/I、格式、bash语法、git diff --check通过。所有新增统计验证目录均由TemporaryDirectory删除，工作树无测试缓存。补丁在正式统计阶段之前按本地提交推送、服务器ff-only更新，已完成筛选的代码/配置与报告不变；统计从后续独立进程读取修正后的入口。

## 2026-09-20 12:11：[feature/bumi-text-only] HumanML3D 全量训练数据交付完成

- 完成状态：服务器2完整流水线退出码0，tmux `bumi-hml3d-prepare` 保留已结束窗口（dead=1/exit=0）；日志 `/data0/user/liwei/logs/bumi_text_umr/humanml3d_prepare_v1.log`。功能提交a3bad0e在统计前由GitHub ff-only同步；无运行中的筛选/编码/构建进程，八张GPU已空闲，没有启动模型训练或TensorBoard。
- 正式产物：`/data0/user/liwei/datasets/bumi_text_humanml3d_umr_pass_v1` 含15个动作分片、train/val/test清单、运动学资产、build_report和train_stats，共21文件/209,464,943字节。配套 `_t5` 目录含60个真实T5分片和conversion，共61文件/6,357,787,606字节。两目录总计6,567,252,549字节，训练依赖两者；原机器人NPZ仍在data2，补齐原人体包在data0，原始文件未改写。
- 最终数量：train7,629、val0、test0；20,608条caption，1,570,581有效帧，14.5424167小时，完整60–300帧/30Hz/qpos28/contact，模型表示30维/T5 150×1024。build额外排除0条、跨库来源未知0条、裁剪0条；本次产物HumanML3D来源占比100%。保留镜像3,830和原动作3,799，其中子片段752；不伪造官方held-out。
- 验收：正式preflight检查7,629/7,629条和全部caption/embedding，valid_frames与统计量一致、crop_count=0，data_identity与单HumanML3D训练加载器完全相同。train_stats采用genmo.bumi_qpos30_stats.v4、dataset=humanml3d；现有BumiEndecoder读取真实统计量，并对首/中/末真实动作编码，30维归一化特征全有限。根高0.4812090992927551是0.48120910的float32表示，以1e-7容差核验；未改动数据根高度。验收是数据与编解码合同验证，不冒充实际网络训练质量。
- 报告：`/data0/user/liwei/dataset_reports/humanml3d_umr_bumi3_v1/delivery_summary.json` 汇总完整阶段、数量/比例/体积、来源构成、模型/关键文件SHA和日志。完整quality报告和SQLite保留全部REVIEW/REJECT原因及异常帧区间，候选仅PASS；主要淘汰信号是脚滑，自碰撞/悬空等原因可能重叠。
- 清理：正式build staging已原子发布且无残留；本地和服务器工作树无__pycache__/.pytest_cache/.ruff_cache，本轮/tmp测试目录均删除；临时跨服务器SSH套接字和known-hosts已清理。正式源包、数据、T5、报告、日志均保留。此条及文档仅记录最终验收，代码不再变化，随后提交推送并ff-only同步服务器。

## 2026-09-20 12:28：[feature/bumi-text-only] MotionMillion质量结果分析与双视角合集入口

- 目标：按用户要求分析服务器2已完成的MotionMillion筛选，从高/低质量各选30条完整动作分别拼成一个视频并传回本地。原报告覆盖559,924条，PASS291,348、REVIEW175,478、REJECT93,098、训练候选155,811，INVALID/ERROR0；视频尚待真实渲染，不能用本条静态验证冒充交付完成。
- 实现：扩展既有 `tools/eval/render_bumi_motion.py` 的quality-report模式，新增同目录 `bumi_quality_review.py` 负责流式分析/有界候选池。核对原报告SHA与完整统计，质量和长度分开，左右脚/多个碰撞对按动作去重计数。高组从有活动的PASS按移动/转向/低姿态/关节活动分层，低组按REJECT原因分层，目录均衡并排除重复来源。保留单artifact旧入口。
- 可视化合同：匹配报告真实MJCF及全部网格、机器人/人体SHA和原始NPZ身份；双视角原30Hz完整播放，相机跟随XY，右侧地面半透明以显示穿地。只执行mj_forward，不平滑、不贴地、不改变qpos。PyAV流式H.264编码，逐帧标注ID/指标/原异常区间，解码帧数与累计原始帧数核验后原子发布；输出中文分析、每条完整指标/章节、原配置与报告快照。
- 发现与修复：旧报告全部来源命名空间为Mirror_MotionGV，跨folder0/4/9首中末9条原人体source_file核验一致，结论限定当前这批转换数据。旧MotionMillion适配器的源数组检查循环复用key，导致辅助source_sequence_key写成trans_orig；修正为field并增加身份回归。质量计算、source_motion_id与文件SHA不依赖该被覆盖字段，原报告及其质量判定不重写；视频用原NPZ真实sequence与源文件名绑定。
- 验证与环境：新分析/选样回归加既有UMR回归28 passed，11.65秒，Ruff F/I、格式、git diff --check通过；测试只在/tmp/genmo-mm-review-tests-*，TemporaryDirectory清理且无缓存。服务器2ykj_umr环境补充PyAV18.1.0供视频编码，原MuJoCo3.3.7与质量报告一致。待GitHub提交推送、服务器ff-only同步后执行真实小样本画面验收和60条正式渲染。

## 2026-09-20 12:39：[feature/bumi-text-only] MotionMillion全量分析与60条视频本地交付

- 执行与身份：功能提交27778a2已本地提交推送，服务器2从GitHub ff-only同步。使用`tools/eval/render_bumi_motion.py --quality-report .../motionmillion_umr_bumi3_v1 --output-dir .../visual_review --per-group 30`，ykj_umr/MuJoCo3.3.7/PyAV18.1.0、EGL设备0。原筛选fingerprint=00d74eed7622459919da3b8ba0a0bd309d96a705cc9b131eb38819cabef25ac0，恢复原配置SHA93f07bbc8519b246365cbe58e89063b2ac1cf77101f2fdeb455ec5aaba031cdc；XML fe93472d及全网格与原报告一致。只分析既有报告并渲染，不重跑筛选，不启动训练。
- 分析：559,924条/78,512,412帧/726.9668小时，机器人NPZ9,603,715,872字节。PASS291,348、REVIEW175,478、REJECT93,098，INVALID/ERROR0；PASS长度排除135,537，其中短于60帧105,559、长于300帧29,978；完整训练候选155,811/181.3202小时/2,440,903,551原NPZ字节。真正REJECT触发家族按动作去重：脚滑70,459、自碰撞21,400、脚穿地2,662、根高度1,160、突变96；前两者并集90,067（96.74%）。候选不是本轮新构建的训练release，文本/T5/split仍有独立合同。
- 范围与复核：559,924条来源全为Mirror_MotionGV；辅助sequence字段错误同为559,924条，原source_motion_id/SHA及质量数组不受变量覆盖影响，本次每条视频校验原NPZ并补verified_source_sequence_key，不覆盖原报告。额外只读folder7/9报告：folder9 PASS15,952中12,267不足60帧，说明其6.06%候选率同时受质量与长度限制影响；文档补充原因去重、来源范围、目录差异、实际视频与边界。
- 视频：高组移动/转向/低姿态/较活跃肢体/一般肢体各6条，10目录各3条；低组脚滑10、自碰撞10、穿地6、突变4，覆盖全部10目录。两组分别30个唯一来源，原Z-up qpos及完整30Hz时间线保持，双视角只mj_forward，红框标记原异常区间。低姿态13–18号为当前规则允许的地面动作，不能把PASS等同站立风格或动力学验收。
- 正式输出：服务器2`/data0/user/liwei/dataset_reports/motionmillion_umr_bumi3_v1/visual_review`，本地`/home/weili/GENMO-bumi-text/outputs/motionmillion_umr_quality_review`。7文件共33,045,137字节；高视频5,638帧/187.9333秒/11,574,780字节，SHA f34e29dd254df66f45c0b08ed86252668d536609f75a64b51d28f1a11ca9f879；低视频4,589帧/152.9667秒/20,626,277字节，SHA 8d63a12809cb2170fc73891bdcf64e8c89346dd2cc9aebf66ee93f8dcd211e85。附中文分析、60条完整指标与章节、原run/summary/config。
- 实际验收：服务器逐帧编码后全解码，两端H.264/1280×720/30FPS、视频帧数等于选中原动作帧数之和。7文件跨机SHA全部一致，本地再次完整解码，并查看60条中点联系表与4张详细帧，确认机器人和标注可见后由incoming原子发布。正式tmux bumi-mm-quality-review已结束（dead=1/exit=0），日志`/data0/user/liwei/logs/bumi_text_umr/motionmillion_quality_review.log`为EXIT_CODE=0。传输时scp目录/.写法被客户端拒绝，改为明确该交付目录内7文件后成功，无缺失文件。
- 清理：服务器`/tmp/genmo-mm-quality-render-check-20260920`、本地`/tmp/genmo-mm-quality-render-qa-20260920`、`/tmp/genmo-mm-review-final-qa-4_53zqpj`、`/tmp/genmo-mm-review-transfer-state-20260920.json`均已精确清除；正式staging/incoming已发布且不存在。用户请求的两视频、分析、原始数据和正式日志保留。代码验证沿用28 passed；此阶段仅更新文档/记录，不将画面抽查视作模型生成质量或实机证明。

## 2026-09-20 13:40：[feature/bumi-text-only] HumanML3D筛选报告与合集渲染适配

- 目标：按用户要求分析HumanML3D全量筛选，生成高/低质量各30条合集并传回本地，复用原MotionMillion渲染入口，不复制脚本。服务器2只读确认报告complete/partial_scan=false：23,242条，PASS8,048、REVIEW4,976、REJECT10,218、训练候选7,629。原机器人data2与既有data0报告/训练数据不改写。
- 实现：render_bumi_motion.py复用load_umr/QualityEngine验证HumanML3D迁移路径、源包清单与双输入SHA，再按具名关节映射至渲染模型；使用报告原配置初始化，只加载验证、不重新计算质量判定。源清单/文本SHA在渲染前后检查，所选双输入渲染后再次核验。数据集标题自动切换，显示第一条原始caption，完整文本留在analysis.json；Z-up原始qpos、完整帧数、30Hz均不修改。
- 分析：bumi_quality_review.py增加HumanML3D来源命名空间及按状态的原动作/镜像、完整源/子片段统计，报告样本数与标题参数化；存在既有训练delivery时核对筛选指纹、候选数及其列出的关键文件SHA。保留母动作去重，缺少某类REJECT时按现有候选补齐，不伪造穿地或突变REJECT。初步逐条分析真正REJECT触发仅脚滑9,845、自碰撞512，其中重叠139；此前报告中的穿地/悬空/突变可能是同条动作的REVIEW级别伴随问题，不能计作其REJECT主因。
- 验证：既有真实资产HumanML3D构建测试新增渲染加载验证，覆盖原动作/镜像/子片段、qpos逐元素不变、caption对应和身份篡改拒绝；新增HumanML3D命名空间、交叉统计及中文报告标题回归。相关29 passed/0 failed，12.23秒，Ruff F/I、格式和git diff --check通过。/tmp/genmo-hml-review-tests-*通过TemporaryDirectory清除，本次ruff缓存精确清除。正式60条渲染尚待GitHub提交推送和服务器ff-only同步后执行。

## 2026-09-20 13:47：[feature/bumi-text-only] HumanML3D全量分析及60条视频交付验收

- 执行：功能提交11cf7ab已推送并在服务器2ff-only同步。复用`tools/eval/render_bumi_motion.py --quality-report /data0/user/liwei/dataset_reports/humanml3d_umr_bumi3_v1 --output-dir /data0/user/liwei/dataset_reports/humanml3d_umr_bumi3_v1/visual_review --per-group 30`；ykj_umr/MuJoCo3.3.7/PyAV18.1.0、EGL设备0，原XML fe93472d和配置7e08a05e与筛选报告一致。tmux bumi-hml-quality-review结束dead=1/exit=0，日志`/data0/user/liwei/logs/bumi_text_umr/humanml3d_quality_review.log`记录EXIT_CODE=0。
- 全量分析：23,242条/5,044,068帧/46.7043小时/591,122,487原机器人NPZ字节，PASS8,048、REVIEW4,976、REJECT10,218、INVALID/ERROR0；PASS长度排除405短+14长，候选7,629（32.8242%）/14.5424小时/183,006,248原NPZ字节。真正REJECT为仅脚滑9,706、仅碰撞373、同时139；含脚滑9,845占REJECT96.35%。REVIEW中脚滑4,005占80.49%；碰撞821/悬空688/轻度穿地25/连续性13为可能重叠的伴随信号。
- 身份与训练交付：该包为HumanML3D训练派生集，非镜像/镜像各11,621条，母来源10,589；候选非镜像3,799、镜像3,830、完整源6,877、子片段752、母来源3,629。已有delivery的筛选fingerprint b1a0da2a、训练数、关键文件SHA通过，正式目录实际21动作文件/209,464,943字节与61个T5文件/6,357,787,606字节均与报告一致。仍为train7,629/val0/test0、20,608 captions；本轮不重新编码或构建训练数据。
- 视频选样与边界：高组五类各6条，低组脚滑20/碰撞10，60条跨组母来源也全部不同。低组不存在穿地或突变REJECT，不按预设类别制造样本。原Z-up qpos、完整帧率与时序保持，仅mj_forward；绑定原caption并显示，过长省略只影响画面文本，完整caption集合保留。高组类别是按指标分组，低姿态样本包含弯腰/鞠躬，不能当成人工语义或动力学可执行性证明。
- 正式交付：服务器2`/data0/user/liwei/dataset_reports/humanml3d_umr_bumi3_v1/visual_review`；本地`/home/weili/GENMO-bumi-text/outputs/humanml3d_umr_quality_review`。7文件35,073,096字节。高视频6,557帧/218.5667秒/12,826,384字节，SHA b560c45c9b1b53813f8fcd30665c94a4f8575e3b189fe7bf226af515c306b029；低视频5,374帧/179.1333秒/21,451,859字节，SHA af35c2d750a4cdd1eb4467626258bb50dc1e5cef698371d50adf5be1e02d8eff。附中文分析、每条指标/文本/章节、原run/summary/config，原MotionMillion交付作为用户所需另一数据集复核场景保留。
- 验收与清理：服务器与本地全解码通过，H.264/1280×720/30FPS及帧数匹配；7文件SHA跨机一致，查看60条中点联系表和4张详细帧后由incoming原子发布。`/tmp/genmo-hml-review-final-qa-r1ov46h7`、`/tmp/genmo-hml-review-transfer-state-20260920.json`精确删除，incoming路径消失；代码回归29 passed，工作树未产生Python/pytest/ruff缓存。最后仅补充本说明与日志，不重跑已通过的代码测试。

## 2026-09-20 14:07：[feature/bumi-text-only] 用户确认放宽脚滑与碰撞持续时间并新增根倾角门禁

- 目标：补齐服务器2 MotionMillion对应文本，按用户确认的新标准全量重筛MotionMillion与HumanML3D，新结果验收后只保留最新结果。用户明确选择脚滑REVIEW0.75/REJECT1.5 m/s、根倾角>30度连续至少15帧、碰撞额外深度>1/>5 cm分别REVIEW/REJECT且均连续超过10帧。
- 实现：修改共用quality_filter_umr_text_30hz_v1.yaml；umr_text_quality.py增加独立root_tilt配置校验、归一化根四元数计算相对世界Z轴倾角、持续超限REJECT与异常帧区间。纯yaw转向不误报，旧配置缺root_tilt时兼容不启用，diagnostic姿态策略不移除新规则。其余质量及长度规则保持；启动器默认报告/已有HumanML3D训练输出改为_latest，避免旧新结果混用。
- 验证：真实资产相关37 passed/0 failed，11.82秒；新增倾角29.9/30.1度与14/15帧边界、纯yaw、碰撞10/11帧及小深度忽略测试，更新1m/s脚滑由REJECT变REVIEW、2m/s仍REJECT断言。Ruff F/I、格式与git diff --check通过，/tmp/genmo-refilter-tests-*自动清理。
- 文本现场核验：服务器1存在原始texts.tar.gz与split.tar.gz；服务器2对应目录未找到文本包。已经由已认证主机公钥验证的内网SSH传入/data0/user/liwei/datasets/motionmillion_text_source_v1。两端SHA一致：texts=29ae0a38089477aed8296ef7dbc6cfa3ae36d336efacdb2853fc93e939765963，split=1ef9084296f2be16f08b92757cc6b0174a941f6501bb3ce6fe0a94db73ebf72a。逐ID文本覆盖与新筛选仍待后续执行，不能将文件传输等同文本全部匹配。

## 2026-09-20 14:16：[feature/bumi-text-only] MotionMillion原始文本精确绑定与新门禁复核适配

- 目标：补齐原生UMR动作文本并使最新可视复核覆盖新增根倾角REJECT。新增motionmillion_text.py模块，由已有prepare_bumi_text.py新增motionmillion-texts/bind-motionmillion-texts子命令调用；流式读取服务器1传来的官方文本和split归档，按完整来源ID、机器人/人体双SHA及原路径绑定，原始字节及SHA保存在SQLite，缺失明确计数，不借basename、非镜像或生成文本。仅保留官方t2m_60_300划分；未列入split保持null，不伪造train。
- 复核：已有bumi_quality_review.py新增root_tilt原因及选样类别；render_bumi_motion.py通过已核验文本目录为MotionMillion显示原caption，所选完整文本与SHA保存于分析JSON，根倾角样本显示角度。文本绑定sidecar与质量结果独立，未更改正在运行的筛选核心代码及fingerprint输入。
- 验证：40 passed/0 failed，11.78秒，含真实资产既有筛选/渲染加载测试及新增同basename不同目录、镜像不借文本、原始文本SHA、源SHA篡改、缺失覆盖、split冲突和重复文本失败不发布。Ruff F/I/格式通过；/tmp/genmo-text-binding-tests-*由TemporaryDirectory清理，仓库缓存残留检查为空。服务器全量文本覆盖和新视频验收尚待此提交同步后执行。

## 2026-09-20 14:25：[feature/bumi-text-only] 两批全量重筛、文本绑定、训练派生与最新视频交付完成

- 执行与身份：规则提交fa0c69a和文本/复核提交0988779均先本地验证提交推送，再服务器2ff-only同步。MotionMillion 559,924条和HumanML3D23,242条全部complete、partial=false、INVALID/ERROR=0、退出0。新旧逐条relative_path、完整来源ID、机器人/人体SHA和帧数完全一致，变化仅由门禁及已记录辅助字段修复产生；报告指纹分别69a6632db27f21a88a4bfbb1ea6a6929e559447df68bb4d6c601efbddba43491、ec592a5eb9dba6a26ad011c57d8eb81df7cf74b156d07b903f272645e79443de。
- 新结果：MotionMillion PASS310,023/REVIEW66,942/REJECT182,959，60–300帧完整PASS156,385（27.9297%，181.2891小时，原NPZ2,470,376,754字节）；HumanML3D PASS9,060/REVIEW7,644/REJECT6,538，候选8,661（37.2644%，16.4597小时，原NPZ207,857,481字节）。根倾角REJECT169,107/5,874；脚滑REJECT从70,459/9,845降为6,893/863；碰撞REJECT从21,400/512降为14,443/276。原因可能重叠。候选分别新进43,223/2,391，移出42,649/1,359，净增574/1,032。
- MotionMillion文本：官方原包已传服务器2并核验两端SHA；在/data0/user/liwei/datasets/motionmillion_umr_text_latest匹配559,922条、11,791,690条caption。缺失Mirror_MotionGV/folder8/494361（54帧）和500208（30帧）均非训练候选，不生成或借用文本。156,385候选全部绑定文本/双输入SHA，共3,294,293条caption，官方train125,085/val7,987/test23,313，无unlisted候选。本轮未编码MotionMillion新BUMI T5或构建其训练分片，不能把候选总数当train数量或已训练成果。
- HumanML3D训练同步重建：/data0/user/liwei/datasets/bumi_text_humanml3d_umr_pass_latest及_latest_t5，复用现有conversion/encode/build/stats/preflight。8,661条、23,387caption、1,777,644帧；train8,661/val0/test0，原4,322/镜像4,339、母来源4,206。17动作分片及附属23文件237,211,713字节，68个T5分片及清单69文件7,215,192,474字节，合计7,452,404,187字节。全量loader逐caption核验8,661/8,661、crop0、stats v4非placeholder。pipeline退出0，delivery_summary记录文件SHA。
- 最新视频：复用render_bumi_motion.py，每库PASS/REJECT各30条。MotionMillion低组根倾角10/脚滑10/碰撞6/穿地2/突变2，高5551帧185.0333秒、低3906帧130.2秒；HumanML3D低组根倾角14/脚滑10/碰撞6，高6763帧225.4333秒、低5837帧194.5667秒。每库跨高低组60个不同母来源，全部保留原qpos、完整时序与caption；H.264/1280×720/30FPS。服务器与本地完整解码、视频SHA、120条中点及详细帧检查通过；服务器MM11/HML9个交付文件跨机SHA一致，本地各另存local_verification.json。首次QA远程python别名不可用，改为绝对解释器后完成，失败阶段无产物遗留。
- 最新路径：报告/data0/user/liwei/dataset_reports/{motionmillion,humanml3d}_umr_bumi3_latest；视频在各visual_review，本地/home/weili/GENMO-bumi-text/outputs/{motionmillion,humanml3d}_umr_quality_review。总交付索引/data0/user/liwei/dataset_reports/bumi_umr_refilter_latest.json。日志/data0/user/liwei/logs/bumi_text_umr/*_latest.log。文档已改当前路径/数据与独立root_tilt语义，旧结果只在变更历史和新报告聚合对照中保留统计。
- 按用户要求清理：验收后精确删除服务器2两个*_umr_bumi3_v1报告目录及其中旧合集、bumi_text_humanml3d_umr_pass_v1及_v1_t5，共15,344,515,914字节；先核验新报告身份/视频SHA/训练加载/无活跃Python引用再清理。本地两个旧视频集合由incoming原子替换并删除superseded；/tmp/genmo-refilter-video-qa-20260920已清除。原data2机器人动作、人体源包、原文本归档、训练日志/checkpoint均保留。测试沿用40 passed，最后仅更新文档与执行记录；筛选/格式/画面核验不代表动力学或实机验收。

## 2026-09-20 14:57：[feature/bumi-text-only] 为待复核REVIEW增加独立视频组

- 目标：回答REVIEW当前不进入训练，并按用户要求复核待定动作；延续两个数据集上下文，各渲染30条完整动作，保留现有PASS/REJECT对照。
- 实现：复用bumi_quality_review.py与render_bumi_motion.py，增加--quality-groups review。仅选60–300帧REVIEW且training_eligible=false、无REJECT原因的记录；按脚滑/碰撞/悬空/穿地/连续性等实际原因等配额分层，按目录均衡、稳定来源哈希选样及母来源去重，避免以极端峰值充满样本。黄色标题/异常边框明确REVIEW / NOT IN TRAINING，悬空样本显示实际标记的最长帧区间；报告增加全库REVIEW原因统计及章节。默认仍选择PASS/REJECT两组。
- 边界：只扩展复核选样和显示；筛选配置、质量状态、原始qpos/时序、训练数据及文本划分不改变。输出拟放在两批最新报告visual_review/review子目录，本地放在现有质量复核目录的review子目录，作为当前同一套结果的第三组。
- 验证：42 passed/0 failed，12.07秒；新增REVIEW分层配额、确定性、母来源去重、长度排除、不得已入训练、review-only全量分析和中文报告回归，沿用真实资产加载及文本身份验证。Ruff F/I及git diff --check通过；/tmp/genmo-review-tests-*由TemporaryDirectory清理。正式视频待本提交推送并服务器ff-only同步后渲染验收。

## 2026-09-20 15:03：[feature/bumi-text-only] 两库各30条REVIEW合集交付验收

- 执行：功能提交0c89b62先推送GitHub再服务器2ff-only同步。复用render_bumi_motion.py --quality-groups review --per-group 30，真实fe934 BUMI资产、原筛选配置及双输入/文本SHA核验；ykj_umr环境，EGL设备0/1。tmux bumi-humanml3d-review30与bumi-motionmillion-review30均dead=1/exit=0，日志/data0/user/liwei/logs/bumi_text_umr/{humanml3d,motionmillion}_review_30_latest.log。
- 样本：每库30条、30个不同母来源，均为REVIEW、training_eligible=false、完整60–300帧。MotionMillion主要选样类型脚滑8/碰撞8/悬空7/穿地7，长度范围内没有可用连续性REVIEW；HumanML3D脚滑8/碰撞6/悬空6/穿地4/连续性6。部分动作可能同时有多个复核原因，此表只计选样类别，不代表全库比例。再次查询质量SQLite确认状态/来源SHA；HumanML3D实际train清单8,661条中与所选30条交集为空。
- 视频：MotionMillion5,020帧/167.3333秒/17,016,220字节，SHA59909d8c18c27f389654b018aaf5cbe5d7ecf21ce31ca6f8b3e6b1513cf47774；HumanML3D6,307帧/210.2333秒/21,450,048字节，SHA9819dbf29cffce01c5a4a024a5d4ba1fc588637e023017a3d5785b135531d837。两段均H.264/1280×720/30FPS、双视角、原始完整qpos；黄色REVIEW标题及当前异常区间框，保留原caption和详细指标/章节。离线mj_forward，不证明动力学执行性；不改变训练准入。
- 交付：服务器2/data0/user/liwei/dataset_reports/{motionmillion,humanml3d}_umr_bumi3_latest/visual_review/review；本地/home/weili/GENMO-bumi-text/outputs/{motionmillion,humanml3d}_umr_quality_review/review。每库6文件两端SHA一致，本地另有local_verification.json；MM本地7文件17,529,414字节，HML7文件21,871,722字节。完整解码11,327帧、60个中点和4张详细帧检查通过，标题、文本、双视角和黄色标记可读。
- 清理：本地.review.incoming已原子发布，/tmp/genmo-review30-video-qa-20260920已精确删除；服务器staging、本地测试目录及Python/pytest/ruff缓存残留检查为空。既有PASS/REJECT对照及正式数据/报告/日志保持；本次新增REVIEW作为同一最新复核集合的第三组。代码回归42 passed；最后仅补充交付文档和本记录。

## 2026-09-20 15:58：[feature/bumi-text-only] KIT-ML 与已有 AMASS SMPL-X 身体数据的精确对齐入口

- 目标：按用户要求下载服务器2 KIT-ML、关联 AMASS 来源并构建 30 Hz 文本动作 metadata。用户确认复用本地 inputs/AMASS/hmr4d_support/smplxpose_v2.pth，而不补下载原始 SMPL+H G。该可信容器18,086条、1,979,394,441字节；已核验模型smplx、姿态66维、形状10维、平移3维，30Hz依据已有AmassDataset契约，原始采集帧率及手脸信息未保存。
- 实现：新增 tools/data/kitml/prepare_kitml_amass.py，接收官方ZIP/目录与固定Unifier映射，支持已有GENMO容器及未来原始SMPL+H两种输入。精确子集/被试/完整动作名对应，校验内嵌file_name与模型身份，MMM真实时间戳交叉核验时长；缺文件、无映射、无文本和错配单独报告。复用AIOZ的SLERP/线性重采样；保留完整来源字段、原始caption、秒数时间范围、已知缺失参数及same-source分组，输出全部metadata与仅ready清单。新增docs/kitml_amass_preparation.md说明命令及数据边界。
- 边界：Unifier固定提交a0c031ad6af69a1f64c1dd4d4d199ff6974d1708，3701个映射中本地源精确覆盖3699个（KIT2090/CMU1566/EKUT43）；另210条未映射。amass_path为逻辑SMPL+H路径，source_container/source_key才是已有SMPL-X真实来源。无手脸补零、轴变换、接地、重定向、机器人筛选、T5或训练；training_ready=false。尚需完整下载、跨机SHA和全量时长核验，不把映射命中作为最终有效样本数。
- 验证：新增13项CPU数据契约测试全部通过（0.78秒），覆盖ZIP、缺动作/空caption、帧数与时长错配、NaN、模型维度、DMPL、跨±π SLERP、身体/手部/shape保留、精确路径与内嵌身份检查。Ruff F/I和格式通过，git diff --check通过；临时目录genmo-kitml-tests-*已由TemporaryDirectory自动清除，源码/测试目录无新增Python、pytest或ruff缓存。正式下载及全量构建待提交推送、服务器ff-only同步后执行。

## 2026-09-20 16:02：[feature/bumi-text-only] 修正 KIT 官方 MMM 时间戳记录精度的采样检查

- 现场证据：官方02489的1387帧XML为60Hz，最后时间戳23.1秒；23.0833等六位有效数字使局部相邻差呈0.0166/0.0167，不能以median(diff)推断帧率或按微秒级固定容差误报非均匀采样。
- 修改：mmm_timing改由完整时间跨度与帧数估计周期，并核对整段均匀网格，容差仅覆盖六位有效数字记录误差；仍要求所有相邻时间严格递增。新增真实精度样式回归，另注入0.01秒跳变确认会拒绝。不会修补动作、不重定时或放宽MMM/AMASS时长门禁。
- 验证：14 passed（0.76秒），Ruff F/I和格式、diff检查通过；本次/tmp/genmo-kitml-tests-*自动清理、无缓存。官方ZIP完整下载4,159,992,190字节，15,644成员CRC全通过，SHA ba51bb70e3b7aa9c3b5539d543336b4cdca5a7ee5ad730b82f1449a2ebed4b43；已有AMASS传输SHA两端一致8112dfdb901be7559b1abf2373114ba7deb14c88078cc8d4db5f722603d53a03。初轮构建尚未验收，待本修正推送和ff-only同步后重新构建再交付。
